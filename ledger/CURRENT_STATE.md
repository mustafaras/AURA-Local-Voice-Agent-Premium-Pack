# Current State

This file is a compact, atomically replaced projection of the append-only ledger.

- Phase: On-device TTS fallback (System TTS) integration in progress
- Active milestone: 20_RELEASE_READINESS → TTS_FALLBACK
- Active task: Implement and test `SystemTTSEngine` as the production fallback TTS adapter; wire it into `AuraKernel`; Chatterbox/Dia neural adapters are deferred.
- Last verified commit: `69c6c90` ("docs(ledger): post-push atomic state update for Phase 20 release readiness"), pushed to `origin/main` on 2026-07-27.
- Build status: Passes (`swift build --target AURA --build-path /tmp/aurabuild-tts`, zero errors, zero non-linker warnings)
- Test status: Passes via `./scripts/aura-test.sh /tmp/aurabuild-tts <bundle>` for `AURAIntegrationTests` 7/7, `AuraAgentTests` 205/205, and `SystemTTSEngine` unit tests 6/6. `AuraAudioTests` bundle contains a pre-existing flaky `antiTriggerSuppressesWakeDuringOutput()` failure unrelated to this TTS work; it fails on both `main` and the working tree. The legacy `wakePipelineAcceptsWakeAndReportsMetrics()` test is also flaky and sometimes fails on `main`.
- Known blockers: None
- Pending confirmations:
  - **Phase 20 is implemented but not committed.** The diff is ready for review; the user must explicitly authorize commit/push and is the only one who can authorize a release.
  - **Latency acceptance-gate evidence is mock-engine-derived only.** `Conversation` emits `LatencyMeasuredEvent` for `wakeToAck` (budget 0.5 s) and `simpleCommandCompletion` (budget 1.5 s) on the deterministic `MockTTSEngine`/`DeterministicMockSTTEngine` path; `PerformanceSampler` aggregates these into `SystemHealthSnapshot`. Tests pass under deterministic clocks, but real-device wake-word/STT/TTS performance is still TBD and documented as such in `docs/testing/38_PERFORMANCE_BUDGETS.md`.
  - **Energy budget remains TBD.** No real on-device models or target hardware exist in this workspace, so energy/thermal claims are not made.
  - **Packaging is design + placeholder scripts only.** `Resources/AURA.entitlements`, `Resources/AURA-Info.plist`, `scripts/build-app-bundle.sh`, `scripts/codesign-adhoc.sh`, and `scripts/verify-signature.sh` are in place and executable. They produce and ad-hoc sign a local `AURA.app`; no Developer ID certificate, `notarytool`, or distribution step is performed. `scripts/verify-signature.sh` currently validates the placeholder `AURA.app` only after `build-app-bundle.sh` is run.
  - **Update mechanism is documented but not implemented.** `docs/operations/UPDATE_MECHANISM.md` specifies a privacy-first, fail-closed, user-controlled XPC-helper design with signature/notarization validation and rollback. The helper, metadata endpoint, approval flow, and install assistant are deferred until a signed release exists.
  - **Accessibility permission tests are deterministic.** `AuraAutomation` now depends on `any AccessibilityHealthChecking`; `AccessibilityHealthSpy` allows granted/denied states to be tested without real `AXIsProcessTrustedWithOptions` side effects.
  - **No network entitlement is enabled.** `com.apple.security.network.client` and `com.apple.security.network.server` are `false` in `Resources/AURA.entitlements`; `AURA.app` is offline by default. Camera access is explicitly disabled with a denied-usage description. Code injection/debugging hardened-runtime flags are disabled for release builds.
  - **Resolved in this working tree:** `AuraKernel` no longer uses `MockTTSEngine` as the default TTS; `SystemTTSEngine` is wired in via a priority-aware `makeTTSEngine` factory that falls back to macOS `AVSpeechSynthesizer`.
  - **Unresolved risks carried forward:** no real on-device STT or wake-word; Chatterbox/Dia TTS adapters are not implemented; `AuraScreen`/`AuraComputerUse`/`AuraSecurity`/`AuraPlugins`/`AuraVSCode`/`WorktreeManager`/`MultiAgentOrchestrator` remain unconstructed by `AuraKernel`; real Accessibility/Screen-Recording UX not validated in this sandboxed environment; `swiftpm-testing-helper` hang workaround in `scripts/aura-test.sh` should be removed after a toolchain fix; `AuraAudioTests` wake-pipeline tests show pre-existing flakiness on this machine.
  - **Flaky test note:** `WakeWordPipelineTests.antiTriggerSuppressesWakeDuringOutput()` consistently records `metrics.antiTriggerSuppressions == 0` in this environment, even without the TTS changes. `wakePipelineAcceptsWakeAndReportsMetrics()` also intermittently fails on `main`. These failures are isolated to the synthetic marker-tone test path and do not affect production runtime or the TTS integration.
- Next safe action: User direction required. Options: (1) continue TTS roadmap — research/prototype Chatterbox on-device adapter; (2) fix the pre-existing `WakeWordPipelineTests` flakiness; (3) proceed to another task of the user's choosing. No release without explicit authorization.
