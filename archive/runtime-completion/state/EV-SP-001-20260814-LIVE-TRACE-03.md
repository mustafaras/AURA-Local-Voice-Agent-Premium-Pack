# EV-SP-001-20260814-LIVE-TRACE-03

- Evidence class: direct user-present live UI and local-process evidence; redacted control-plane summary.
- Timestamp: 2026-08-14T08:44:20Z closeout capture; live interaction window preceded this closeout on the same session.
- Branch/commit: `main`; `HEAD == origin/main == 76ce21ab423bd3c828e3386fb7174bf11ec56862`. The verified non-projection baseline remains `d82fde6be6e95bc8d3ccb64341bd2538baf12a92`.
- Environment: target Mac, macOS 27, Apple Silicon arm64, Swift 6.4; local unsigned test bundle `/tmp/aura-sp001-live/AURA.app`; no installation, signing, TCC mutation, dependency/model installation, provider contact, commit, push, or merge.
- Authority: explicit user-present authority for local AURA launch and safe/reversible mutation only.
- Procedure and result:
  - Launched the locally built bundle with `/usr/bin/open /tmp/aura-sp001-live/AURA.app` so the app was not attached to a terminal.
  - User-present speech observation completed: the user said `merhaba`; the UI showed the user transcript and AURA response, ending at `Idle — speech complete`.
  - Safe observation `çalıştır date` was allowed once. The UI showed `Command completed with exit code 0`; this was read-only and caused no known mutation.
  - A repeated `çalıştır date` request was denied. The UI showed `Blocked: confirmationDenied`; no denied action was executed.
  - Calculator was manually opened by the user because the app-activation path truthfully timed out when the app was not running. `kapat Calculator` displayed `app.terminate` confirmation; the user selected `Allow Once`; the UI showed `Quit com.apple.calculator.` and a read-only process check returned `NOT_RUNNING`.
  - A confirmation was left untouched until it disappeared. No execution result appeared; the UI ended at `Restricted — thinking timeout`. This is fail-closed no-execution evidence, but the UI did not label the terminal reason as `confirmation timeout`.
  - Changed-plan test submitted `çalıştır date` and then `çalıştır pwd` while the confirmation flow was active. The prior request was blocked with `confirmationDenied`; no success or execution result for `pwd` appeared.
  - The live UI showed the emergency-stop interlock state `Stopped — Generated input is disabled until explicitly re-armed`; after re-arm, voice remained restricted without changing permissions. No destructive action was executed.
  - The app was quit and reopened with `/usr/bin/open`. The fresh UI showed an empty conversation and `Restricted — Complete voice permission onboarding`; no prior confirmation card or approval was present after restart.
- Scope: OPEN-02 / SP-001 only. SP-002 and every later prompt remained unopened.
- Direct artifacts: local app bundle `/tmp/aura-sp001-live/AURA.app`; direct process verification command `pgrep -f '/tmp/aura-sp001-live/AURA.app/Contents/MacOS/AURA'` observed a new process after restart. User screenshots remain in the conversation surface and were not copied into the repository to avoid retaining private screen content.
- Truthfulness limitation: the UI exposed confirmation/action/result text and runtime status, but no redacted correlation ID or causation ID was displayed. `AuraEventBus` is in-memory and the live app does not persist the required event chain into a durable evidence ledger. Therefore the allow-to-execute-to-verify chain is directly observed at the UI/process level but not independently correlated by a persisted ID bundle.
- Additional limitations: dismissal was exercised only through the user-initiated app quit/restart path; a distinct UI dismissal event was not separately exposed. The timeout terminal label was `thinking timeout`, not an explicit confirmation-timeout label. Failed-verification and concurrent-turn isolation were not proven by this safe mutation.
- Verdict: direct live evidence substantially reduces OPEN-02, proving safe observation, displayed confirmation, one allowed reversible mutation, deny, changed-plan, replay protection by repeated approval request, emergency-stop interlock, and restart fail-closed behavior. The universal postcondition remains unproven because correlation/causation IDs and a distinct dismissal/explicit confirmation-timeout trace are not available in the live UI or durable event ledger. Keep SP-001 blocked.
