# EV-SP-022-20260826-LIVE-UI-01

- **Evidence ID:** `EV-SP-022-20260826-LIVE-UI-01`
- **Prompt / gap:** SP-022 / OPEN-10 / R9
- **Timestamp:** 2026-08-26T01:30:00Z
- **Branch / commit:** `main`; `HEAD == origin/main == 4d6022fa83c5b880d3544234d62cab6ef78d674e`; working tree dirty with the SP-022 Task Center slice + this record, uncommitted at capture time
- **Class:** Live UI observation via macOS Accessibility (AX) tree, user present / authority granted
- **Environment:** `AURA.app` built from the SP-022 source slice via `scripts/build-app-bundle.sh` and `scripts/codesign-adhoc.sh`; launched `open -n -a` in an isolated profile (`CFFIXED_USER_HOME=/tmp/aura-sp022-profile`, `HOME=/tmp/aura-sp022-profile`, `AURA_SP022_LIVE_ACCEPTANCE=1`); macOS 27 / Apple Silicon / Swift 6.4
- **Authority:** user granted all permissions; app launch + computer use authorized; no TCC mutation performed.

## What was exercised live (AX driver) and observed

The app launched and the main window was brought up via the Window menu (`Window → AURA`). The AX `entire contents` tree was read and controlled by identifier.

### A. Capability Center exposes the new Task Center controls truthfully (SP-022 source slice, live)
Navigated to the **Capabilities** tab. The capability list renders the SP-022-delivered
task lifecycle controls as **Hazır (Ready)** and **Yerel (Local)**:
- `Görevi Duraklat` (Pause Task) — Ready, Local
- `Görevi Sürdür` (Resume Task) — Ready, Local
- `Görevi Tekrar Dene` (Retry Task) — Ready, Local
- `Görevi İptal Et` (Cancel Task), `Görevi Durumu` (Task Status) — Ready, Local

Disabled capabilities render with an explicit reason and **no fake success**:
- `Computer-use run` → Disabled: "…not yet wired into the composition root; it
  requires an approved, live-validated beta app."
- `browser.read` → Disabled: "Safari bridge is not provisioned for this profile"
- `mail.read` → Disabled: "no approved mail account yet"
- `calendar.read`, `contacts.lookup` → Disabled: "reading is turned off in configuration"
- All VS Code capabilities → Disabled: "start disabled until the authenticated
  extension bridge is live"

### B. Recovery / diagnostics (live)
Recovery tab renders truthful permission states (Microphone/Speech/Accessibility
Granted; Screen Recording Denied), component health (ready/degraded/disabled with
reasons), wake-word `unsupported` (Push to Talk only), and "Support bundles are not
enabled in this R9 slice" — a truthful disabled state, not a fake success.

### C. Models / voice (no-model, no fake success)
Models tab renders "Kimlik doğrulama ve model kullanılabilirliği doğrulanmadı"
(authentication/model availability unverified) — no model claim is fabricated.
Speech recognition Granted; reference-voice cloning "not enabled… requires consent."

### D. Privacy / memory / integrations
Privacy tab shows integrations Not connected with disabled reasons and next steps;
cloud context disabled by machine policy; memory "0 visible of 0 records" (no
inspectable records — truthful for a fresh isolated profile). Audit/security memory
explicitly excluded from inspection/export.

### E. Onboarding
Opening onboarding shows the terminal **"Kurulum tamamlandı"** (Setup complete)
stage in this profile (permissions were already granted), with the Close control
(`aura.onboarding.close`) reachable and working.

### F. Emergency Stop (live)
Triggered Emergency Stop via Cmd+Shift+Esc (the app's documented shortcut). The
status pill changed live to **"Durduruldu. Üretilen giriş açıkça yeniden
etkinleştirilene dek devre dışı"** (Stopped. Generated input disabled until
explicitly re-armed) — the emergency-stop postcondition is observable and
truthful.

## What was NOT exercised live (remaining SP-022 residual)

- **Live durable-task pause/resume/retry state change on a real backend turn:**
  the Task Center renders the controls (Ready), but the isolated profile has no
  authenticated/live coding backend (claude/codex auth+model unverified, copilot
  dependencyMissing) and no durable task was enqueued. The state-transition
  postcondition of pause/resume/retry on a live-running task was **not**
  demonstrated end-to-end; it is proven deterministically by
  `EV-SP-022-20260826-TASK-CONTROLS-SOURCE-01` (`AuraTasksTests` retry + scope
  tests, `pauseAndResumeRunningTask`), but not on a live app turn.
- **TCC permission denial/revocation:** not mutated (deliberately; requires a
  live user action in System Settings). Onboarding denial/revoke/restart recovery
  was not exercised end-to-end on a truly clean profile where the permission is
  missing.
- **Support-bundle generation / safe-reset execution:** the Recovery tab
  truthfully reports support bundles are not enabled in this R9 slice; the
  surface does not offer a fake export.

## Falsifier
A capability that renders Ready but policy-denies on the live path; a task
control that reports a state change it did not make; a disabled capability
without a reason; or an emergency stop that does not disable generated input.

## Limitations
- AX observation is the programmatic equivalent of user interaction; the user
  was present and granted authority, but I (agent) drove the AX driver rather
  than the user clicking each control.
- Live durable-task pause/resume/retry and true TCC denial/revocation/restart
  recovery remain open; they require a live coding backend and a real
  permission-change, both out of this run's isolated/no-TCC scope.

## Next safe action
Exercise the live durable-task pause/resume/retry state change and the real
TCC denial/revocation/restart recovery on a clean profile under an approved
live-backend turn, then mark SP-022 completed and open SP-023.
