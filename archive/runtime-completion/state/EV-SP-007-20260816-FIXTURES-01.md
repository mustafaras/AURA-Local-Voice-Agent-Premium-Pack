# Evidence: EV-SP-007-20260816-FIXTURES-01

## Identity

- **Evidence ID:** EV-SP-007-20260816-FIXTURES-01
- **Prompt:** SP-007 — Live Computer-Use Planner in Approved Apps
- **Gap:** OPEN-05 (R4: Computer-Use Productization)
- **Timestamp:** 2026-08-16T17:30:00Z
- **Branch:** main
- **Commit:** 4a5040c89b53998836628236d10495b284b1415f
- **Session:** AURA-SP-007-COMPUTER-USE-20260816

## Command / procedure

1. Read Tier-0 context (SECOND_PASS_READ_FIRST, SECOND_PASS_CONTROL_CONTRACT,
   SECOND_PASS_PROMPT_CONTRACT, SECOND_PASS_STATE.json,
   SECOND_PASS_OPEN_GAPS.md, current-state.json, session-handoff.json,
   ACTIVE_CONTEXT.md) and Tier-1 context (ADR-039, ComputerUseControlLoop,
   DeterministicComputerUsePlanner, ComputerUseBetaAllowlist,
   ComputerUseAppFixtures, ComputerUseVerifier, R4ProductizationTests).
2. Built `AuraComputerUse` target: `swift build --target AuraComputerUse` →
   Build complete.
3. Ran `AuraComputerUseTests` via `./scripts/aura-test.sh /tmp/aura-sp007-build
   AuraComputerUseTests` → 68/68 tests passed, 0 failed bundles.
4. Ran the full 21-bundle regression via `./scripts/aura-test.sh
   /tmp/aura-sp007-full` → 21/21 bundles PASSED, 0 failed bundles.

## Environment

- macOS 27+/arm64, Swift 6.4, Xcode-select toolchain.
- Local build at `/tmp/aura-sp007-build` and `/tmp/aura-sp007-full`.
- No app launch, TCC mutation, Accessibility/Screen Recording grant, or
  network provider call was performed.

## Result

### Source changes (edit-only authority)

**`Sources/AuraComputerUse/ComputerUseAppFixtures.swift`**: Expanded the
fixture table from 2 apps (Finder, Terminal) with 1 task each to 3 apps
(Finder, Terminal, Notes) with 3 tasks each — one per required action type:

| App | Action type 1 (A11y-anchored) | Action type 2 (coordinate fallback) | Action type 3 (confirmation-required) |
|---|---|---|---|
| Finder | `focus_search_field` (AXTextField anchor) | `quick_look_preview` (normalized 0.5, 0.6) | `open_folder_new_window` (navigate intent) |
| Terminal | `focus_terminal` (AXTextArea anchor) | `print_working_directory` (normalized 0.5, 0.95) | `clear_screen` (navigate intent) |
| Notes | `focus_note_body` (AXTextArea anchor) | `create_new_note` (normalized 0.9, 0.05) | `delete_current_note` (destructive / mandatory-confirmation) |

Each plan's step carries:
- an accessibility anchor or bounded normalized coordinate;
- a semantic intent in the closed vocabulary (`.observe`, `.navigate`,
  `.delete`);
- a target app bundle identifier matching the observation; and
- a paired `ComputerUsePostcondition` semantic predicate.

The `delete_current_note` task uses `.delete` intent — a
mandatory-confirmation intent that the control loop's mandatory
confirmation gate blocks even on a bare `.allow` policy decision
(`ComputerUseSemanticIntent.requiresMandatoryConfirmation == true`).

### Test changes

**`Tests/AuraComputerUseTests/R4ProductizationTests.swift`**: Added 8 new
tests covering the three-app / three-action-type fixture coverage:

- `finderFixturesCoverThreeActionTypes` — verifies Finder has 3 tasks
  covering A11y-anchored, coordinate fallback, and confirmation-required.
- `terminalFixturesCoverThreeActionTypes` — same for Terminal.
- `notesFixturesCoverThreeActionTypes` — same for Notes; verifies the
  confirmation task's intent `.delete` requires mandatory confirmation.
- `plannerEmitsA11yAnchoredPlanForFinder` — planner emits a plan with
  accessibility hints for `focus_search_field`.
- `plannerEmitsCoordinateFallbackPlanForFinder` — planner emits a plan
  with coordinate fallback for `quick_look_preview`.
- `plannerEmitsConfirmationPlanForNotesDelete` — planner emits a `.delete`
  intent plan requiring mandatory confirmation for `delete_current_note`.
- `plannerEmitsPlansForAllThreeApprovedApps` — planner emits typed,
  app-scoped plans for all three approved apps.

Updated 4 existing tests to use the new fixture objective keys
(`focus_search_field` replacing the old `reveal_downloads`).

### Deterministic test results

- `AuraComputerUseTests`: 68/68 tests passed, 0 failed bundles.
- Full 21-bundle regression: 21/21 PASSED, 0 failed.

## Artifact path / hash

- `Sources/AuraComputerUse/ComputerUseAppFixtures.swift` (modified)
- `Tests/AuraComputerUseTests/R4ProductizationTests.swift` (modified)
- Build artifacts at `/tmp/aura-sp007-build` and `/tmp/aura-sp007-full`.

## Scope and limitations

**This is source-side structural-readiness evidence only.** It does NOT
close OPEN-05.

The SP-007 completion gate requires:
> Three approved apps pass the required live tasks with semantic
> verification and no unsafe fallback; otherwise `computerUse.run` stays
> disabled.

**What was done:** the fixture table now covers the three approved apps
(Finder, Terminal, Notes) the prompt's "at least three approved beta
applications" requires, and each app's fixtures cover the three action
types the procedure requires (Accessibility-anchored, bounded coordinate
fallback, confirmation-required). The planner, allowlist, confirmation
store, verifier, and control loop all pass their deterministic test
suites with zero regressions.

**What was NOT done:** no live validation was performed. The SP-007
procedure step 1 requires "explicit user-present Accessibility and Screen
Recording authority" and the hard boundaries prohibit launching apps or
mutating TCC unless the prompt's authority explicitly permits it.
`SECOND_PASS_STATE.json` authority has `launch_or_install_app: false` and
`mutate_permissions: false`. All apps remain `.disabled` on the
`ComputerUseBetaAllowlist.initial` — only `.validating()` after live
evidence opens an entry. `computerUse.run` stays disabled.

**Blocker:** the live gate cannot be passed without explicit user-present
Accessibility/Screen Recording authority and app-launch authority. The
prompt's own completion gate states "otherwise `computerUse.run` stays
disabled."

## Falsification test

If any of the following is true, this evidence is invalid:
- A fixture's accessibility anchor or coordinate fallback is out of bounds
  or semantically wrong for the app.
- The planner emits a plan for an unapproved or disabled app.
- The planner emits a step with a raw model-text-driven kind.
- The full regression suite has any failed bundle.
- The mandatory-confirmation gate does not block the `.delete` intent step
  on a bare `.allow` policy decision.

## Residual risk

- `RISK-NO-PRODUCTION-COMPUTER-USE-PLANNER` remains **Mitigating** — the
  planner, allowlist, confirmation store, and verifier exist and pass
  deterministic tests, but live beta-app evidence has not been performed.
- `RISK-SP-006-URL-OPEN-FAILS-LIVE` and
  `RISK-SP-006-CONFIRMATION-EXPIRY-UNEXPLAINED` (forwarded from SP-006)
  remain open and do not block SP-007's structural readiness.
- SP-007 remains `blocked` on the live gate; SP-008 must not be opened.