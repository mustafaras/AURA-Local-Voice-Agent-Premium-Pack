# Evidence: EV-SP-009-20260817-CLOSEOUT-03

## Identity

- **Evidence ID:** EV-SP-009-20260817-CLOSEOUT-03
- **Prompt:** `15_SESSION_CLOSEOUT.prompt.md` (mandatory after every SP-009 attempt)
- **Class:** Process / closeout
- **Timestamp:** 2026-08-17
- **Session:** AURA-SP-009-PACKAGING-AUTH-20260817
- **Why it exists:** SP-009's required records mandate running the closeout
  prompt "after this attempt, even if blocked". The first SP-009 attempt did
  not run it — that omission is defect 2 in
  `EV-SP-009-20260817-CORRECTION-02`, and this record closes it.

## 1. Verified state before writing the handoff

| Item | Value |
|---|---|
| Branch | `main` |
| `HEAD` | `92c45f60b5b564b016122de7238e4d7f2b34a7ed` |
| Remote relation | `HEAD` == `origin/main` (identical SHA) at closeout start |
| Working tree | Dirty — SP-009 product, test, extension, evidence, and state changes staged for delivery |
| Active prompt / state | SP-009 completed (corrected); SP-010 pending and unopened |

### Unrelated changes deliberately left alone

Four untracked SP-006 artifacts predate this session and are **not** swept into
the SP-009 delivery:

- `EV-SP-006-20260816-7SCENARIO-02.entries.log`
- `EV-SP-006-20260816-LIVERERUN-05.entries.log`
- `EV-SP-006-scenario3-quit.png`
- `EV-SP-006-scenarios1-2-ui.png`

They were untracked across previous deliveries too. Two are screenshots; the
prompt boundary keeps raw screenshots out of ledgers and context files, so they
stay out of this commit rather than being committed as a side effect.

### Files changed this session

Product: `ProductivityAdapters_SafariBridgeEnvelopeWriter.swift` (new),
`ProductivityAdapters_SafariBridgeNativeMessage.swift` (new),
`ProductivityAdapters_SafariBridgeSecurity.swift`,
`ProductivityAdapters_AuthenticatedSafariTransport.swift`,
`SafariBridgeAvailability.swift`.

Extension: `Resources/SafariExtension/background.js`, `manifest.json`,
`README.md`; `content.js` deleted.

Tests: `Tests/AuraProductivityTests/AuraProductivityTests.swift` (+5).

Records: `EV-SP-009-20260817-CORRECTION-02.md` (+ regression log),
`EV-SP-009-20260817-PACKAGING-AUTH-01.md` (correction banner), this file,
`EVIDENCE_INDEX.md`, `RISK_REGISTER.md`, `SECOND_PASS_LEDGER.md`,
`PROGRAM_LEDGER.md`, `PROJECT_LEDGER.md`, `SECOND_PASS_OPEN_GAPS.md`,
`SECOND_PASS_STATE.json`, `current-state.json`, `capability-matrix.json`,
`session-handoff.json`, `ACTIVE_CONTEXT.md`.

### Commands actually run

| Command | Result |
|---|---|
| `swift build --target AuraProductivity` | Clean |
| `./scripts/aura-test.sh` | 21/21 bundles, 954/954 tests, 0 failed |
| `node --check Resources/SafariExtension/background.js` | OK |
| `python3 scripts/validate_second_pass_program.py` | exit 0 |
| `python3 scripts/validate_runtime_completion.py` | exit 0 |
| `python3 scripts/validate_repo_hygiene_program.py` | exit 0 |
| `python3 scripts/validate_repo_hygiene_supply_chain.py` | exit 0 |

`swift test` was also attempted directly and failed to codesign test bundles in
the iCloud-synced working directory. That is the known environment limitation,
not a product failure; the repository runner builds into `/tmp/aurabuild` and
passes. **No live browser, no extension install, and no Safari run occurred.**

### Evidence IDs created this session

- `EV-SP-009-20260817-CORRECTION-02` (correction + deterministic source-side)
- `EV-SP-009-20260817-CLOSEOUT-03` (this record)

## 2. Diff review

- Scope: confined to the Safari bridge slice, its tests, its package, and the
  SP-009 records. No unrelated product area touched.
- Secrets: none added. The shared secret stays in the Keychain-backed store;
  no secret, token, cookie, raw page text, or account data appears in any
  record, fixture, or ledger.
- Generated files/schemas: `session-handoff.json` and `capability-matrix.json`
  were brought back **into** schema compliance, not around it — no schema was
  relaxed to make a validator pass.
- No TODO or placeholder is represented as complete; the extension is still
  explicitly unbuilt and uninstalled.
- Acceptance criteria were strengthened, not weakened: constant-time tag
  verification, a new fail-closed error state, and a narrower manifest surface.
- Documentation matches implementation: the extension README now describes the
  producer/consumer split that the code actually implements.

## 3. Acceptance verdict by criterion

| SP-009 completion-gate criterion | Verdict |
|---|---|
| Real packaged bridge | **PASS at the deterministic boundary** — extension → handler → writer → transport → adapter is complete and covered end-to-end from the real wire format; not installed or live-run |
| Authenticated | **PASS** — HMAC-SHA256 envelope over canonical JSON, constant-time verification, version/identity/profile/nonce/freshness all enforced |
| Bounded | **PASS** — active-tab visible text only, ≤ 20 000 characters, bounded raw message, no cookies/passwords/hidden state |
| Revocable | **PASS** — revoking the secret fails both halves closed, proven by test |
| Visibly degraded when unavailable | **PASS** — six distinct transport states map to distinct user-presentable availability reasons |
| Capability kept disabled until live verification | **PASS** — `browser.read` remains disabled |

## 4. Blockers, open decisions, residual risks

- **No blockers.** SP-009 is complete for its deterministic boundary.
- `RISK-SAFARI-BRIDGE-NOT-LIVE` — **open**, owns install/convert/sign and the
  real native-messaging round trip. Outside SP-009: that authority was never
  granted to this prompt.
- `RISK-MISSING-PRODUCTIVITY-ADAPTERS` — remains **Mitigating**; NLU/UI
  reachability, provider onboarding, mutation/send, and live acceptance stay
  with SP-010/SP-011.
- Open decision forwarded: the freshness window (5 s container staleness vs
  30 s envelope lifetime) should be settled against live timing in SP-010/SP-011.

## 5. Authority

Edit-only for all corrections, plus an explicit in-turn user go-ahead for
**commit, push, and merge**, which is exercised at the end of this session and
does not carry into the next one. No install, launch, TCC mutation, provider
contact, beta enrollment, signing, notarization, release, or deployment
occurred or was authorized.

## 6. Exact next safe action

SP-010 (provider/account composition and UI) is next eligible and stays
**pending and unopened**; open it only under its own authority. After the
delivery commit, the state projections must be aligned to the delivery commit
SHA, matching the program's established two-commit pattern.
