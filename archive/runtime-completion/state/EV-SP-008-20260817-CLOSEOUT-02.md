# Evidence: EV-SP-008-20260817-CLOSEOUT-02

## Identity

- **Evidence ID:** EV-SP-008-20260817-CLOSEOUT-02
- **Prompt:** `15_SESSION_CLOSEOUT.prompt.md`, mandatory after the SP-008 attempt
- **Gap:** OPEN-15 (session closeout procedure)
- **Timestamp:** 2026-08-17T06:57:03Z
- **Branch:** `main`
- **Commit:** `0000b4afae1dc1bc748f7cf1f4ae22a00916e592`; `HEAD == origin/main`
- **Session:** AURA-SP-008-ADVERSARIAL-20260817

## 1. Repository and authority state

| Field | Value |
|---|---|
| Branch | `main` |
| `HEAD` | `0000b4afae1dc1bc748f7cf1f4ae22a00916e592` |
| `origin/main` | `0000b4afae1dc1bc748f7cf1f4ae22a00916e592` (equal) |
| Worktree | `dirty_expected` |
| Active prompt | `SP-009` pending/unopened; `SP-008` completed |
| Authority | edit-only |

User-owned worktree contents: all eleven SP-008 source/test changes plus the
SP-008 evidence and state projections, and the four intentionally untracked SP-006
evidence artifacts (`.log`/`.png`, which supply-chain policy forbids tracking and
H-003 forbids ignoring). Nothing else.

Not exercised and not authorized: install, launch, TCC mutation, provider contact,
beta enrollment, signing, notarization, commit, push, merge, release, deploy.

## 2. Diff review

Reviewed for accidental scope, secrets, stale claims, weakened assertions,
generated-file drift, and missing documentation.

- **Scope:** every source change sits inside the computer-use adversarial/recovery
  boundary SP-008 names. The one change outside `AuraComputerUse` —
  `ScreenCaptureBlockReason.windowNotVisible` in `AuraCore` plus
  `ScreenContextEngine.exclusionReason(for:)` — exists because the computer-use
  loop surfaced a hidden-window refusal under a sensitive-application label, which
  is squarely SP-008's "no hidden window/permission bypass" clause.
- **Secrets:** none added. No raw audio, screenshots, tokens, account data, or
  unredacted model output entered any ledger or context file. The adversarial test
  fixtures contain synthetic attacker strings only.
- **Assertions:** no test was weakened. Every new adversarial case asserts the
  executor call count in addition to the outcome, which is strictly stronger than
  the pre-existing style. One pre-existing test
  (`secureFieldFocusBlocksStepExecution`) already ignored the outcome and asserted
  only zero executions, so the new terminal case did not require changing it.
- **Generated files:** none regenerated; `.build` output is untracked.
- **Documentation:** gap register, evidence index, risk register, all three
  ledgers, machine state, handoff, and `ACTIVE_CONTEXT.md` updated; historical
  wording preserved and older overlays demoted rather than deleted.

## 3. Verification results

| Check | Command | Result |
|---|---|---|
| Product build | `swift build --product AURA` | Clean |
| Full Swift regression | `./scripts/aura-test.sh /tmp/aurabuild-sp008-full` | **21/21 bundles, 931/931 tests, 0 failed** |
| Focused bundle | `./scripts/aura-test.sh … AuraComputerUseTests` | 93/93 |
| Second-pass validator | `python3 scripts/validate_second_pass_program.py` | exit 0 |
| Runtime-completion validator | `python3 scripts/validate_runtime_completion.py` | exit 0 **(was failing before this session — see §4)** |
| Repo-hygiene validator | `python3 scripts/validate_repo_hygiene_program.py` | exit 0 |
| Supply-chain validator | `python3 scripts/validate_repo_hygiene_supply_chain.py` | exit 0 |
| Governance unit tests | `python3 -m unittest discover -s scripts/tests -p 'test_*.py'` | **38/38 OK** |
| Python compile | `python3 -m compileall -q scripts` | OK |
| Shell syntax | `zsh -n scripts/*.sh` | OK |
| Whitespace/diff | `git diff --check` | OK |
| JSON parse | all `AURA_RUNTIME_COMPLETION/**/*.json`, `ledger/**/*.json` | OK |

`Package.swift` declares exactly 21 test targets, so no bundle was skipped.
Regression totals were recomputed from the log rather than read off a summary
line; log SHA-256
`7f98b3b78e8b818ff92393f88bbe188a5de798596c65324f92a8ef971b15d111`.

## 4. Inherited defect found and repaired during closeout

`validate_runtime_completion.py` was failing **before this session began**,
confirmed by stashing every SP-008 change and re-running at clean `HEAD`, where it
failed identically.

- **Cause:** SP-007's delivery commit `0000b4a` changed product source
  (`AuraKernel_Construction.swift`, `ComputerUseAppFixtures.swift`,
  `R4ProductizationTests.swift`) but left `repository.verified_head`,
  `repository.remote_head`, and `capability-matrix.json`'s `repository_commit` at
  `9774287a6fb8512a85f22cda5d0f019a8bc6ea48`. The validator's
  `projection_only_paths` check therefore saw non-projection changes between
  `verified_head` and live `HEAD`, and failed closed — correctly.
- **Repair:** all three pointers now name
  `0000b4afae1dc1bc748f7cf1f4ae22a00916e592`, which is exactly `origin/main`.
  `user_owned_changes` was rewritten to disclose SP-008's uncommitted changes and
  to record this repair.
- **Stated limitation:** content verification at `0000b4a` rests on **SP-007's
  recorded 21/21 sweep**, not on a fresh clean-tree sweep by this session. This
  session's 21/21 / 931-test sweep ran against `0000b4a` **plus** SP-008's
  uncommitted changes. The user was offered a clean-tree re-verification and chose
  the direct repair.

## 5. Acceptance verdict

The mandatory closeout procedure passed. This closes the **session procedure**
only. It does not close any live, beta, release, signing, deployment, or
first-pass R2–R12/FINAL gate; all of those remain independently open.

SP-008 is `completed` for OPEN-05's adversarial and recovery residuals **at the
deterministic boundary its authority covers**. SP-009 is next eligible and remains
`pending` and unopened.

## 6. Residual risks carried forward

New: `RISK-SP-008-LIVE-ADVERSARIAL-RESIDUAL`,
`RISK-SP-008-PLANNER-DECLARED-INTENT-TRUST`.

Unchanged: `RISK-SP-006-URL-OPEN-FAILS-LIVE`,
`RISK-SP-006-CONFIRMATION-EXPIRY-UNEXPLAINED`,
`RISK-INJECTION-COVERAGE-NON-DIALOGUE`, `RISK-SP-004-TOCTOU-RACE`,
`RISK-SP-004-HANDLER-COMPROMISE`, `RISK-SP-003-MODEL-LATENCY`,
`RISK-SP-003-LIVE-VOICE-RESIDUAL`, `RISK-STT-MIC-NOT-CAPTURING`.

## 7. Exact next action

Open `SP-009` only under its own explicit authority. Read
`RISK-SP-008-LIVE-ADVERSARIAL-RESIDUAL` first: SP-008's guards are proven in
control flow, but `AccessibilitySecureFieldDetector` and
`AccessibilityModalDialogDetector` have never been observed against a real
credential field or a real `SecurityAgent` dialog, and a detector that silently
returns `false` makes every guard above it inert while all tests still pass.

Authority resets to edit-only. SP-008's changes are local and uncommitted;
delivery requires an explicit in-turn go-ahead.
