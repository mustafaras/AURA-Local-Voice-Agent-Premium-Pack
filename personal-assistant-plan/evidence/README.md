# Evidence

Captured command outputs, redacted label dumps, probe files, and numbers cited by `ledger/PHASE_LEDGER.md` entries, one subfolder per phase (`PA-0/` … `PA-6/`). Never a token, secret, account identifier, or unredacted transcript.
