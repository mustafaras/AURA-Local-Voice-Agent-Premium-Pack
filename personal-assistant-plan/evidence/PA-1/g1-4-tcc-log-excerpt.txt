TCC unified-log observation (PA-1 G1-4, 2026-09-16T17:49:26+0300)

`log show --last 15m --predicate 'subsystem == "com.apple.TCC"'` and `--predicate 'process == "tccd"'` both return 0 lines for the window covering launches 1-4 of the PA-1 bundle (and 0 lines for any process). On this macOS 27 host the unified log withholds tccd entries from an unprivileged reader; reading them would require Full Disk Access or sudo, which the plan forbids requesting (PA-1 prompt, hard boundaries). Recorded as the observation the design doc §6.4 anticipated; the permission states are evidenced by the Privacy-tab indicators (screenshots) and the absence of SecurityAgent / UserNotificationCenter windows during each launch instead.
