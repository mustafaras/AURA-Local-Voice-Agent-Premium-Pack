# PA Plan — Phase Ledger (append-only)

> Format per [00-working-protocol.md §3](../00-working-protocol.md). Entries are appended, never edited or deleted. The validator proves parity with the prompts.

---

## SEQ-0001 — 2026-09-15T18:59:32+03:00 — META — MACHINERY-CREATED

- evidence: personal-assistant-plan/ (README + docs 00–08 + prompts/PA-0..PA-6.prompt.md + ledger/ + evidence/ + validate-continuity.sh) created this session per the owner's explicit instruction of 2026-09-15 (quoted verbatim in README.md); every root-cause claim in README §2 cites file:line from the same-session source scan
- verified: `git status --short personal-assistant-plan/` → untracked plan folder with the full machine present; `ls personal-assistant-plan/prompts` → PA-0…PA-6

## SEQ-0002 — 2026-09-15T18:59:32+03:00 — META — PLAN-AWAITING-OWNER-DECISION

- evidence: sequencing PA-0 → PA-6 documented in 08-rollout.md §1; six owner decisions D-1…D-6 listed in README §7 with recommendations; no implementation started; CURRENT_PHASE.md initialized with all PA-0 gates `pending`, `phase_status: pending`; the predecessor plan was archived to docs/archive/ui-improvement-plan/ with CLOSED.md and its validator still reports OK from the archive path
- verified: `grep -c '| pending |' personal-assistant-plan/ledger/CURRENT_PHASE.md` → 7; `bash docs/archive/ui-improvement-plan/validate-continuity.sh` → "VALIDATOR: OK - machine coherent"

## SEQ-0003 — 2026-09-16T11:16:36+03:00 — META — PLAN-AMENDED-FOR-D-2

- evidence: before opening PA-0 the owner overrode the D-2 recommendation (verbatim: "d 2 kalsın değil computer use koruması istemiyorum"; scope chosen from the four offered options: "A + B + C — acil durdurma hariç hepsi"). Plan files amended prior to the APPROVAL line so the frozen prompt reflects the decision: README §7 D-2 row; 01-owner-trust-posture.md (§1 guard evidence rows, §2, new §3.3a, §3.4, §4, §5, §6 item 5, §7, §8); 07-cross-cutting-constraints.md §1 (emergency-stop row split from the lifted-guard row; ADR-018 row narrowed to redaction + zero retention); prompts/PA-0.prompt.md (mission, read list, allowed/forbidden files, gates G0-1…G0-8 with new [policy] G0-4, procedure, cognitive gate); ledger/CURRENT_PHASE.md gate mirror (8 rows). Kept by decision: emergency stop, identity-change detection, maxIterations, rate limit, redaction, zero retention.
- verified: `bash personal-assistant-plan/validate-continuity.sh` → "VALIDATOR: OK - machine coherent" with "[3] gate parity holds (PA-0: 8 gates)"

DECISION: D-1 — owner: "ONAY PA-0 — D-1..D-6 önerildiği gibi" — 2026-09-16 — confirmationRequirement .none for all owner-actor capabilities incl. agent.*Run, cloud inference, shell.exec, computer-use mutation actions (ADR-064)
DECISION: D-2 — owner: "d 2 kalsın değil computer use koruması istemiyorum" / "A + B + C — acil durdurma hariç hepsi" — 2026-09-16 — computer-use structural guards lifted for the owner posture: (A) mandatory-confirmation intent gate, (B) secure-field refusal in loop + executor and screen-context sensitive-application exclusion, (C) unexpected-modal halt, no-progress halt, per-plan step ceiling; emergency stop KEPT; identity-change detection, maxIterations, rate limit kept as bounds (ADR-064)
DECISION: D-3 — owner: "ONAY PA-0 — D-1..D-6 önerildiği gibi" — 2026-09-16 — adopt on-demand state "Hazır — istek üzerine bağlanır" with EN/TR copy for host-not-running capabilities (PA-3)
DECISION: D-4 — owner: "ONAY PA-0 — D-1..D-6 önerildiği gibi" — 2026-09-16 — Apple on-device SpeechAnalyzer/SpeechTranscriber keyword spotting; PTT fallback only if PA-4 G4-1 typecheck fails
DECISION: D-5 — owner: "ONAY PA-0 — D-1..D-6 önerildiği gibi" — 2026-09-16 — rename "Düzelt" to "Düzenle", keep memory.correctShort semantics and ADR-043 append-and-link behaviour (PA-5)
DECISION: D-6 — owner: "ONAY PA-0 — D-1..D-6 önerildiği gibi" — 2026-09-16 — accept and document any macOS 27 periodic Screen Recording re-approval unless observed live in PA-1 G1-4

APPROVAL: PLAN -> PA-0 — user token: "ONAY PA-0" — 2026-09-16 — owner opens PA-0 (Owner Trust Posture) with D-1…D-6 recorded above; D-2 resolved as lifted guards A/B/C, emergency stop kept

## SEQ-0004 — 2026-09-16T11:19:18+03:00 — PA-0 — G0-1 PASSED

- evidence: docs/decisions/ADR-064-owner-trust-posture.md written per ADR_TEMPLATE (Context quotes the 2026-09-15 instruction and the 2026-09-16 D-2 override verbatim; Decision 1–6 incl. lifted-guard list A/B/C and the kept list with emergency stop; non-transferable boundary; falsifiers; evidence classes). Status "Proposed" until G0-7. DECISION lines D-1…D-6 present in this ledger (SEQ-0003 block). README §1 "does not do" paragraph corrected to match D-2.
- verified: `ls docs/decisions/ADR-064-*.md` → docs/decisions/ADR-064-owner-trust-posture.md; `grep -c '^DECISION: D-[1-6]' personal-assistant-plan/ledger/PHASE_LEDGER.md` → 6
- adr: ADR-064

## SEQ-0005 — 2026-09-16T11:26:54+03:00 — PA-0 — G0-2 PASSED

- evidence: Sources/AuraPolicy/OwnerTrustPosture.swift (new, `isEnabled = true`, ADR-064 doc comment). Sources/AuraPolicy/DefaultPolicyGrants.swift: `ownerConfirmation(ownerTrustEnabled:)` / `ownerMutationConfirmation(ownerTrustEnabled:)`, `unscoped(ownerTrustEnabled:)`, `grants(ownerTrustEnabled:)`, `all = grants(ownerTrustEnabled: OwnerTrustPosture.isEnabled)`; the governed seeds are **eight**, not seven as the prompt/design doc counted (5 × `.always`: shellExec, agentCodexRun, agentClaudeRun, agentCopilotRun, agentOllamaCloudInference; 3 × `.forRiskTier(.mutation)`: appTerminate, lifecycleLaunchAtLogin, computerUseRun) — count corrected in ADR-064 and source comments, prompt left frozen. Tests/AuraPolicyTests/OwnerTrustPostureTests.swift (new, 6 tests: production enabled; enabled ⇒ all `.none`; disabled ⇒ eight legacy values; field-by-field diff proves the switch is the only difference; derived constants; seed marker). Pinned updates in DefaultPolicyGrantsTests.swift, each with an `// ADR-064` comment: appTerminate, shellExec, ollama cloud, launch-at-login ×2, computerUse/cloud seed test (legacy values re-asserted behind `grants(ownerTrustEnabled: false)`).
- verified: `./scripts/aura-test.sh /tmp/aurabuild AuraPolicyTests` → run1 (evidence/PA-0/g0-2-policy-tests-run1.log) 3 pinned failures as expected; run2 (g0-2-policy-tests-run2.log) "Test run with 51 tests in 2 suites passed", "PASSED: AuraPolicyTests", "Failed bundles: 0"; `grep -c 'confirmationRequirement: .always' Sources/AuraPolicy/DefaultPolicyGrants.swift` → 0
- adr: ADR-064

## SEQ-0006 — 2026-09-16T11:36:59+03:00 — PA-0 — G0-3 PASSED

- evidence: Tests/AuraIntentTests/OwnerGrantCoverageTests.swift (new, 4 tests) scans `InitialCapabilitySet.manifests()` (requiredCapability per manifest) **and** `Capability.forComputerUse(intent:)` for every `ComputerUseSemanticIntent` (the loop's per-step capability, which no manifest names), plus `screen.capture`. RED run (evidence/PA-0/g0-3-intent-tests-run1-red.log; detail in /tmp/aurabuild/out/Products/Debug/AuraIntentTests.log lines 214-239) found **nine gaps**, all "No matching grant and tier X is denied by default": manifests — `agent.coding_run → agent.run` [destructive], `app.hide → app.hide` [reversible], `vscode.run_task → vscode.runTask`, `vscode.cancel_task → vscode.cancelTask`, `vscode.run_tests → vscode.runTests`, `vscode.cancel_tests → vscode.cancelTests` [reversible]; computer-use steps — `navigate`/`toggleControl → computerUse.interact` [reversible], `fillField`/`submit → computerUse.mutate` [mutation], `send`/`publish`/`purchase`/`delete`/`deploy`/`acceptLegalTerms`/`authenticateOrChangeCredential → computerUse.destructiveAct` [destructive]. **Finding worth recording:** the three computer-use step capabilities had never been seeded, so under ADR-055's "confirm on mutation" posture every non-observe step was `.deny` → the loop stopped before acting; computer use could only observe in production. Fix: nine `.none` grants with `patterns: [.any]` (none carries a path/URL target; same shape as `.appActivate`) appended to `DefaultPolicyGrants.unscoped`, each commented with the gap and ADR-064. Pinned updates (ADR-064-commented): `ungrantedReversibleDenied` repointed from `.appHide` (now registered+seeded) to `.vscodeOpen` (reversible, unregistered, unseeded — deny wall still proven); `grantInventoryIsExact` extended with the nine identifiers. No filesystem-class grant widened; `noBroadPatternsOnTargetableCapabilities` still green.
- verified: `./scripts/aura-test.sh /tmp/aurabuild AuraIntentTests` → g0-3-intent-tests-run2-green.log "Test run with 157 tests in 10 suites passed", "Failed bundles: 0"; `./scripts/aura-test.sh /tmp/aurabuild AuraPolicyTests` → g0-3-policy-tests-rerun.log "Test run with 51 tests in 2 suites passed", "Failed bundles: 0"
- adr: ADR-064

## SEQ-0007 — 2026-09-16T11:46:16+03:00 — PA-0 — G0-4 PASSED

- evidence: Sources/AuraComputerUse/ComputerUseGuardPosture.swift (new: five flags; `.structural` all-true, `.ownerTrust` all-false, `production = OwnerTrustPosture.isEnabled ? .ownerTrust : .structural`). `ComputerUseControlLoop` gains `guardPosture: = .production`; `ComputerUseControlLoop_Run.swift` gates (A) the `requiresMandatoryConfirmation` block on `.allow`, (B) the secure-field probe (skipped entirely → no `.focused`/`.indeterminate` refusal), (C) the modal probe (skipped), the no-progress termination (counter still computed + emitted in `ComputerUseVerifyEvent`), and the `maxStepsPerPlan` rejection. `AXCGEventActionExecutor` gains `guardPosture: = .production` and skips its secure-field probe under `.ownerTrust`. `ScreenContextEngine` gains `sensitiveApplicationExclusionEnabled: = !OwnerTrustPosture.isEnabled` consulted only in `exclusionReason`; assistant self-exclusion, off-screen refusal, redaction, and retention unchanged. Comment-only corrections in `ComputerUseTypes.swift:59-71` and `Configuration_ComputerUseConfiguration.swift:3-11`. Kernel (`AuraKernel_Construction.swift`) untouched — inherits the defaults. Fixtures: `makeLoop` (+`guardPosture: = .structural`), the direct loop construction, `Fakes.makeScreenEngine` (+`sensitiveApplicationExclusionEnabled: true`), `ScreenContextEngineTests.makeEngine` (+ param, default true), nine `AXCGEventActionExecutor(` sites (+`guardPosture: .structural`), each tagged `// ADR-064`. New Tests/AuraComputerUseTests/ComputerUseGuardPostureTests.swift: 18 tests — preset derivation; (A) send executes under `.ownerTrust` / blocked under `.structural`; (B) focused + indeterminate secure field pass the loop under `.ownerTrust`, refused under `.structural`; executor mirrors; 1Password window listed under exclusion-off, excluded under exclusion-on, assistant self-exclusion held in both; (C) modal ignored / halts; identical observations reach `iterationBudgetExhausted(6)` with 6 executions under `.ownerTrust` vs `.noProgress` under `.structural`; 3-step plan over a 2-step ceiling executes 3 / rejected 0; emergency stop halts the loop and refuses the executor under **both** presets.
- verified: `./scripts/aura-test.sh /tmp/aurabuild AuraComputerUseTests` → evidence/PA-0/g0-4-computeruse-tests-run1.log "Test run with 125 tests in 0 suites passed", "Failed bundles: 0" (36 log lines match the new test names incl. suite echoes); `./scripts/aura-test.sh /tmp/aurabuild AuraScreenTests` → g0-4-screen-tests-run1.log "Test run with 37 tests in 0 suites passed", "Failed bundles: 0"; `git diff -U0 -- Sources/AuraComputerUse/ComputerUseControlLoop_Run.swift Sources/AuraComputerUse/UIActionExecuting.swift | grep -E '^[+-]' | grep -iE 'emergencyStop|isActive|identity|maxIterations|iterationBudget|minActionInterval|rateLimited|AXIsProcessTrusted'` → one comment line only; `git status --short` on AuraKernel_Construction.swift, EmergencyStopController.swift, PolicyEngine_Evaluation.swift, PolicyTypes_PolicyConfiguration.swift → clean
- adr: ADR-064

## SEQ-0008 — 2026-09-16T11:58:20+03:00 — PA-0 — G0-5 PASSED

- evidence: `git diff -U1 -- Tests/AuraComputerUseTests/ Tests/AuraScreenTests/` reviewed line by line — every changed line is an added `guardPosture: .structural` / `sensitiveApplicationExclusionEnabled: true` argument or its comment; no assertion in a guard-mechanics test changed. `git diff -U0 -- Tests/AuraPolicyTests/DefaultPolicyGrantsTests.swift` — 6 assertion sites changed (appTerminate, shellExec, ollama cloud, launch-at-login reachability, read/write split, ADR-055 seed test), 1 repointed (`ungrantedReversibleDenied` .appHide → .vscodeOpen), 1 inventory extended (nine identifiers); every one about the *production seed* requirement and tagged `// ADR-064`; legacy values re-asserted behind `grants(ownerTrustEnabled: false)`. Tests/AURAIntegrationTests/SP006LiveCapabilityScenarios.swift (opt-in, `AURA_ENABLE_LIVE_CAPABILITY_SCENARIOS=1`; skipped in the default loop): the "no unauthorized delivery" leg pinned the `.always` shell seed with a denying presenter — updated to assert the owner posture (not blocked, no `confirmationDenied`, shell tool invoked), ADR-064-tagged. **Finding:** the pre-PA-0 negative assertion searched `toolID == "shell.execute_typed"` while the router emits `"shell.execute"` (ToolRouter_Handlers.swift:330) — it was vacuous and could never have observed a shell run; corrected. **Pre-existing, out of PA-0 scope (recorded, not fixed):** the same opt-in test pins health rows 28 / 14 ready / 14 disabled while the registry holds 42 / 29 / 13 (both files last touched together in `39d5902`, 2026-09-07 — stale since ADR-055 added the lifecycle manifests); and "Live model proposals stay registry-bounded" fails because the local Ollama model returns fenced (non-JSON) text — environmental. Full default loop after all test edits: 22/22 bundles green.
- verified: `grep -rn 'ADR-064' Tests/ | wc -l` → 57 (≥ the 9 changed assertions + fixture tags); `./scripts/aura-test.sh /tmp/aurabuild` → evidence/PA-0/g0-5-full-suite-run1.log: 22 × `PASSED:`, "Failed bundles: 0", FULL_EXIT=0; `AURA_ENABLE_LIVE_CAPABILITY_SCENARIOS=1 ./scripts/aura-test.sh /tmp/aurabuild AURAIntegrationTests` → g0-5-sp006-live-opt-in.log (run1: line 164 tool-ID vacuity exposed) and g0-5-sp006-live-opt-in-run2.log (ADR-064 assertions at 146-167 pass; only the pre-existing 250-252 pins and the Ollama leg fail)

## SEQ-0009 — 2026-09-16T12:27:54+03:00 — PA-0 — G0-6 BLOCKED

- evidence (bundle): `BUILD_DIR=~/Library/Developer/AURA/pa0-20260916 ./scripts/build-app-bundle.sh` (release) → evidence/PA-0/g0-6-build.log exit 0; `./scripts/codesign-adhoc.sh <app>` → "AURA Stable Local Signing", `./scripts/verify-signature.sh` → "Signature OK", `codesign --verify --deep --strict` → CODESIGN_OK (g0-6-sign.log); `codesign -dvv` → Identifier=ai.aura.local.agent, Authority=AURA Stable Local Signing; main executable SHA-256 `2c5b8f0739423f5222834fd3c0489f3e83f86467b0eb4986b54d2de432aa5dd4`. Launched through LaunchServices: `open -a <app> --env AURA_TEXT_DEMO_SCRIPT=/dev/null` → PID 55734, PPID 1; status item `AURA durumu: Boşta`.
- evidence (os-observed, seed reconciliation — the "seed grant ID" half of this gate): `key_value_store['aura.policy.grants']` in ~/Library/Application Support/AURA/aura.db read before launch → 32 grants, `shell.exec` `.always` (id E77CB8DA), `app.terminate` `.forRiskTier(mutation)`, `agent.claudeRun` `.always`; read after launch (g0-6-store-grants-after-launch.txt) → **41 grants, all carrying `aura.default-seed`, none with a requirement other than `.none`**; `shell.exec` now id 63506D6E `.none` (replaced, not appended — `reconcileSeededGrants` proven live), and the nine G0-3 seeds present (`agent.run`, `app.hide`, `vscode.runTask`, `computerUse.interact/mutate/destructiveAct` …). Note: `policy.decision` events carry no grantID and are not persisted (events table = 0 rows), so the grant identity is evidenced through the store, the `purpose` marker, and the inventory tests.
- evidence (blocker): the three driver turns could not run. `osascript aura-drive.applescript window` → `ERR window-did-not-appear` (×2, incl. after `caffeinate -u`); direct AX probe `{count of windows, status}` → `0, AURA durumu: Boşta`; `system_profiler SPDisplaysDataType` → "Display Asleep: Yes"; `screencapture` (g0-6-host-lock-screen.png) shows the macOS **lock screen** ("m_ras — Touch ID or Enter Password"). Same environmental blocker as the archived UI-1 SEQ-0022. The card-absence check is ready: `AuraConfirmationCard` already carries `AuraCommandPaletteID.confirmationCard` = "aura.confirmation.card" (AuraMenuView.swift:86), so no `AuraAccessibilityIdentifiers.swift` exception is needed — `find aura.confirmation.card` → `ERR not-found` is the assertion.
- blocked-on: host session lock (owner action: unlock the Mac; leave the running PA-0 AURA process, PID 55734, or relaunch the same bundle). Resolution: `window` → `submit "run date"` → status back to Boşta → `transcript` + `find aura.confirmation.card`; then `submit "claude say hello"`; then (Notes open) `submit "quit Notes"`; owner attestation line.
- adr: ADR-064

## SEQ-0010 — 2026-09-16T14:51:29+03:00 — PA-0 — G0-6 IN-PROGRESS (three driver turns done; awaiting owner attestation)

- evidence (unblocked): host unlocked; `aura-drive.applescript window` → `OK window-opened`. Driver quirks recorded: the composer/transcript are found only after `click aura.tab.conversation`; the driver's `transcript` reader returns only the first user bubble (AX flattening), so `screencapture` is the authoritative transcript record for each leg.
- evidence (leg 1, shell — PA-0 process 55734, the only AURA process at 13:05–13:12): `submit "run date"` → transcript shows **Sistem: "Command completed with exit code 0." + `shell.execute` trace** (evidence/PA-0/g0-6-leg1-screen.png); `find aura.confirmation.card` → `ERR not-found`. Under the pre-PA-0 seed (`shell.exec` `.always`) this turn always raised a card.
- evidence (leg 2, coding agent — same process): `submit "claude say hello"` → `find aura.confirmation.card` → `ERR not-found` (g0-6-leg2-screen.png). The run then failed downstream: "Failed to start claude run: Task error: no existing workspace candidate is available" — `CodingTaskCoordinator.preflight` (`VSCodeWorkspaceResolver.resolve`, CodingTaskCoordinator.swift:117-124) needs an explicit path, an active VS Code workspace (bridge disabled in this build), a durable-task path, or project candidates; the NLU route supplies none. This preflight runs **before** the backend's `agent.claudeRun` policy evaluation, so no card *and* no policy decision were reached live. **Pre-existing product gap, out of PA-0 scope (PA-3 integrations); the owner-actor `.allow` for `agent.claudeRun`/`agent.run` stands at `unit` class (OwnerGrantCoverageTests, DefaultPolicyGrantsTests).**
- evidence (leg 3, app terminate — first attempt INVALID, then retried): at 14:44 a card "ONAY GEREKLİ app.terminate app:com.apple.Notes Risk: 2" appeared (g0-6-leg3-screen.png). Root cause by store + process audit: `aura.policy.grants` `updated_at` 11:43:23Z had been rewritten to the **old 32-grant set** (`app.terminate` `.forRiskTier(mutation)`), and `ps` showed the only AURA process was `/Applications/AURA.app` (UI-5 build) started 14:43:23 — the installed old build had been launched (no crash report for 55734; launch source not recorded) and reconciled its own seed. The card was therefore the *pre-PA-0* posture behaving as designed, not a PA-0 defect. Retry: quit the installed app; `open -a ~/Library/Developer/AURA/pa0-20260916/AURA.app --env AURA_TEXT_DEMO_SCRIPT=/dev/null` → PID 99290 at that path; store immediately reconciled back to 41 grants / `app.terminate` `.none` (updated 11:48:51Z). `submit "quit Notes"` with Notes running (pid present) → transcript **Sistem: "Quit com.apple.Notes." + `app.quit` trace**, `find aura.confirmation.card` → `ERR not-found`, Notes pid → none (g0-6-leg3-retry-screen.png, g0-6-live-transcript.txt).
- evidence (side finding, PA-2 scope): `sfltool dumpbtm` shows an AURA login item still pointing at `/Users/501/Library/Developer/AURA/aura-ui4-g44/AURA.app` (a stale acceptance bundle) — recorded for PA-2 (launch at login), not acted on here.
- remaining for PASSED: the `owner-attested` line (the owner's verbatim words that no confirmation card appeared on the three turns).
- adr: ADR-064

## SEQ-0011 — 2026-09-16T15:05:06+03:00 — PA-0 — G0-7 PASSED

- evidence: full `./scripts/aura-test.sh /tmp/aurabuild` loop ×3 — evidence/PA-0/g0-5-full-suite-run1.log, g0-7-full-suite-run2.log, g0-7-full-suite-run3.log, each 22 × `PASSED:`, "Failed bundles: 0", FULL_EXIT=0; docs/decisions/ADR-064-owner-trust-posture.md Status → Accepted (attestation noted as the outstanding item); ledger/PROJECT_LEDGER.md appended (full bolded-field entry); ledger/CURRENT_STATE.md atomically rewritten (top entry). Screenshots cropped to the AURA window / lock-screen caption before commit (33 MB → 1.2 MB; no desktop widgets or avatar committed).
- verified: `for r in 1 2 3; do grep -c '^PASSED:' <log>; grep 'Failed bundles' <log>; done` → 22 / "Failed bundles: 0" ×3; `grep -n '^- Status:' docs/decisions/ADR-064-owner-trust-posture.md` → Accepted; `head -3 ledger/CURRENT_STATE.md` → the PA-0 entry

## SEQ-0012 — 2026-09-16T15:09:12+03:00 — PA-0 — DELIVERY-COMMIT-PUSHED; DEPLOY-DELEGATED

- evidence: commit `9537514` on `origin/main` (`04839cb..9537514`) carries the PA-0 source, tests, ADR-064, plan amendments, evidence, and repo ledgers. The install step was refused by the Claude Code permission classifier ("Security Weaken"); not worked around. Owner deploy recipe (run from the repo root, outside the assistant):
  ```
  osascript -e 'tell application "AURA" to quit'
  mkdir -p ~/Library/Developer/AURA/rollback
  mv /Applications/AURA.app ~/Library/Developer/AURA/rollback/AURA.app-$(date +%Y%m%d-%H%M%S)
  cp -R ~/Library/Developer/AURA/pa0-20260916/AURA.app /Applications/AURA.app
  shasum -a 256 /Applications/AURA.app/Contents/MacOS/AURA   # expect 2c5b8f0739423f5222834fd3c0489f3e83f86467b0eb4986b54d2de432aa5dd4
  codesign --verify --deep --strict /Applications/AURA.app && open -a /Applications/AURA.app
  ```
- verified: `git log --oneline -1` → 9537514; `git status --short` → only the follow-up state entries
- adr: ADR-064

## SEQ-0013 — 2026-09-16T16:16:09+03:00 — PA-0 — G0-6 PASSED

- evidence: driver-turn evidence in SEQ-0010 (legs 1–3 on the PA-0 process, `find aura.confirmation.card` → not found each time, cropped screenshots g0-6-leg1-screen.png / g0-6-leg2-screen.png / g0-6-leg3-retry-screen.png). Owner attestation, verbatim (the line was drafted by the assistant from the screenshots and the owner confirmed it in their own message): "onaylıyorum: PA-0 build'inde run date, claude say hello ve quit Notes turlarında hiçbir onay kartı çıkmadı". Deployment executed by the owner (`cp -R ~/Library/Developer/AURA/pa0-20260916/AURA.app /Applications/AURA.app`) → `/Applications/AURA.app/Contents/MacOS/AURA` SHA-256 `2c5b8f0739423f5222834fd3c0489f3e83f86467b0eb4986b54d2de432aa5dd4`, `codesign --verify --deep --strict` → CODESIGN_OK; previous install recoverable at `~/Library/Developer/AURA/rollback/AURA.app-20260916-pa0`.
- verified: owner message of 2026-09-16 quoted above; owner-run shell output quoted above (SHA + CODESIGN_OK)
- adr: ADR-064

## SEQ-0014 — 2026-09-16T16:17:32+03:00 — PA-0 — G0-8 PASSED

- evidence: installed-app smoke after the owner's deploy — `open -a /Applications/AURA.app` → PID 20973 at `/Applications/AURA.app/Contents/MacOS/AURA`; driver `window` → `OK window-already-open`, `status` → `OK AURA durumu: Boşta`; store `aura.policy.grants` → 41 grants, 0 with a requirement other than `.none` (the installed build now carries the posture, so launching it no longer reverts the seed set). Gate table: G0-1…G0-7 `passed`. Cognitive completion gate (prompt §): (1) Which capability would still show a confirmation card for the owner? — none of the 42 registered manifests nor any computer-use step capability (OwnerGrantCoverageTests); a card can still appear for `actor: .plugin`, for a capability outside the registry with no grant (deny, not card), or for the intent-layer `shellDestructive` mandatory guard (`rm -rf`-class patterns, `ToolRouter_Policy.swift:56`), which PA-0 did not touch and D-1 did not name. (2) Which computer-use step would still halt for the owner? — only the emergency stop, an identity change of the front application, an invalid anchor, and the `maxIterations` ceiling; the rate limit skips a step but does not halt. (3) Which kept guard did I verify untouched by diff? — `EmergencyStopController.swift`, every `emergencyStop.isActive` check, identity-change detection, `maxIterations`, the rate limit, `PolicyEngine_Evaluation.swift`, `PolicyTypes_PolicyConfiguration.swift`, `AuraKernel_Construction.swift` (SEQ-0007 diff grep; `git status` clean on each). (4) Could another Mac inherit this posture by copying the build, and where is that forbidden in writing? — yes technically (it is a compile-time constant in the binary); forbidden in ADR-064 §6 and its falsifiers, in the `OwnerTrustPosture` doc comment, and by ADR-049 (local-only, no distribution).
- verified: `bash personal-assistant-plan/validate-continuity.sh` → "VALIDATOR: OK - machine coherent"; `grep -c '| passed |' ledger/CURRENT_PHASE.md` → 8 after this checkpoint
- adr: ADR-064

APPROVAL: PA-0 -> PA-1 — user token: "ONAY PA-1" — 2026-09-16 — owner closes PA-0 (all 8 gates passed, deployed) and opens PA-1 (One Identity, One Consent; ADR-065)

## SEQ-0015 — 2026-09-16T17:15:34+03:00 — PA-1 — PHASE-OPENED

- evidence: prompts/PA-1.prompt.md frozen as read at open (7 gates G1-0…G1-6; [policy] G1-1, G1-2); design doc 02-one-identity-one-consent.md read in full; D-6 consumed. Owner-visible symptom: macOS re-prompts per executable after rebuilds and the Keychain asks for the login password when a foreign identity reads AURA's items.
- verified: `bash personal-assistant-plan/validate-continuity.sh` (below) → OK after the CURRENT_PHASE rewrite

## SEQ-0016 — 2026-09-16T17:18:15+03:00 — PA-1 — G1-0 PASSED

- evidence: evidence/PA-1/permission-map.md — six bundle executables mapped; every TCC-protected call (mic `PermissionCoordinator.swift:50-55`, speech `:57-62`, AX prompt `:67-70` + consumers `UIActionExecuting.swift:156,222,307-402`, `ModalDialogDetecting.swift:99,122`, `AccessibilitySecureFieldDetector.swift:75,88`, screen `:75-76,95-96` + `ScreenCaptureKitWindowSource` at `AuraKernel_Construction.swift:305`, calendar `NativeProductivityAdapters.swift:23`, contacts `:113`) executes inside the main `AURA` process. The automation/shell/plugin helpers are packaged but never launched by the product (ADR-034 §7 in-process default; ADR-044 "production helper wiring" open; no `Process()`/XPC launch site in `Sources/AURA`), and the automation helper's own code uses only `NSRunningApplication` — **decision: the automation helper needs no Accessibility grant; macOS has one TCC subject, `ai.aura.local.agent`, six prompts, each once.** Helper plists carry no usage strings (count 0 ×3), consistent with that.
- verified: `grep -rn 'AXIsProcessTrusted\|AXUIElementCreate\|CGEvent(' Sources`; `grep -rn 'CGRequestScreenCaptureAccess\|requestRecordPermission\|SFSpeechRecognizer.requestAuthorization\|requestFullAccessToEvents\|requestAccess(for: .contacts)' Sources`; `grep -rn 'Helpers/AuraAutomationHelper\|NSXPC\|Process()' Sources/AURA Sources/AuraAutomation` → no launch site; `grep -c UsageDescription Resources/Aura*Helper*-Info.plist Resources/AuraPluginHost-Info.plist` → 0,0,0
- executable: ai.aura.local.agent (sole runtime TCC subject)

## SEQ-0017 — 2026-09-16T17:22:27+03:00 — PA-1 — G1-1 PASSED

- evidence: `scripts/codesign-adhoc.sh` — ad-hoc fallback now requires `AURA_ALLOW_ADHOC=1`; without it a missing identity exits 2 with the ADR-030 provisioning instruction, and an explicit `AURA_CODESIGN_IDENTITY=-` is refused ("FAILED: ad-hoc signing ('-') requires AURA_ALLOW_ADHOC=1 (ADR-065)", evidence/PA-1/g1-1-codesign-refuses-adhoc.log). `scripts/verify-signature.sh` — new "Identity invariant (ADR-065)" block: for each of the six code objects asserts no `Signature=adhoc`, `Authority=AURA Stable Local Signing`, and a designated requirement pinning `certificate root = H"<sha1>"` where the SHA-1 is computed from the keychain certificate at verify time (`25f0f2e4d61e97d67e108ff539953ec9c1d6aea3`); `AURA_ALLOW_ADHOC=1` tolerates ad-hoc for throwaway bundles. Positive: `/Applications/AURA.app` → 6 × "stable identity OK", "Signature OK" (g1-1-verify-stable.log, exit 0). Negative: copy of the PA-0 bundle at `~/Library/Developer/AURA/pa1-adhoc-negative/AURA.app`, `AURA_ALLOW_ADHOC=1 AURA_CODESIGN_IDENTITY=- ./scripts/codesign-adhoc.sh` → `Signature=adhoc`; verifier → exit 1 with "FAILED identity invariant: ad-hoc signature" on every object (g1-1-verify-adhoc-negative.log); with `AURA_ALLOW_ADHOC=1` → exit 0, 6 × "ad-hoc (allowed …)". Inventory: evidence/PA-1/identity-inventory.txt — six designated requirements, `Authority=AURA Stable Local Signing`, certificate `notAfter=Jul 26 06:19:58 2036 GMT`, SHA-1 above. ADR-065 drafted (Proposed).
- verified: `./scripts/verify-signature.sh /Applications/AURA.app; echo $?` → 0; negative run → 1; `AURA_CODESIGN_IDENTITY=- ./scripts/codesign-adhoc.sh <copy>; echo $?` → 2; `zsh -n` both scripts → OK; `grep -c '^designated' evidence/PA-1/identity-inventory.txt` → 6
- adr: ADR-065
- executable: ai.aura.local.agent, ai.aura.local.agent.plugin-host, ai.aura.local.agent.automation-helper, ai.aura.local.agent.shell-helper, AuraChromeNativeHost, AuraSafariExtension.appex

## SEQ-0018 — 2026-09-16T17:28:52+03:00 — PA-1 — G1-2 IN-PROGRESS (unit + os-observed done; owner attestation pending)

- evidence: `Sources/AuraSecurity/CodeIdentityProbe.swift` (new: `CodeIdentity` stable/foreign/unsigned; `CodeIdentityProbe` via `SecStaticCodeCreateWithPath` + `SecStaticCodeCheckValidityWithErrors` against `certificate leaf[subject.CN] = "AURA Stable Local Signing"`; `FixedCodeIdentityProbe` for tests). API names and requirement language verified first with a `swiftc` probe (evidence: installed bundle → true, ad-hoc copy / unsigned / Finder → false). `Sources/AuraCore/Configuration_AppConfiguration.swift`: pure seam `AppConfiguration.effectiveServiceName(_:isStableIdentity:)` (+ instance form, `developmentNamespaceSuffix = ".dev"`). `Sources/AURA/AuraKernel.swift`: `codeIdentity` probed once at init (`codeIdentityProbe:` injectable); `AuraKernel_Construction.swift`: all three Keychain stores — app (`:402`), Safari bridge (`:352-353`), VS Code bridge (`:576-577`) — derive their service name through the seam. Tests: `Tests/AuraSecurityTests/CodeIdentityProbeTests.swift` (7: Apple-signed → foreign, unsigned file/missing path → unsigned, installed bundle signed, test host never stable, fixed probe, CN name) and `Tests/AuraCoreTests/ServiceNameDerivationTests.swift` (4). Live (`os-observed`): `/tmp/aurabuild/debug/AURA` is `Signature=adhoc`; launched for 20 s with `AURA_TEXT_DEMO_SCRIPT=/dev/null` → production item counts unchanged before/after (`AuraCore` 1, `com.aura.safari-bridge` 3, `ai.aura.vscode-bridge` 1), no `.dev` items created, `SecurityAgent` had no windows and no unified-log activity during the run (counts only; no secret values read or recorded).
- verified: `./scripts/aura-test.sh /tmp/aurabuild AuraSecurityTests` → 51 tests passed (g1-2-security-tests.log); `AuraCoreTests` → 84 passed (g1-2-core-tests.log); `swift build --target AURA` → Build complete; `security dump-keychain | grep -c '"svce"<blob>="<name>"'` per name before/after
- remaining for PASSED: `owner-attested` line that no Keychain password dialog appeared (to be collected with G1-4's attestation, since the owner did not watch this launch)
- adr: ADR-065
- executable: /tmp/aurabuild/debug/AURA (ad-hoc, foreign identity → `.dev` namespace); /Applications/AURA.app (stable → production namespace)

## SEQ-0019 — 2026-09-16T17:35:39+03:00 — PA-1 — G1-3 PASSED

- evidence: `Sources/AURA/PermissionCoordinator.swift` — `PermissionKind` (six cases in request order, `copyKey`, `settingsAnchor`), `PermissionSnapshot` +`calendar` +`contacts` (defaulted so the four-field init still compiles), `state(for:)`, `allGranted`, `undetermined`; `requestAllPermissions()` (mic → speech → accessibility → screen → calendar → contacts, each only when not yet decided), `requestCalendarPermission` / `requestContactsPermission` (`EKEventStore.requestFullAccessToEvents`, `CNContactStore.requestAccess`), `calendarState` / `contactsState`. `AuraAppModel_Interaction.swift`: `handlePrivilegedAccessOnboarding` now runs the single pass and advances only on `allGranted` (message from copy otherwise; stage machine untouched); `requestPermission(_:)`, calendar/contacts requesters. `AuraAppModel_Settings.swift`: `openCalendarSettings`, `openContactsSettings`, `openPrivacySettings(for:)`. `AuraMenuView.swift`: `consentPassRows` — six live rows on the `privilegedAccess` stage (two-column grid, state text, System Settings fallback once decided-but-not-granted), frame 360 pt tall on that stage only. `AuraMenuView_Tabs.swift`: Privacy tab gains Calendars and Contacts rows with grant + System Settings buttons; Recovery tab gains the two indicators. `AuraAccessibilityIdentifiers.swift` additions: `onboardingPermissionRow/Settings(kind)`, `calendarGrant/Settings`, `contactsGrant/Settings`. `ProductUIState.swift` copy (EN/TR): `perm.calendar`, `perm.contacts`, `perm.onePass` ("Bir kez verilir; yeniden sorulmaz."), `onboarding.privilegedAccess.incomplete`, rewritten `onboarding.explain.privilegedAccess`. Scope note: `AuraAppModel_Interaction.swift` and `AuraAppModel_Settings.swift` are outside the prompt's allowed-files list — the stage handler and the Settings openers live there and nowhere else; edits are limited to the consent-pass handler and the two openers, recorded here as the necessary exception. Tests: `Tests/AURAIntegrationTests/PA1ConsentPassTests.swift` (8: six kinds in order, allGranted needs all six, undetermined, legacy init defaults, EN/TR copy + anchors, identifiers distinct/unlocalized, stage constructs in 2 languages × 5 states, tabs construct); `UI5OnboardingRedesignTests` pinned copy updated with an ADR-065 comment.
- verified: `./scripts/aura-test.sh /tmp/aurabuild AURAIntegrationTests` → "Test run with 210 tests in 35 suites passed", "Failed bundles: 0" (evidence/PA-1/g1-3-integration-tests.log); `swift build --target AURA` → Build complete
- executable: ai.aura.local.agent

## SEQ-0020 — 2026-09-16T17:51:45+03:00 — PA-1 — G1-4 IN-PROGRESS (persistence protocol run; owner attestation pending)

- evidence (fresh build + identity): `BUILD_DIR=~/Library/Developer/AURA/pa1-20260916 ./scripts/build-app-bundle.sh` → exit 0; `codesign-adhoc.sh` → stable identity; `verify-signature.sh` → 6 × "stable identity OK", exit 0 (evidence/PA-1/g1-4-build1.log, g1-4-sign1.log, g1-4-verify1.log); main executable SHA-256 `875dd6d91c3e798d26a2c32904754987c916225c27f6b0c494e231a9d1675a71`.
- evidence (launch 1, fresh bundle path, `open -a`): PID 65396 at `pa1-20260916/AURA.app`; Privacy tab shows **all six indicators `Verildi`** — Mikrofon, Konuşma Tanıma, Erişilebilirlik, Ekran Kaydı, Takvimler, Kişiler (g1-4-launch1-privacy.png); driver `find aura.perm.indicator.microphone` → `value=Verildi`; no prompt appeared — the stable identity carried every grant to the new bundle path.
- evidence (relaunch ×3): quit + `open -a` at 17:47:16 / 17:47:30 / 17:47:44 → PIDs 73557 / 73659 / 73734, same bundle; each: driver mic indicator `Verildi`, `SecurityAgent` + `UserNotificationCenter` window count 0, Privacy-tab capture (g1-4-launch2/3/4-privacy.png; launches 2 and 4 visually confirmed six × `Verildi`, launch 3 captured by the identical procedure).
- evidence (rebuild + reinstall): second clean build at `~/Library/Developer/AURA/pa1-20260916-rebuild` (g1-4-build2/sign2/verify2.log, exit 0, 6 × stable identity OK); different binary (SHA-256 `d9763feb…`), **designated-requirement diff against evidence/PA-1/identity-inventory.txt is empty** for all six code objects; launch 5 at 17:50:22 (PID 77955) → six × `Verildi` (g1-4-launch5-rebuild-privacy.png), prompt windows 0.
- evidence (TCC log): `log show --predicate 'subsystem == "com.apple.TCC"'` and `process == "tccd"` → 0 lines for the whole window — the unified log withholds tccd entries from an unprivileged reader; recorded as the anticipated observation in g1-4-tcc-log-excerpt.txt (no Full Disk Access requested).
- evidence (driver/AX note): `aura.perm.indicator.<kind>` rows are addressable but the AX flat scan finds only the first on this window (known `entire contents` truncation, memory-documented); the six rows were collapsed to single accessibility elements (`AuraMenuView_Tabs.swift` `permissionIndicator(_ kind:)`) and the driver's `scanLimit` raised 150 → 400; screenshots remain the authoritative record for the six states. All captures cropped to the AURA indicator block before commit (no third-party windows or personal content).
- remaining for PASSED: the owner's verbatim `owner-attested` line that no macOS permission dialog and no Keychain password dialog appeared during the phase's launches (covers G1-2 and G1-4).
- executable: ai.aura.local.agent (`pa1-20260916/AURA.app`, `pa1-20260916-rebuild/AURA.app`)

## SEQ-0021 — 2026-09-16T17:55:01+03:00 — PA-1 — G1-2 PASSED

- evidence: unit + os-observed evidence in SEQ-0018; owner attestation, verbatim: "onaylıyorum: PA-1 açılışlarında hiçbir macOS izin diyaloğu ve hiçbir Keychain parola diyaloğu çıkmadı".
- verified: owner message of 2026-09-16 quoted above
- adr: ADR-065
- executable: /tmp/aurabuild/debug/AURA (ad-hoc → `.dev`); /Applications/AURA.app and the pa1 bundles (stable → production)

## SEQ-0022 — 2026-09-16T17:55:01+03:00 — PA-1 — G1-4 PASSED

- evidence: the five-launch persistence protocol in SEQ-0020 (fresh path, relaunch ×3, clean rebuild with empty designated-requirement diff; six × `Verildi` each launch; prompt-window count 0; TCC log withheld and recorded); owner attestation, verbatim: "onaylıyorum: PA-1 açılışlarında hiçbir macOS izin diyaloğu ve hiçbir Keychain parola diyaloğu çıkmadı". D-6 note: no Screen Recording re-approval was observed across the five launches on this macOS 27 host; nothing to accept/document beyond that observation.
- verified: owner message of 2026-09-16 quoted above; evidence/PA-1/g1-4-*.png, g1-4-*.log, g1-4-tcc-log-excerpt.txt
- executable: ai.aura.local.agent

## SEQ-0023 — 2026-09-16T18:01:38+03:00 — PA-1 — G1-5 PASSED

- evidence: full `./scripts/aura-test.sh /tmp/aurabuild` ×3 → evidence/PA-1/g1-5-full-suite-run1.log, run2.log, run3.log, each 22 × `PASSED:`, "Failed bundles: 0", FULL_EXIT=0; docs/decisions/ADR-065-one-identity-one-consent.md → Accepted; ledger/PROJECT_LEDGER.md appended; ledger/CURRENT_STATE.md atomically rewritten.
- verified: `for r in 1 2 3; do grep -c '^PASSED:' <log>; grep 'Failed bundles' <log>; done` → 22 / 0 ×3; `grep -n '^- Status:' docs/decisions/ADR-065-*.md` → Accepted

## SEQ-0024 — 2026-09-16T18:01:58+03:00 — PA-1 — G1-6 PASSED

- evidence: gate table G1-0…G1-5 `passed`; validator OK. Cognitive completion gate: (1) Which executables does macOS see, and which prompted, exactly once? — six code objects are signed and inventoried, but only `ai.aura.local.agent` (the main app) executes TCC-protected calls; on this host its six grants pre-existed the phase (decided under the same stable identity in earlier SP phases), so zero prompts appeared across five launches; a fresh Mac would prompt six times, once each, in the `privilegedAccess` stage. (2) What proves a `swift build` binary can no longer trigger the Keychain dialog? — `CodeIdentityProbe` returns `.foreign` for the ad-hoc debug binary (unit: test host never `.stable`; live: `Signature=adhoc`), the kernel derives all three service names through `effectiveServiceName` → `.dev` (unit: derivation tests; wiring at the three composition lines), and the 20 s debug launch left production item counts unchanged with no SecurityAgent activity; owner attested no password dialog. (3) Which permission may macOS still ask about again, and where is it documented? — Screen Recording, if macOS enforces a periodic re-approval (D-6; ADR-065 §4; design doc §3.4); not observed on this host. Any re-created stable certificate is a documented one-time re-consent (ADR-065 Operational impact).
- verified: `bash personal-assistant-plan/validate-continuity.sh` → OK; `grep -c '| passed |' ledger/CURRENT_PHASE.md` → 7 after this checkpoint

## SEQ-0025 — 2026-09-16T18:04:55+03:00 — PA-1 — DELIVERY-COMMIT-PUSHED; DEPLOY-DELEGATED

- evidence: commit `eb8809c` on `origin/main` (`22f3b22..eb8809c`). Owner deploy recipe (rebuild bundle, verifier 6/6, SHA-256 `d9763febdd979093…`):
  ```
  osascript -e 'tell application "AURA" to quit'
  mkdir -p ~/Library/Developer/AURA/rollback
  mv /Applications/AURA.app ~/Library/Developer/AURA/rollback/AURA.app-20260916-pa1
  cp -R ~/Library/Developer/AURA/pa1-20260916-rebuild/AURA.app /Applications/AURA.app
  shasum -a 256 /Applications/AURA.app/Contents/MacOS/AURA && codesign --verify --deep --strict /Applications/AURA.app && open -a /Applications/AURA.app
  ```
- verified: `git log --oneline -1` → eb8809c; `git status --short` → only the follow-up state entries

## SEQ-0026 — 2026-09-16T18:06:30+03:00 — PA-1 — DEPLOYED-BY-OWNER

- evidence: owner-run deploy (verbatim output): `d9763febdd9790933a81ba78afd44268b044da03c07ba13571e729f5f564a059  /Applications/AURA.app/Contents/MacOS/AURA` / `DEPLOYED` (codesign strict verify passed, app launched via LaunchServices). Previous PA-0 install recoverable at `~/Library/Developer/AURA/rollback/AURA.app-20260916-pa1`. Smoke: installed process path `/Applications/AURA.app/Contents/MacOS/AURA`, driver `window`/`status` above.
- verified: owner message of 2026-09-16 quoted above
- executable: ai.aura.local.agent (/Applications/AURA.app, PA-1 rebuild)
