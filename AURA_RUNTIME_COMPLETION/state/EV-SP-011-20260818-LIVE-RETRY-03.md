# EV-SP-011-20260818-LIVE-RETRY-03

- **Timestamp:** 2026-08-18T12:12:05Z (15:12:05 Europe/Istanbul)
- **Prompt / gap:** SP-011 / OPEN-06
- **Branch / commit:** `main` / `33688e2a54f1e5d53574d0ddea22d5256eec29c7` (`origin/main` matched; worktree intentionally dirty)
- **Evidence class:** live hardware / partial runtime observation
- **Environment:** macOS target with `xcode-select` at Xcode 27.0.0 beta 5, Swift 6.4, arm64; `AURA_SP011_LIVE_ACCEPTANCE` and `AURA_SP011_TEST_EMAIL` were not set in the shell environment.

## Procedure and result

1. Built the production bundle with `BUILD_DIR=/tmp/aura-sp011-live ./scripts/build-app-bundle.sh` — exit 0.
2. Signed `/tmp/aura-sp011-live/AURA.app` with `./scripts/codesign-adhoc.sh /tmp/aura-sp011-live/AURA.app` — exit 0, local signing identity accepted.
3. Ran `./scripts/verify-signature.sh /tmp/aura-sp011-live/AURA.app` — exit 0; helper sandbox attestations and strict hardened-runtime verification passed.
4. Launched `/tmp/aura-sp011-live/AURA.app` with `/usr/bin/open` — exit 0; the exact app executable was observed alive as PID 89390.
5. Observed privacy-redacted `ai.aura.local:wake` log events from PID 89390. No event payload, account data, audio, screenshot, token, or message body was recorded.
6. Sent `TERM` to the exact observed temporary-bundle PID and verified the process was absent — clean stop.

The signed main executable hash was:

`ad9bdd24d7389568da943a7993b7a7a0463c54e83fe4db193176d55231b795ec`  `/tmp/aura-sp011-live/AURA.app/Contents/MacOS/AURA`

## Deterministic verification in the same attempt

- Final `./scripts/aura-test.sh /tmp/aura-sp011-final` — **21/21 bundles, 1010/1010 tests, 0 failed bundles**, including `AuraProductivityTests` 48/48.
- `python3 scripts/validate_second_pass_program.py` — exit 0.
- `python3 scripts/validate_runtime_completion.py --ci` — exit 0.
- `python3 scripts/validate_repo_hygiene_program.py` — exit 0.
- `python3 scripts/validate_repo_hygiene_supply_chain.py` — exit 0.
- `python3 -m unittest discover -s scripts/tests` — 38/38 passed.
- `git diff --check` — exit 0.
- `xcrun swift-format lint --recursive --strict --configuration .swift-format Sources Tests` — exit 1 with 66 diagnostics across 22 existing dirty source/test files; no formatter mutation was performed. This is recorded as an unresolved repository-formatting limitation outside the live acceptance gate.

## Scope and limitations

This proves only that the current production bundle builds, signs, launches,
runs, emits privacy-redacted runtime signals, and quits on this machine. It
does **not** prove the SP-011 completion gate: no Gmail OAuth client ID or
access token was supplied, no real provider account was connected, no
`connectMailAccount`/live Gmail read/thread summary/revoke flow was exercised,
no Safari extension was packaged/installed or native-messaging round trip
performed, and no TCC/Contacts/Calendar prompt was clicked. Mutation/send was
not attempted. Xcode is present, but the repository still contains only the
Safari extension source directory; no live packaged/install/trust-path
evidence was available in this attempt. SP-011 remains **blocked** and SP-012
is not safe to start.
