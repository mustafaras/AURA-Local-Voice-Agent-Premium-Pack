# EV-SP-006-20260816-CLOSEOUT-03

**Session:** `AURA-SP-006-LIVE-CAPABILITY-20260816` (resumed — the SP-006 run was interrupted before its mandatory closeout)
**Timestamp:** 2026-08-16T16:05:00Z
**Branch / commit:** `main`, `HEAD == origin/main == 94e9e36a149bfd1913d67ebf76e7e29ec9e9e8a5`, working tree `dirty_expected`
**Environment:** macOS 27 / Apple Silicon arm64 / Swift 6.4 / CommandLineTools; Python 3.14.6
**Evidence class:** Process/closeout — verifies the repository is left resumable after SP-006, and records what the interrupted session had not finished.

## Why this record exists

The SP-006 live run (`EV-SP-006-20260816-7SCENARIO-02`) completed its seven scenarios and wrote
its source changes, tests, evidence artifacts, gap update, and most projections — then the
session ended before `15_SESSION_CLOSEOUT` ran. The prompt requires that closeout "after this
attempt, even if blocked". This record is that closeout, run against the surviving working tree.

## What the interrupted session had left undone

Verified by re-running the gates rather than by reading its summary:

1. **`validate_second_pass_program.py` was failing, exit 1** — `ACTIVE_CONTEXT.md must record the
   synchronized SP-007/completed overlay`. The session advanced `SECOND_PASS_STATE.json` to
   `active_prompt: SP-007` / `active_state: completed` but never wrote the matching
   `ACTIVE_CONTEXT.md` overlay, so the validator's derived expectation
   (`` `SP-007` / `completed` ``) was unsatisfied. Both program ledgers already asserted "all four
   governance validators exit 0", which was true when written and false by the end of the
   session. **Corrected**: the SP-006 closure overlay is now the current overlay and the SP-005
   overlay is demoted to superseded; the validator now exits 0.
2. **`RISK_REGISTER.md` was never updated**, though SP-006's own "Required records" section names
   it explicitly. **Corrected** — see the risk dispositions below.
3. **`capability-matrix.json` carried only a bumped header.** Its `intent.capability_registry`
   row still read `implementation_state: partial`, `user_path_state: developer_only`,
   `verification_state: unit_only`, with an `open_gaps` string describing the pre-SP-004 world
   ("4 of 14 registered capabilities … have a reviewed manifest but no adapter yet … the
   required 7-scenario live completion demonstration has not been performed"). Every clause of
   that was false after SP-004/SP-005/SP-006. **Corrected** to the highest *proven* class:
   `user_path_state: ui_reachable`, `verification_state: live_verified`, four truthful
   `open_gaps` entries, and the three SP evidence IDs. `implementation_state` deliberately stays
   `partial` — see item 4.
4. **The evidence file understated one limitation.** `EV-SP-006-…-7SCENARIO-02` did not disclose
   that `CapabilityPlanner` is constructed only in tests. Confirmed here:
   `grep -rn "CapabilityPlanner(" Sources/` returns no match, while `CapabilityRegistry` *is*
   used from `ToolRouter_ToolRouter.swift`, `IntentEngine_IntentEngine.swift`, and
   `AuraKernel_Construction.swift`. Scenario 4 therefore proves the planner's typed validation
   and real registry→policy→adapter step execution, but not that a user's sentence reaches a
   planner. **Corrected**: the limitation is now stated in the evidence file and in the matrix
   `open_gaps`. This does not change the SP-006 verdict — the completion gate is "the seven
   direct/live scenarios pass with typed evidence and no registry bypass", and planner
   production-wiring is a pre-existing R3 residual that SP-006 does not own.
5. **Mandatory closeout artifacts were absent**: no closeout evidence record, no closeout ledger
   entries, and `NEXT_SESSION_STARTER.md` still described SP-006 as "next, pending and
   unopened". All three are corrected in this closeout.
6. **An empty untracked `nohup.out`** was left at the repository root. Removed; it is a run
   artifact, not evidence.

## Verified before writing the handoff (closeout step 1)

- **Branch / HEAD / remote:** `main` at `94e9e36a149bfd1913d67ebf76e7e29ec9e9e8a5`, equal to
  `origin/main`; relation clean.
- **Working tree:** `dirty_expected` — SP-006 source/test files, evidence artifacts, and control
  plane projections. No unrelated user-owned change was present and none was overwritten.
- **Active prompt/state:** `SP-007` / `completed` per the program convention (next-eligible
  `SP-007`, just-closed `SP-006`); `completed_prompts` = `SP-000`…`SP-006`.
- **Commands actually run in this closeout:**
  - `./scripts/aura-test.sh /tmp/aura-sp006-verify-20260816` — **21/21 bundles, 880/880 tests,
    0 failed**, exit 0. Bundle count and test total were recomputed from the log
    (`grep -c "^PASSED:"`, summed `Test run with N tests`), not read from the prior summary.
    Log SHA-256 `1eb02473728b19c9130d97f4fdba6eb595c82bcda13ffc111971654eeb130c8c`. This
    independently reproduces the interrupted session's 880/880 claim.
  - `python3 scripts/validate_second_pass_program.py` — exit 1 before the fix, exit 0 after.
  - `python3 scripts/validate_runtime_completion.py --ci` — exit 0.
  - `python3 scripts/validate_repo_hygiene_program.py --ci` — exit 0.
  - `python3 scripts/validate_repo_hygiene_supply_chain.py --ci` — exit 0 (6 allowlisted fixture
    findings, 0 unallowlisted; 0 external Swift dependencies; 3 SHA-pinned actions).
  - `python3 -m unittest discover -s scripts/tests -p 'test_*.py'` — **38/38**, exit 0.
- **One adjustment the validators forced:** appending this closeout's line to
  `session-handoff.json.completed[]` pushed it to 31 entries against the schema's `maxItems: 30`,
  which failed `validate_runtime_completion.py` and one governance test. The array is a bounded
  rolling window by design (its own trailing NOTE entry says so), so the oldest entry — the R1-era
  "Bound confirmation execution to exact structured plan fingerprints…" line — was rolled off.
  Full history for it remains in `PROGRAM_LEDGER.md`. No content was rewritten.
- **A residual first-pass pointer inconsistency was found and left alone, deliberately:**
  `current-state.json.active_prompt.id` is `R2`/`pending` while `ACTIVE_CONTEXT.md` describes
  first-pass `FINAL` as active. That predates SP-006 and belongs to the first-pass track, so it
  was not silently "fixed" here. What *was* corrected in that block is narrower and factual: its
  `step` cited the **retracted** `EV-SP-003-20260815-R2-DIALOGUE-TESTS-15`, and its `next_action`
  was SP-002-era and named a stale HEAD (`813a504…`). Both now state the truth.
- **Live procedures run in this closeout:** none. No app launch, TCC interaction, model
  inference, or filesystem/URL side effect occurred; the live evidence is the interrupted
  session's and is not re-claimed here.
- **Evidence IDs:** consumed `EV-SP-006-20260816-7SCENARIO-02`; created
  `EV-SP-006-20260816-CLOSEOUT-03`.
- **Blockers / open decisions:** none.
- **Authority:** edit/test/ledger only in this closeout. Delivery (commit/push/merge) is
  recorded as granted for SP-006 in `NEXT_SESSION_STARTER.md` but was **not** exercised here —
  it is left for an explicit in-turn go-ahead, per the standing rule that a go-ahead covers only
  the work completed when it was given.

## Diff review (closeout step 2)

- Scope is exactly SP-006 plus its required record-keeping. Four production files changed:
  `DefaultPolicyGrants.swift` (new), `AuraKernel_Grants.swift` (seeds from it),
  `ToolRouter_Handlers.swift` (folder-slot dispatch), `AuraAppModel_Runtime.swift` (demo-driver
  budget 45→120 s).
- The `AuraAppModel_Runtime.swift` change was checked for production reach: it is inside
  `runTextDemoIfRequested`, which returns immediately unless `AURA_TEXT_DEMO_SCRIPT` names a
  readable file. It cannot affect a normal user session.
- No secrets, tokens, raw audio, or private targets in any diff or record. The two retained PNGs
  are UI captures of the assistant's own panel.
- No TODO/placeholder represented as complete; no acceptance criterion or safety check weakened.
  The new grants match each manifest's declared `confirmationRule` rather than relaxing it, and
  are pinned by 8 tests against an unmodified `PolicyConfiguration()`.
- Documentation now matches implementation — items 3 and 4 above were exactly the places where
  it did not.

## Risk dispositions recorded (closeout step 4)

- `RISK-SP-003-MODEL-LATENCY` — **bound widened** to 28.5–49.0 s (was 19.8–36.1 s). Still inside
  the 90 s think budget and 120 s request timeout, so honest degradation holds, but any budget
  derived from the old figure is too tight.
- `RISK-SP-006-DEFAULT-GRANT-BREADTH` — **new, bounded.** The three new grants use
  `patterns: [.any]`, so policy-layer target narrowing for filesystem/URL capabilities is absent
  and all refusal rests on `OpenTargetValidator`. Closure criterion: pattern-scoped grants, or an
  explicit accepted-risk decision under R10.
- Unchanged and forwarded: `RISK-SP-004-TOCTOU-RACE`, `RISK-SP-004-HANDLER-COMPROMISE`,
  `RISK-INJECTION-COVERAGE-NON-DIALOGUE`, `RISK-SP-003-LIVE-VOICE-RESIDUAL`,
  `RISK-STT-MIC-NOT-CAPTURING`.

No `DECISION_REGISTER.md` row was added: SP-006 introduced no new architectural decision. The
default-grant change is a posture correction that makes policy match the already-reviewed
manifests, and it is tracked as a risk rather than a decision, matching how SP-004 and SP-005
were recorded.

## Closeout verdict

`SP-006` is **completed**; its completion gate — the seven direct/live scenarios pass with typed
evidence and no registry bypass — was met by the live run and is not weakened by anything found
in this audit. The two corrections above make the records match what was actually proven.

The repository is resumable from files alone: `SECOND_PASS_STATE.json`, `session-handoff.json`,
`ACTIVE_CONTEXT.md`, `NEXT_SESSION_STARTER.md`, `SECOND_PASS_OPEN_GAPS.md`,
`SECOND_PASS_LEDGER.md`, `PROGRAM_LEDGER.md`, `PROJECT_LEDGER.md`, `EVIDENCE_INDEX.md`,
`RISK_REGISTER.md`, `capability-matrix.json`, and `current-state.json` now agree.

**Exact next action:** commit/push/merge the SP-006 working tree under an explicit in-turn
delivery go-ahead, then open `SP-007` only under its own explicit authority and read order.
