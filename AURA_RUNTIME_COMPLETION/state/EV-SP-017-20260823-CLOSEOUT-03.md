# EV-SP-017-20260823-CLOSEOUT-03

- **Timestamp:** 2026-08-23T14:28:05Z
- **Session / actor:** `AURA-SP-017-SYSTEM-TTS-CLOSEOUT-20260823` / Codex; computer-use skill used for the read-only AX observation.
- **Active prompt / scope:** SP-017 / OPEN-08 / R7 only. SP-018 is the first uncompleted prompt and remains pending/unopened.
- **Branch / repository:** `main`; `HEAD` `f6518e1333015b31c4783f0a4e8f033555f6e1f1`; `origin/main` is the same SHA; working tree is dirty as expected with uncommitted user/agent edits. No commit, push, merge, release, deploy, signing, install, model download, provider, telemetry, beta, or TCC action was performed.
- **Objective:** close TTS/resource governance or define a truthful system-TTS-only release.
- **Observed symptom / root cause:** neural adapters were the release default while live neural resource/quality/thermal/echo gates were unproven; a CPU Chatterbox helper sample reached approximately 3991 MiB on the 16 GiB host. The product-policy root cause was the adapter-chain default, not a proven failure of system TTS.
- **Direct resolution:** `TTSAdapterChain()` now defaults to `system`; `releaseTTSDefaultIsSystemOnly` locks the default; ADR-042 is accepted for PTT + system TTS with neural/wake/physical acoustic capabilities explicitly excluded; OPEN-08, risk, decision, first-pass, second-pass, and handoff projections were synchronized.
- **Commands / procedures and result:**
  - `AURA_ENABLE_SYSTEM_TTS_LIVE_TESTS=1 swift test --filter 'SystemTTSLatencyTests|SystemTTSEngineTests|releaseTTSDefaultIsSystemOnly' --build-path /tmp/aura-sp017-live-final` — **14/14 passed**, first chunk 0.733 s, full utterance 1.400 s.
  - `./scripts/aura-test.sh /tmp/aura-sp017-closeout-final` — **21/21 bundles passed; Failed bundles: 0**. Relevant final counts include AuraCoreTests 32/32, AuraAudioTests 39/39, AuraAgentTests 237/237, AuraSTTTests 19/19, and AURAIntegrationTests 82/82.
  - `swift test --filter 'VoiceResourceGovernorTests|OllamaAdapterTests|releaseTTSDefaultIsSystemOnly' --build-path /tmp/aura-sp017-focused-final` — governor/reasoning/default tests passed (8 AuraCore tests, 18 Ollama tests); isolated `AuraAgentTests` rerun passed 237/237 after an earlier aggregate helper-exit flake.
  - `PYTHONPATH=Runtime/chatterbox <AURA chatterbox venv>/python -m unittest discover -s Runtime/chatterbox/tests -v` — **4/4 passed**. `git diff --check` — pass. `python3 scripts/validate_second_pass_program.py` — `SECOND-PASS VALIDATION PASSED`.
- **Evidence classes:** direct live system-TTS (`EV-SP-017-20260823-LIVE-SYSTEM-TTS-01`); direct host/resource plus computer-use AX (`EV-SP-017-20260823-RESOURCE-SCOPE-02`); deterministic governor/Ollama (`EV-SP-017-20260823-GOVERNOR-IDLE-UNLOAD-01`); full regression and control-plane closeout (this record).
- **Acceptance verdict:** SP-017 **completed** through the prompt's explicit exclusion branch. PTT + system TTS is truthful and release-qualified for this bounded scope. Neural reference voice, MPS/CPU neural quality/soak, wake word/passive listening, human listening, energy/thermal qualification, and physical speaker-to-microphone echo are not passed and remain outside this prompt.
- **Falsifier:** a release build resolving the default chain to a neural adapter, a future neural/wake readiness claim without new direct evidence, or a live system-TTS regression below the recorded 14/14 result would reopen this decision.
- **Residual risks:** `RISK-MODEL-MEMORY-PRESSURE` and `RISK-NEURAL-TTS-LATENCY` remain future neural/co-residency qualification risks; `RISK-VOICE-RECOVERY-LIVE` remains physical route/sleep/echo verification. The overall R7 track and program remain in progress.
- **Artifact path / hash:** `/tmp/aura-sp017-closeout-final` (ephemeral build/test output; no raw audio, screenshot, secret, token, account data, or unredacted model output retained); durable records are this file and the linked ledgers/state files. No raw output hash is claimed.
- **Exact next safe action:** SP-018 is pending/unopened. A new session must read `SECOND_PASS_READ_FIRST.md`, `SECOND_PASS_CONTROL_CONTRACT.md`, `SECOND_PASS_PROMPT_CONTRACT.md`, SP-018, and current ledgers before acting; no SP-018 work is part of this closeout.
