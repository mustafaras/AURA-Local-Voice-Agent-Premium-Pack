# EV-SP-017-20260823-FULL-SUITE-01 — SP-017 full-suite regression evidence

## Record

- **Evidence ID:** `EV-SP-017-20260823-FULL-SUITE-01`
- **Timestamp:** 2026-08-23
- **Branch / commit:** `main` working tree (SP-017 edits uncommitted; commit/push not granted).
- **Prompt / gap:** `SP-017` / `OPEN-08` (R7).
- **Evidence class:** Full-suite deterministic system/contract run.

## Command / procedure

```
./scripts/aura-test.sh
```

Run with a clean `/tmp/aurabuild` build path. Exit code `0`.

## Result

**21/21 bundles, Failed bundles: 0.** Log: `/tmp/aura-sp017-full.log`.

PASSED bundles (21): AuraCoreTests, AuraStoreTests, AURAIntegrationTests,
AuraAudioTests, AuraAutomationTests, AuraAgentTests, AuraSTTTests,
AuraPolicyTests, AuraShellTests, AuraComputerUseTests, AuraSecurityTests,
AuraPluginsTests, AuraIntentTests, AuraConfigTests, AuraVSCodeTests,
AuraTasksTests, AuraMemoryTests, AuraContextTests, AuraScreenTests,
AuraAdversarialTests, AuraProductivityTests.

The governor-idle-unload and Ollama reasoning-admission changes introduced in
this prompt (see `EV-SP-017-20260823-GOVERNOR-IDLE-UNLOAD-01`) do not regress
any bundle. `AuraAgentTests` passed (noted as "helper timeout after completed
suite" — the runner's own helper-exit handling, not a test failure).

## Falsification test

Any future change that breaks the governor idle-unload, the Ollama reasoning
admission, or any other suite fails a focused or full run.

## Limitations

- Aggregate count only; individual test counts are in per-bundle log sections.
- This is not live hardware/soak evidence; it is a deterministic regression run.
