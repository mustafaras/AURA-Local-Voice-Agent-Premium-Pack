# EV-SP-030-20260830-OPENING-01

**Evidence ID:** EV-SP-030-20260830-OPENING-01
**Track:** SP-030 / R12 / OPEN-13
**Type:** Process/authority — SP-030 opening under owner continuation-path approval
**Commit:** `37805cb0e61cd4c46d7a3653c2ad8da212295a7a` (`main`; `HEAD == origin/main == 37805cb0`; working tree dirty with uncommitted SP-028/SP-029 source + control-plane projections)
**Environment:** macOS 27.0 arm64, Swift 6.4, Xcode 27.0 beta 5, Python 3.14.6, Git 2.54.0
**Session:** AURA-SP-029-BETA-CONTRACT-20260829
**Authority source:** The release owner explicitly stated "devam yolunun yetkilerini de veriyor ve onaylıyorum" — approving the continuation-path authority: R11 locally-closable gates, ADR-046 local-only formalization, SP-030 opening, and `telemetry_or_beta: true`.

## What was approved

The release owner approves the SP-030 (beta SLOs, scenarios, incidents, sign-offs) continuation path:

- **R11 locally-closable gates:** live launch-at-login, sleep/wake/crash recovery, safe mode, support-bundle export, migration on a populated profile — to be exercised in a user-present session.
- **ADR-046 local-only formalization:** advance ADR-046 to `Accepted` under the explicit local-only scope (local updater/rollback/recovery/safe-mode/reset contract is implemented + adversarially tested; real external signed update/transport remains out of scope per SP-027).
- **SP-030 opening:** SP-030 transitions from `pending` to `in_progress`.
- **`telemetry_or_beta`:** flipped to `true`, enabling SP-030's live beta SLO/scenario measurement.

## Explicitly NOT approved / not fabricated

- Developer ID signing, notarization, stapling, external clean-machine, signed update transport: external-Apple-prerequisite, out-of-scope by SP-027, and not fabricatable.
- `release_or_deploy`, `sign_or_notarize`, `mutate_permissions` (TCC), `install_dependencies`, `download_models`, `provider_accounts`: remain `false`.
- `beta-readiness.json` remains `blocked`; the artifact stays `development_unverified`.
- No participant consent is collected, no telemetry is transmitted, no SLO is measured, and no sign-off is obtained by this opening itself.

## State transition

- `SECOND_PASS_STATE.json`: `active_prompt=SP-030`, `active_state=in_progress`, `telemetry_or_beta=true`, `next_action` updated.
- `current-state.json`: `active_prompt=SP-030`, `state=in_progress`, step updated to SP-030 in_progress.
- `session-handoff.json` / `ACTIVE_CONTEXT.md`: SP-030 in_progress overlay recorded.

## Falsifiers

- Any claim that an SLO was measured, telemetry was transmitted, a participant was consented, a sign-off was obtained, or `beta-readiness.json` left `blocked` would falsify this opening record.
- Any claim that R11 is `completed` or that Developer ID/notarization/clean-machine evidence exists is false.

## Next action

Run the SP-030 beta SLO/scenario/incident/sign-off evidence program in a user-present session; collect content-free aggregates only under explicit opt-in; close the locally-closable R11 gates; formalize ADR-046; keep `beta-readiness.json` blocked until SP-030/SP-031 evidence closes the R12 direct-evidence gates.
