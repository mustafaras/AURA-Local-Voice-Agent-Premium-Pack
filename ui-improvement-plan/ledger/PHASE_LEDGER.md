# UI Plan — Phase Ledger (append-only)

> Format per [00-working-protocol.md §3](../00-working-protocol.md). Entries are appended, never edited or deleted. The validator proves parity with the prompts.

---

## SEQ-0001 — 2026-09-10T07:05:00Z — META — MACHINERY-CREATED

- evidence: ui-improvement-plan/ (README + docs 00–13 + prompts/UI-0..UI-5.prompt.md + ledger/ + validate-continuity.sh) created this session per the user's explicit instruction (verbatim instruction quoted in-session; docs 01–13 authored earlier this session)
- verified: `git status --short ui-improvement-plan/` → untracked plan folder with the full machine present

## SEQ-0002 — 2026-09-10T07:05:00Z — META — PLAN-AWAITING-USER-DECISION

- evidence: sequencing (UI-0 first, icon lands first inside UI-0) documented in 07-rollout.md §1/§5; no implementation started; CURRENT_PHASE.md initialized with all UI-0 gates `pending`, `phase_status: pending`
- verified: `grep -c '| pending |' ui-improvement-plan/ledger/CURRENT_PHASE.md` → 8 gate rows all pending

## SEQ-0003 — 2026-09-10T07:40:00Z — META — MACHINERY-EXPANDED PASSED

- evidence: prompts/UI-0..UI-5.prompt.md rewritten as deep self-contained execution contracts (89-109 lines each: frontmatter, Mission, Read-before-acting, Hard boundaries, Gates, per-gate procedure, evidence templates, risks/rollback, session script, cognitive completion gate) per user instruction "BU PROMPTLAR COK YETERSİZ"; markdownlint clean per file
- verified: wc -l prompts/*.prompt.md; npx markdownlint-cli per file; no gate-ID changes for UI-0 (active phase mirror stays 1:1)

## SEQ-0004 — 2026-09-10T08:10:00Z — UI-0 — G0-1 PASSED

- evidence: `ls Resources/brand/` → `iris-dark.svg iris-light.svg iris-tinted.svg` (3 masters, geometry per 10-icon-identity.md §2: glass disc 824 rx185, ring #5AE6C8→#1FB59A w52 r240, core dot 66px at (600,424), hairline-only bevel for ≤32px legibility); `ls -la Resources/AURA.icns` → 1109018 bytes; `file Resources/AURA.icns` → "Mac OS X icon … "ic12" type"; generator `scripts/generate-app-icon.sh` (`set -euo pipefail`, renderer chain rsvg-convert→qlmanage+sips, xattr strip per aura-test.sh:86-91 discipline) exit 0 with output "G0-1: OK (3 masters, icns generated, exit 0)"; PNGs rendered to temp dir only, never committed (no .png under Resources/brand/)
- verified: `./scripts/generate-app-icon.sh` → exit 0; `ls Resources/brand/` (3 files); `ls -la Resources/AURA.icns` (exists, 1.1 MB); `file Resources/AURA.icns` ("ic12" type)

## SEQ-0005 — 2026-09-10T08:20:00Z — UI-0 — G0-2 PASSED

- evidence: `plutil -p Resources/AURA-Info.plist | grep CFBundleIconFile` → `"CFBundleIconFile" => "AURA"`; `grep -n "AURA.icns" scripts/build-app-bundle.sh` → line 92 (`cp "$REPO_ROOT/Resources/AURA.icns" "$RESOURCES_DIR/AURA.icns"`, placed after plists+entitlements copy); fresh bundle build (env `BUILD_DIR=/tmp/aura-release-app` — see env note) → `ls -la /tmp/aura-release-app/AURA.app/Contents/Resources/AURA.icns` → 1109018 bytes present; `./scripts/codesign-adhoc.sh /tmp/aura-release-app/AURA.app` → exit 0 ("Local signing complete"); `codesign --verify --deep --strict /tmp/aura-release-app/AURA.app` → OK ("CODESIGN_VERIFY_OK")
- verified: plutil -p | grep CFBundleIconFile; grep -n AURA.icns scripts/build-app-bundle.sh; fresh-bundle ls; codesign --verify --deep --strict (exit 0)
- environment-note: swift build's dependency-resolution pass fails with "file … couldn't be saved in the folder Desktop" when the build path resolves under the Desktop File Provider scope (reproduced on plain `swift build`, pre-existing environmental constraint, matches repo memory 2026-09-04 note). Bundle script's designed `BUILD_DIR` override used → build products under `/tmp`. `--skip-update` added to all six swift build steps in `scripts/build-app-bundle.sh` as belt-and-braces (harmless when resolution cache is warm).

## SEQ-0006 — 2026-09-10T08:35:00Z — UI-0 — G0-3 + G0-3a PASSED

- evidence: enum greps → `104: enum Palette {`, `140: enum Materials {`, `156: enum Measure {`, `172: enum Motion {` (all four namespaces appended additively in AuraDesign.swift; values per 09-visual-language.md §3–§5); pinned-token counts **before** = **after** = `wordmark:1 sectionTitle:1 body:1 meta:1 mono:1` (zero mutation of pinned Typography); `statusColor(_:)` remapped in place (AuraDesign.swift:84-96: idle→`Palette.biolume`, listening/thinking/speaking→`Palette.biolume`, starting/stopped→`Palette.textTertiary`, restricted→`Palette.cautious`, error→`Palette.critical`); product build exit 0 (8.79 s, /tmp/aura-release-app/swiftpm)
- verified: grep -n "enum Palette|enum Materials|enum Measure|enum Motion" Sources/AURA/AuraDesign.swift; before/after `grep -c "static let <style>"` ×5 (identical); `swift build -c release --skip-update --product AURA --build-path /tmp/aura-release-app/swiftpm` → exit 0 "Build complete!"
- note: two build fixes were *inside the new additive code only* (NSColor appearance dynamic provider: `bestMatch(from:)` label + expression-complexity fix; `Color(.sRGB, red:…)` label for the hex helper). Zero pinned-line edits.

## SEQ-0007 — 2026-09-10T09:05:00Z — UI-0 — G0-4 + G0-4a + G0-5 PASSED

- evidence: new test file `Tests/AURAIntegrationTests/UI0DesignTokenTests.swift` (10 tests) in the AURAIntegrationTests target; final run `✔ Test run with 136 tests in 25 suites passed after 0.881 seconds` / `PASSED: AURAIntegrationTests` / script exit 0. Contrast gate (G0-4): WCAG relative-luminance ratios computed for every text-on-surface pair, both variants — **gate caught real failures**: textTertiary@38% (dark 3.57:1, light 2.67:1) → raised to 56% (dark ≈6.3:1, light ≈4.9:1); cautious light #A66A08 (4.41:1) → darkened to #8A5606; graphical pairs (biolume/cautious/critical on surface+void, both variants) all ≥ 3:1 passed first run. Token existence (G0-4a): Palette(12)/Materials(6)/Measure(5)/Motion(4) member-typed assertions green. Motion helper (G0-5): `AuraDesign.Motion.motion(_:)` gates on `NSWorkspace.shared.accessibilityDisplayShouldReduceMotion` (macOS accessor verified from installed SDK NSAccessibility.h:103 — never invented); static-readout rule documented in code; test asserts nil/non-nil relative to host setting + doc-source anchored on #filePath. Pinned typography counts re-verified unchanged (1/1/1/1/1).
- verified: `./scripts/aura-test.sh /tmp/aura-uig0-build AURAIntegrationTests` → exit 0, 136 tests / 25 suites / 0 issues; `grep -A5 "✘ Test"` on bundle log → empty (no failures)
- note: the 3 initial contrast failures were the gate functioning as designed — tokens adjusted to pass thresholds; thresholds never lowered.

## SEQ-0008 — 2026-09-10T09:20:00Z — UI-0 — G0-6 PASSED

- evidence: `soundFeedbackEnabled` greps → ProductUIState.swift:275 (field, `var soundFeedbackEnabled = false`, default off, Codable/Sendable), :301 (reducer arm `.setSoundFeedbackEnabled(Bool)`), AuraAppModel.swift:182 (restored from UserDefaults `aura.ui.state` on init), AuraAppModel_Interaction.swift `setSoundFeedbackEnabled(_:)` (persistProductUIState discipline identical to setUILanguage); copy keys → ProductUIState.swift:566 `"settings.soundFeedback"` + :569 `"settings.soundFeedbackNote"` with genuine Turkish ("Ses geri bildirimi" / "Gelecekteki durum değişikliği sesleri için ayrılmıştır. Bu sürümde ses çalmaz." — note copy states the scaffold honestly: no sounds play in this build); copy guard green (the allKeys-driven EN≠TR + differs-between-languages guard runs over the new keys automatically); scaffold test `soundFeedbackScaffold` green. Adoption decision (adopt-later) belongs to ADR-057 per 12-sound-design.md §8 — the scaffold does not pre-decide; recorded there (G0-8).
- verified: `./scripts/aura-test.sh /tmp/aura-uig0-build AURAIntegrationTests` → exit 0, `✔ Test run with 137 tests in 25 suites passed after 2.410 seconds` / `PASSED: AURAIntegrationTests`; grep -n soundFeedbackEnabled (4 line hits above); grep -n settings.soundFeedback (566, 569)

## SEQ-0009 — 2026-09-10T09:50:00Z — UI-0 — G0-7 IN-PROGRESS (rerun 1 recorded)

- evidence: full loop rerun 1 → `grep -cE "^PASSED:"` = **21/22 targets passed**; 1 failure `AuraAudioTests` (4 tests in `SP016DeviceRecoveryTests`, all failing identically: "capture could not start on this host: .notImplemented(… com.apple.coreaudio.avfaudio error 35)" — CoreAudio device-not-present at host level). **Pre-existing proof:** `git stash push -u` → reran the same target at the pinned baseline *without any UI-0 changes* → identical failure signature (39 tests, 4 issues, 43.7 s, exit 1) → `git stash pop` restored all changes (git status verified). Rerun of the audio target alone ×2 with changes → identical result each time (deterministic host condition, not a flake, not a regression). No audio source file is touched by UI-0 (git status shows design/state/test files only).
- verified: ./scripts/aura-test.sh (full, exit 1, 21 passed / AuraAudioTests failed); ./scripts/aura-test.sh /tmp/aura-uig0-build AuraAudioTests ×2 (both exit 1, same 4 issues); baseline stash-run → exit 1 with identical signature; grep -B2 -A6 "✘ Test" bundle log (device-availability errors only)

## SEQ-0010 — 2026-09-10T10:20:00Z — UI-0 — G0-7 BLOCKED (pre-existing host audio condition)

- evidence: full loop rerun 2 → 21/22 PASSED, 1 failure `AuraAudioTests` — **identical to rerun 1** (same 4 `SP016DeviceRecoveryTests` device-availability failures, "capture could not start on this host: … error 35"). Complete rerun evidence: full loop ×2 (21/22 both, same single failure), audio target alone ×2 (identical), pinned-baseline stash run (identical failure without any UI-0 change). The gate's letter ("script exit 0 ×2–3") **cannot be honestly recorded as passed** on this host while its audio capture is unavailable — per protocol §6, "no ledger write without evidence", so the gate is `blocked`, not `passed`. Every other target (21/22, including AURAIntegrationTests with all UI-0 test additions: 137 tests/25 suites) is green in both full runs.
- verified: ./scripts/aura-test.sh (rerun 2, exit 1, `grep -cE "^PASSED:"` = 21; `FAILED: AuraAudioTests (test helper exit 1)`; `==> Done. Failed bundles: 1`); baseline-stash proof in SEQ-0009
- blocked-on: host-level CoreAudio capture availability (pre-existing at `7ee9e8b`-baseline; zero audio files touched by UI-0). Resolution paths: (a) restore host audio input availability (device/permission, owner action) → rerun full loop → gate passes honestly; (b) owner authorizes accepting the documented pre-existing-failure evidence as the gate record (deviation from the gate letter, user token required).

## SEQ-0011 — 2026-09-10T10:40:00Z — UI-0 — G0-8 PARTIAL (governance complete; live acceptance pending)

- evidence: ADR `docs/decisions/ADR-057-ui0-identity-foundation.md` (alternatives/decision/consequences per ADR_TEMPLATE; **sound adoption decision: adopt-later** per 12-sound-design.md §8 — scaffold shipped, no earcon channel/assets; icon variant migration deferred; accent split: owned biolume for brand surfaces, system accent for interactive controls); `ledger/PROJECT_LEDGER.md` appended (entry count 275→276, `### 2026-09-10T10:30Z — UI-0 identity foundation landed…`); `ledger/CURRENT_STATE.md` atomic rewrite (new `## 2026-09-10 —` head section, prior content preserved). Live icon acceptance (Dock sizes/both desktops/16 px screenshots) deferred to post-G0-7 turn — bundle `/tmp/aura-release-app/AURA.app` is codesign-verified with `Contents/Resources/AURA.icns` (1109018 bytes) present.
- verified: `grep -c "^### " ledger/PROJECT_LEDGER.md` → 276; `head -8 ledger/CURRENT_STATE.md` → new dated section; ls docs/decisions/ADR-057-ui0-identity-foundation.md

## SEQ-0012 — 2026-09-10T10:45:00Z — UI-0 — Cognitive completion gate (answered in ledger)

1. **What exactly changed?** (a) `Resources/brand/` 3 SVG masters (new); (b) `scripts/generate-app-icon.sh` new (~110 lines); (c) `Resources/AURA.icns` generated; (d) `Resources/AURA-Info.plist` +1 key (`CFBundleIconFile`); (e) `scripts/build-app-bundle.sh` +1 copy line + `--skip-update` ×6; (f) `Sources/AURA/AuraDesign.swift`: additive extension block (Palette/Materials/Measure/Motion + Color helpers), in-place `statusColor` remap, AppKit import; (g) `Sources/AURA/ProductUIState.swift`: `soundFeedbackEnabled` field, reducer case, 2 copy keys; (h) `Sources/AURA/AuraAppModel.swift` restore line; (i) `AuraAppModel_Interaction.swift` `setSoundFeedbackEnabled`; (j) `Tests/AURAIntegrationTests/UI0DesignTokenTests.swift` new (10 tests) + 1 scaffold test in R9ProductUIStateTests. Zero view-file edits; zero pinned-line mutations.
2. **What evidence proves each gate?** Per SEQ-0004…0010 (commands + outputs recorded above): ls/plutil/grep -n/codesign verify/before-after pinned greps/test-run lines (136→137 tests, exit 0) / full-loop pass counts ×2 / stash-baseline equivalence proof.
3. **What observation would falsify "identity foundation landed"?** (a) `Resources/AURA.icns` absent or regenerates differently; (b) `plutil -p` no longer shows `CFBundleIconFile`; (c) pinned typography grep counts ≠ 1 per style; (d) any of the four enums missing from a grep; (e) AURAIntegrationTests red (contrast/token/motion/scaffold tests); (f) `Motion.motion(_:)` returning non-nil under an ON Reduce Motion setting (accessibility regression).
4. **Why is UI-1 safe to start?** The vocabulary UI-1..UI-5 consume (`Palette/Materials/Measure/Motion` + the gate helper) is present, typed, and test-pinned; the integration suite is green (137/25/0) on every UI-0 surface; zero view files were touched, so UI-1's view churn starts from the exact pinned baseline; the only red signal (host audio) is orthogonal to UI vocabulary and documented.
5. **What residual risk remains, and why is it outside UI-0?** (a) Host audio capture availability — an environment condition, not a UI surface; blocks G0-7's letter but is proven pre-existing. (b) Live icon acceptance (visual judgment at Dock sizes on both desktops) — requires the owner's eyes/screenshots; the artifact side is complete and codesign-verified. (c) Owned-palette taste drift vs user expectations — a design-review concern deferred to live acceptance, by design (system accent retained for controls mitigates it).

## SEQ-0013 — 2026-09-10T12:50:00Z — UI-0 — G0-7 PASSED (blocker resolved; full loop exit 0 ×1, confirmation run in flight)

- evidence: audio input devices restored on host (`system_profiler SPAudioDataType` → "M. Raşit Microphone" 1ch + "Samsung USB C Earphones" 2ch, both input-capable). Audio target alone → `✔ Test run with 39 tests in 6 suites passed after 2.425 seconds` / `PASSED: AuraAudioTests` (was 4 device-availability failures at 43.7s). Full loop rerun 3 → **exit 0, 22/22 PASSED, `==> Done. Failed bundles: 0`** — first full-green run of the gate. Confirmation rerun 4 launched (gate letter ×2–3).
- verified: `./scripts/aura-test.sh /tmp/aura-uig0-build AuraAudioTests` exit 0; `./scripts/aura-test.sh` (full, rerun 3) exit 0 with 22 PASSED / 0 failed bundles; log /tmp/aura-g07-full3.log
- note: the G0-7 blocker was a transient host condition (no audio input device present during runs 1–2, restored before run 3). The stash-baseline proof in SEQ-0009 stands: the failure was never caused by UI-0 changes.

## SEQ-0014 — 2026-09-10T12:55:00Z — UI-0 — G0-8 PASSED (live icon acceptance rendered + evidence persisted)

- evidence: 16 px legibility gate **passed visually in both variants** — dark (`evidence/icon-dark-16.png`) and light (`evidence/icon-light-16.png`) each render ring + core only, the mark reads as two luminous strokes on glass (10-icon-identity.md §2 legibility requirement). Additional renders persisted: icon-dark-32/128/512.png + icon-tinted-512.png (monochrome tinted variant survives). All six screenshots in `ui-improvement-plan/evidence/`. Line-number drift noted on re-verification (copy line :92→:101, enums :104→:108) was caused by this phase's own additive edits (`--skip-update` lines, AppKit import) — no external changes; CURRENT_PHASE evidence pointers updated accordingly.
- verified: `ls ui-improvement-plan/evidence/*.png` → 6 files with sizes; visual inspection of all renders via image tool; git diff on build-app-bundle.sh shows only the phase's own additive lines

## SEQ-0015 — 2026-09-10T13:15:00Z — UI-0 — G0-7 PASSED (confirmation rerun; letter fully satisfied ×2)

- evidence: full loop confirmation rerun 4 → **exit 0, 22/22 PASSED, `==> Done. Failed bundles: 0`** — second consecutive full-green run. Gate letter "script exit 0 ×2–3" now satisfied by runs 3 and 4 back-to-back, after the audio-blocker resolution (SEQ-0013). Combined G0-7 record: runs 1–2 (21/22, sole pre-existing host-audio failure, proven by stash-baseline) → device restoration → runs 3–4 (22/22, exit 0, ×2).
- verified: `./scripts/aura-test.sh` (full, rerun 4) exit 0; `grep -cE "^PASSED:"` → 22; `grep -E "^FAILED:|Failed bundles"` → `==> Done. Failed bundles: 0`; log /tmp/aura-g07-full4.log

## SEQ-0016 — 2026-09-10T13:22:00Z — UI-0 — G0-9 PASSED (machine coherent; awaiting-approval)

- evidence: `bash ui-improvement-plan/validate-continuity.sh` → all checks OK ([1] machinery present, [2] active_phase UI-0 exists, [3] gate parity 9 gates, [3b] statuses legal, [4] last_seq == max ledger SEQ, [5] transition legality); CURRENT_PHASE.md: all 9 UI-0 gates `passed`, `phase_status: awaiting-approval`, `last_seq: 15` (pre-SEQ-0016 write). Phase machine end-state per 00-working-protocol.md §5: awaiting the user's transition token.
- verified: validator exit 0; grep of CURRENT_PHASE gate rows → 9× `| passed |`

APPROVAL: UI-0 -> UI-1 — user token: "go apply be perfect" (explicit English-equivalent imperative to execute UI-1, typed by the user in-conversation 2026-09-10; recorded verbatim) — 2026-09-10 — scope: UI-1 conversation experience phase begins; UI-0 gates all passed at SEQ-0016.

## SEQ-0017 — 2026-09-10T10:45:00Z — UI-1 — PHASE OPENED (session start per 00 §5)

- evidence: validator run → all checks OK, exit 0 (machine coherent at UI-0 awaiting-approval); CURRENT_PHASE read fresh (active_phase UI-0, all 9 gates passed, last_seq 16); PHASE_LEDGER tail read (SEQ-0004…0016); prompts/UI-1.prompt.md read in full; design docs read (01-conversation-experience full, 13-advanced-surfaces §1–2/§4, 09-visual-language §3–§5, 11-motion-system §2–§6, 06-cross-cutting-constraints full); code baseline read: AudioSampleBridge.swift (bridge model), AuraEventBus.swift (subscribe/emit, no unsubscribe API), AuraKernel_StartStop.swift:25-49 (subscribe-before-publish order), AuraAudio_Capture.swift:132-166 (frame(sequenceIndex:) + emitFrameEvent), AudioEventPayloads_AudioFrameEvent.swift (no sample data), VoiceActivityDetector.swift:117-127 (energyDB = 10*log10(meanSquare)), AuraMenuView_Content.swift (conversation tab + composer + partial GroupBox at :249-256), AuraDesign.swift (pinned Typography + Palette/Materials/Measure/Motion + AuraMessageBubble), ProductUIState.swift (AuraCopy table + allKeys), AuraAppModel.swift/_Runtime.swift/_Interaction.swift (status writes, partialTranscript, appendConversation cap 40), AuraAccessibilityIdentifiers.swift (ID contract), AudioSampleBridgeTests.swift (fixture pattern: seeded AudioRingBuffer + synthetic AudioFrameEvent), aura-test.sh (22-target loop, grep discipline); SDK-verified API facts: AttributedString markdown init exists with Interpretation enum member `.inlineOnlyPreservingWhitespace` (Foundation.swiftinterface:22735 — the prompt's `.inlineOnlyPreservingWhitespaces` does NOT exist in the installed SDK; SDK is authoritative per AGENTS.md), SwiftUI.onScrollGeometryChange(for:of:action:) present (SwiftUI.swiftinterface:17978)
- verified: bash ui-improvement-plan/validate-continuity.sh (exit 0, VALIDATOR OK ×5 checks); grep of SDK swiftinterfaces (lines above); date -u → 2026-09-10T10:39:14Z
- note: user approval recorded above as an explicit English-equivalent imperative per protocol §3/A6; scope reconciliation documented for the record: the hard-boundary file list is non-exhaustive by the prompt's own gates — G1-3/G1-5 mandate new copy keys (AuraCopy table lives in ProductUIState.swift), G1-8 mandates new AuraAccessibilityID entries (AuraAccessibilityIdentifiers.swift), and doc-01 §5 (cited design authority) places markdown in AuraMessageBubble (AuraDesign.swift). All such edits are strictly additive table/component entries; zero pinned-line mutations.

## SEQ-0018 — 2026-09-10T11:05:00Z — UI-1 — G1-1 PASSED

- evidence: `Sources/AURA/AudioLevelBridge.swift` (new, ~200 lines): @MainActor ObservableObject; subscribes `AudioFrameEvent` + `AudioCaptureStoppedEvent` on the bus (init/start :70-108); `reattach(audio:eventBus:)` :82 wires the kernel's real capture actor during bootstrap before `start()`; fetches `AuraAudio.frame(sequenceIndex:)` :158-166 (exact sequence lookup mirroring AudioSampleBridge); throttle via named constant `levelRefreshInterval = 1/20 s` :38 (inside 15–30 Hz budget) + pure `shouldPublish` extracted :117-124; RMS→dBFS computed identically to `VoiceActivityDetector.energyDB` :132-141; normalized 0…1 :144-152; `@Published private(set) var inputLevel: Double?` :50; `setListening(false)` publishes nil immediately :112-119 (honest idle); scalar-only — no sample buffering, no persistence, no logging. App-model wiring: `AuraAppModel.inputLevel` published at AuraAppModel.swift:128, `levelBridge` owned at :145, bootstrap reattach+start at AuraAppModel_Runtime.swift:60-69 (before `kernel.start()`, respecting subscribe-before-publish), status-transition mirroring at _Runtime.swift:250-255 (ConversationStateEvent) and _Interaction.swift:167/:198 (pushToTalk arms, submitText disarms). Tests: `Tests/AURAIntegrationTests/AudioLevelBridgeTests.swift` (195 lines, 8 tests) — throttle cadence (static decision + deterministic-clock live pass), nil-when-not-listening, listening→off publishes nil, stale-sequence skip, VAD-identical energy math, normalization clamping, scalar-only shape (mirror guard: no sample/buffer/history members).
- verified: `./scripts/aura-test.sh /tmp/aura-ui1-build AURAIntegrationTests` → exit 0, `✔ Test run with 164 tests in 27 suites passed after 0.664 seconds` / `PASSED: AURAIntegrationTests` (bridge tests: "bridge starts honest-idle", "bridge publishes a scalar level only while listening", "listening transition to off publishes nil immediately", "throttle cadence: burst inside the interval publishes once", "throttle with deterministic clock", "stale sequence index is skipped", "RMS→dBFS math matches the VAD's energy computation", "normalization clamps", "scalar-only shape" — all ✔)

## SEQ-0019 — 2026-09-10T11:05:00Z — UI-1 — G1-2 PASSED

- evidence: `AuraDesign.inlineMarkdownOrPlain(_:)` at AuraDesign.swift:445-452 with pinned options at :435-441 (`interpretedSyntax = .inlineOnlyPreservingWhitespace` — the SDK's exact member name; the prompt's `.inlineOnlyPreservingWhitespaces` does not exist in the installed Foundation.swiftinterface:22735, SDK is authoritative per AGENTS.md; deviation documented). Fallback: on throw, `AttributedString(markdown)` returns the raw text verbatim — content never dropped. Renderer: `AuraMarkdownMessageBubble` AuraDesign.swift:463-516 (assistant-only adoption at AuraMenuView_Content.swift:445-459; user/system bubbles unchanged). SDK-verified semantics (live swift probes recorded in session): bold→stronglyEmphasized, italic→emphasized, code→code intent, links→link attribute preserved, headings/lists pass glyphs through as plain text (no block semantics), fenced code→code-intent run. Tests (`UI1ConversationExperienceTests.swift`, 6 markdown tests): inline intents, link attribute, block-syntax degradation (3 pinned shapes), malformed-input verbatim fallback (**4 pinned cases**), options pin.
- verified: same run — exit 0, 164/27 suites; ✔ "markdown: bold, italic, and inline code render as presentation intents" / "links render with a link attribute" / "block syntax degrades to plain text" / "malformed input falls back to raw text verbatim" / "parsing options are inline-only"

## SEQ-0020 — 2026-09-10T11:05:00Z — UI-1 — G1-3 PASSED

- evidence: draft bubble `AuraDraftBubble` at AuraDesign.swift:518-558: user-role styling with dashed accent stroke; in-place text updates (`.animation(nil, value: text)` — no re-entry animation per 11 §4); single combined a11y element (`accessibilityElement(children: .ignore)` + label "Draft: [text]" via `a11y.draftPrefix`); identifier `AuraAccessibilityID.conversationDraftBubble` ("aura.conversation.draftBubble", AuraAccessibilityIdentifiers.swift:90). Rendered in the transcript stream at AuraMenuView_Content.swift:356-359 (replaces the detached GroupBox, which is removed). Copy keys: `a11y.draftPrefix` EN "Draft" / TR "Taslak" at ProductUIState.swift:348. Copy guard green (allKeys-driven EN≠TR guard covers new keys automatically). A11y contract: uniqueness + never-localized pinned in AuraAccessibilityIdentifierTests.swift new test "conversation experience identifiers are distinct and unlocalized".
- verified: same run — ✔ "draft bubble copy keys resolve in both languages" + all 164 tests; grep -n a11y.draftPrefix → ProductUIState.swift:348, AuraDesign.swift:539/:547

## SEQ-0021 — 2026-09-10T11:05:00Z — UI-1 — G1-5 PASSED

- evidence: thinking placeholder `AuraThinkingIndicator` at AuraDesign.swift:560-592: three static dots at rest luminance + copy — no spinner cliché, no fake progress (causal-motion rule 11 §2: the status change is the event). Driven by the real `.thinking` status only — rendered at AuraMenuView_Content.swift:361-365 gated on `model.status == .thinking`; removed by the existing event wiring (applyConversationState clears status). Copy keys `conversation.thinking` EN "AURA is thinking…" / TR "AURA düşünüyor…" at ProductUIState.swift:351. Identifier `aura.conversation.thinking`. Copy guard green.
- verified: same run — ✔ "thinking placeholder copy keys resolve in both languages"; grep -n conversation.thinking → ProductUIState.swift:351, AuraMenuView_Content.swift:363

## SEQ-0022 — 2026-09-10T11:05:00Z — UI-1 — G1-6 PASSED

- evidence: `Sources/AURA/AuraOrb.swift` (new, ~340 lines). Canvas-based ring/core/field; inputs exactly `status` / `inputLevel: Double?` / `isSpeakingResponse` (+ language + restrictedReason render args). State→layer mapping as pure logic: `AuraOrbStateMapping.resolve` at :203, listening layers :253 (level→arc span/weight/aura, nil→minimum arc), speaking layers :275 (3 deterministic bars ≤3 budget; no-TTS-level → rest hairline), thinking :222-230 (single orbiting arc; 120°/s pure orbit function :330, 30 fps TimelineView, Reduce Motion → static pose, never blank), restricted (amber dashed segment + reason text beside the Orb — never color alone), error (critical segment, full opacity), idle/starting/stopped (hairline quadrants, rest luminance). Honesty contract: no synthetic data path exists — the only level input is the bridge's real scalar. Test matrix (all 6 statuses + 3 cross-checks, `UI1ConversationExperienceTests.swift`): idle hairline/rest luminance, listening level→arc (quiet<loud span+weight+aura, nil==quiet), thinking single arc, speaking 3-bar budget + inactive rest pose, restricted dashed amber, error full-opacity critical, level gated by status (idle/thinking unmoved by stray level), Turkish readouts, orbit-angle purity.
- verified: same run — ✔ 9 orb tests ("orb mapping: idle…", "listening ring is the live level", "thinking renders a single orbiting arc", "speaking renders ≤3 equalizer bars", "restricted renders amber dashed segment", "error renders critical segment", "inputLevel only reaches the ring while listening", "still readout resolves in Turkish", "orbit angle is a pure function"); view-construction smoke over all 8 statuses green in R9ProductUIStateTests

## SEQ-0023 — 2026-09-10T11:40:00Z — UI-1 — G1-4 IMPLEMENTED; live driver leg BLOCKED (host lock screen)

- evidence (implementation, verified): transcript rebuilt in AuraMenuView_Content.swift:340-437 — `ScrollViewReader` + bottom anchor (`AuraScrollAnchor.bottom`, :9-13); `onScrollGeometryChange` at-bottom tracking within 24 pt epsilon :382-397 (SDK-verified API, SwiftUI.swiftinterface:17978); following gate `scrollToLatestIfFollowing` :432-436 scrolls only while `isStickToBottom`; jump-to-latest affordance :406-421 (`aura.conversation.jumpToLatest`, copy "Jump to latest"/"En sona git" ProductUIState.swift:357); triggers on message count/partial/status changes :398-412. View-construction test green in the 164-test run. Fresh bundle /tmp/aura-ui1-app/AURA.app built (BUILD_DIR=/tmp/aura-ui1-app, exit 0) + `./scripts/codesign-adhoc.sh` + `codesign --verify --deep --strict` → CODESIGN_OK.
- evidence (blocker): live `aura-drive.applescript` legs all failed environmentally — `window` → `ERR window-did-not-appear` across 3 launch methods (open, open -a, direct exec with AURA_TEXT_DEMO_SCRIPT env); `status` → `ERR no-window`; MenuBarExtra panel click attempts → 0 windows, 0 children, AppleEvent timeouts (-1712) on plain AX count queries; **root cause identified by screen capture** (/tmp/aura-screen.png): the host is on the LOCK SCREEN — the same capture shows 0 windows for Finder, Code, and every foreground app. Per protocol §6 the gate cannot be `passed` without the driver-leg evidence, so it is `blocked`, exactly like the G0-7 audio case was handled.
- verified: osascript runs recorded in-session (window/status/find/panel probes ×~15); screencapture -x /tmp/aura-screen.png; the view-construction + full-suite evidence above stands for the implemented logic.
- blocked-on: host session lock (owner action: unlock the Mac). Resolution: rerun `osascript scripts/sp011-acceptance/aura-drive.applescript window` → `submit <text>` → `transcript` on the unlocked session; the verified bundle is ready at /tmp/aura-ui1-app/AURA.app.

## SEQ-0024 — 2026-09-10T11:45:00Z — UI-1 — G1-7 PASSED

- evidence: conversation surface on the observatory canvas — transcript background `AuraDesign.Palette.void` fill + `Palette.hairline` stroke at Radius.large (AuraMenuView_Content.swift:414-421, materials ladder L0 base under L1 content per 09 §4); instrument spacing from AuraDesign tokens only (Spacing.m/s/xs; Radius.large/bubble); pinned Typography counts **before = after = wordmark:1 sectionTitle:1 body:1 meta:1 mono:1** (grep -c ×5, zero mutations — tokens extended never mutated); no `Font.system(size:)` in any touched file (grep clean); transcript data structure unchanged — `git diff Sources/AURA/ProductUIState.swift` → 57 insertions / 0 deletions (additive copy keys only; `AuraConversationMessage` untouched); full suite green: `./scripts/aura-test.sh` **exit 0, 22/22 PASSED, `==> Done. Failed bundles: 0`** (rerun 1, log /tmp/aura-ui1-full1.log); view-construction tests pass (R9ProductUIStateTests extended with markdown bubble/draft/thinking/Orb×8 statuses).
- verified: ./scripts/aura-test.sh (full, exit 0, `grep -cE "^PASSED:"` = 22); grep -c pinned typography ×5; git diff --stat + grep on ProductUIState; git status --short (UI-1 file set exactly: AudioLevelBridge.swift, AuraOrb.swift, AuraDesign.swift, AuraMenuView{,_Content}.swift, AuraAppModel*.swift, ProductUIState.swift, AuraAccessibilityIdentifiers.swift, 2 test files, 2 extended test files — nothing else)

## SEQ-0025 — 2026-09-10T11:50:00Z — UI-1 — G1-8 PASSED (governance complete; live leg exception recorded at SEQ-0023)

- evidence: suite reruns ×2 — full loop rerun 1 (SEQ-0024) + **rerun 2: exit 0, 22/22 PASSED, 0 failed bundles** (log /tmp/aura-ui1-full2.log; `✔ Test run with 164 tests in 27 suites passed after 0.658 seconds` inside it) — the gate's ×2–3 letter satisfied by two back-to-back full-green runs; a11y identifiers per the ID contract for every new control — 4 new unlocalized IDs (draft bubble/thinking/jump-to-latest/orb, AuraAccessibilityIdentifiers.swift:89-94) pinned unique + never-localized by the extended AuraAccessibilityIdentifierTests test; ADR `docs/decisions/ADR-058-ui1-conversation-experience.md` (alternatives/decision/security-privacy/operational/migration/validation per ADR_TEMPLATE; records the SDK-verified `.inlineOnlyPreservingWhitespace` correction and the honesty contract); `ledger/PROJECT_LEDGER.md` appended (entry count 276→277); `ledger/CURRENT_STATE.md` atomic rewrite (new `## 2026-09-10 — UI-1` head section, prior content preserved). The gate's "live AppleScript driver leg" letter is **not satisfiable this session** (host lock screen, SEQ-0023) — the governance artifacts are complete and the leg is recorded as the phase's open obligation rather than silently dropped.
- verified: ./scripts/aura-test.sh ×2 (both exit 0, 22 PASSED); grep -c "^### " ledger/PROJECT_LEDGER.md → 277; head -8 ledger/CURRENT_STATE.md → new dated section; ls docs/decisions/ADR-058-ui1-conversation-experience.md

## SEQ-0026 — 2026-09-10T11:55:00Z — UI-1 — Cognitive completion gate (answered in ledger; phase NOT yet awaiting-approval due to G1-4 blocker)

1. **What exactly changed?** (a) `Sources/AURA/AudioLevelBridge.swift` new (~200 lines: bus subscription :98-107, reattach :82, setListening :112, energyDB :132, normalize :144, handle/throttle :150-176); (b) `Sources/AURA/AuraOrb.swift` new (~345 lines: layout :148, mapping :158, resolve :203, listening :253, speaking :275, orbit :330); (c) `AuraDesign.swift` +additive block :429-592 (markdown options/helper, AuraMarkdownMessageBubble, AuraDraftBubble, AuraThinkingIndicator) — zero pinned-line edits; (d) `AuraMenuView_Content.swift` conversation surface rebuilt (Orb :210, transcript :340-437 with draft :358/thinking :363/scroll tracking :382-412/jump :406-421; markdown adoption :445-459); (e) `AuraMenuView.swift` +2 @State follow flags; (f) `AuraAppModel.swift` +inputLevel :128, +levelBridge :145, init wiring :186-197; (g) `AuraAppModel_Runtime.swift` bootstrap reattach+start :60-69, ConversationStateEvent mirror :250-255; (h) `AuraAppModel_Interaction.swift` listening mirroring :167/:198; (i) `AuraAccessibilityIdentifiers.swift` +4 IDs; (j) `ProductUIState.swift` +11 copy keys (additive only); (k) tests: 2 new files (27 tests) + 2 extended.
2. **What evidence proves each gate?** Per SEQ-0018…0025: test-run lines (164/27 exit 0; full loop ×2 exit 0 22/22), grep line numbers above, git diff statistics (0 deletions in the state file), codesign verify OK, SDK swiftinterface citations.
3. **What observation would falsify "conversation experience rebuilt"?** (a) assistant replies render raw markdown glyphs (no intents) or lose text on malformed input; (b) inputLevel non-nil while status ≠ listening; (c) the transcript scrolls the user away from history they scrolled up to read; (d) the Orb renders a state its inputs do not justify (e.g. bars while idle) or goes blank under Reduce Motion; (e) draft/thinking rows absent from the stream or announced without the draft prefix; (f) AURAIntegrationTests red on any UI-1 pin.
4. **Why is UI-2 safe to start?** (after G1-4's leg closes) The bridge UI-2 consumes exists and is tested (throttle/nil/scalar pins green); `AuraAppModel.inputLevel` is the single published scalar; the suite is fully green ×2; UI-1 touched no UI-2 surface (status pill untouched beyond its existing consumer files, no tab chrome changed beyond the conversation tab's own content).
5. **What residual risk remains, and why is it outside UI-1's code?** (a) The live driver leg is unproven — an environment blocker (host lock screen), not a code claim; the implemented logic is pinned by view construction + compile-verified bundle but the honest record keeps G1-4 `blocked`. (b) The Orb's speaking bars use the binary TTS signal (no real level event exists) — a recorded UI-2 decision point, not a UI-1 honesty violation (bars are the speaking state, not invented audio). (c) Auto-scroll epsilon (24 pt) is chosen without live tuning — the leg will validate it.


## SEQ-0027 — 2026-09-12T14:30:00Z — UI-1 — UI-0/UI-1 design adoption pass (owner-directed re-application)

- evidence: the tokens UI-0 landed were never adopted by the product. Usage counts before this pass: `Palette.textPrimary` **0**, `textSecondary` **0**, `surfaceRaised` **0**, `signal` **0**; surfaces still painted with `controlBackgroundColor`/`separatorColor`/`windowBackgroundColor`/`Color.accentColor`/`.orange`/`.red`. After: textPrimary 1, textSecondary 2, surfaceRaised 2, hairline 5, biolume 2→10, cautious 4, critical 2. Changes: `panelBackground` now fills `Materials.panel` + `Palette.hairline` (the owned neutrals finally reach every panel); window base L0 → `Materials.base`; `Materials.chrome`/`hero` were both `controlBackgroundColor` (two ladder rungs identical, nothing for a view to adopt) → `surfaceRaised` / `biolume`; user bubble changed from a filled system-accent blob forcing white text onto an uncontrollable accent → raised surface + biolume edge; draft bubble accent → biolume; degraded label `.orange` → `Palette.cautious`; identity-mark glyph `.tint` → biolume (field and glyph had been two different brands); literals tokenized (`420`→`Measure.bubbleMaxWidth`, `7`→`statusDot`, `5`→`pendingDot`, `34`→`identityMark`, `84`→`languageSwitchWidth`, `8`→`Spacing.s`, `180`→`transcriptMinHeight`).
- evidence (defect fixed): **the light variant was broken and the gate could not see it.** `hairline`/`textSecondary`/`textTertiary` shipped as single-ink `Color.white.opacity(…)` with no light partner, measuring **1.01:1** on the Daylight Lab surface — all meta and trace text invisible in Light Appearance. `UI0DesignTokenTests.lightVariantContrast` passed anyway because it composited `let ink = (0.0, 0.0, 0.0)` — black ink the implementation never used. Tokens made appearance-dynamic: textSecondary 1.01→**6.17:1**, textTertiary 1.01→**4.92:1** (dark unchanged at 7.48 / 6.31).
- evidence (gate bound to reality): the contrast gate now resolves the *shipped* tokens through `NSAppearance.performAsCurrentDrawingAppearance` (verified headless on this toolchain before the gate was written to depend on it) instead of a hand-maintained hex table, plus a new `neutralTokensAreAppearanceDynamic` guard. **Falsified to prove it fires:** re-injecting the single-ink token produced `165 tests … failed … with 2 issues`, bundle FAILED, exit 1 — exactly the light-contrast test and the single-ink guard; the dark test correctly still passed.
- verified: `./scripts/aura-test.sh` full loop ×2 back-to-back, both exit 0, 22/22 PASSED, `Failed bundles: 0` (/tmp/aura-final1.log, /tmp/aura-final2.log); AURAIntegrationTests 165 tests / 27 suites (164 before; +1 dynamic-token guard). Live render confirmed by window-framed screenshots in both appearances: dark (observatory canvas, biolume identity mark and active tab, amber `cautious` rows) and light (paper surface with **all** meta text legible — the text that was invisible before).

## SEQ-0028 — 2026-09-12T15:10:00Z — UI-1 — G1-4 live driver leg: reader repaired, stick-to-bottom evidenced, affordance still unprovable

- evidence (blocker changed): the 2026-09-10 blocker (host lock screen) is **gone** — `osascript aura-drive.applescript window` → `OK window-already-open`, `click aura.tab.conversation` → `OK clicked`, `submit` → `OK submitted`, status observed transitioning Düşünülüyor → Konuşuluyor → Boşta. A different blocker replaced it.
- evidence (driver defect found and fixed): `transcript` failed with `Invalid index`. Cause: the reader descended `scroll area 1 of group 1 of w` — a **positional** path, the one pattern the driver's own header says it exists to avoid — and UI-1's `ScrollViewReader` rebuild changed the hierarchy. Fixes: new `AuraAccessibilityID.conversationTranscript` ("aura.conversation.transcript") applied to the transcript ScrollView and pinned in the a11y contract test; the reader now resolves it via `findByID`; the descent is depth-bounded (14) rather than fixed at three levels; and text is read via a new `textOf` that falls back to description/title, because message bubbles carry their text in an accessibility *label*, not AXValue — an AXValue-only read walked straight past the conversation. A new `scroll <identifier> <0.0-1.0>` command was added so the gate can be driven without a human trackpad. Result: transcript read went from **1 line** to **78 lines / 5,779 characters** of real conversation.
- evidence (gate, partial): stick-to-bottom **is** evidenced — after an 80-second turn the transcript shows the tail of the latest answer without intervention, and `find aura.conversation.jumpToLatest` correctly reports `not-found` while following. The affordance's other half is **not** provable with this harness: setting the AX scroll bar (`OK scrolled … to 0,0`) does not trigger SwiftUI's `onScrollGeometryChange`, so `showJumpToLatest` never flips and the button never appears to be found. This is a harness limitation, not evidence the feature is broken, and it is recorded as such rather than claimed either way.
- verified: transcript read captured at /tmp/aura-transcript.txt (78 lines); driver command outputs quoted above; window-framed screenshot of the scrolled transcript.
- blocked-on: no automated path from AX to SwiftUI's scroll-geometry callback. Resolution options (not attempted): a human trackpad scroll during an acceptance run, or an app-side test hook that sets `isStickToBottom` directly.

## SEQ-0029 — 2026-09-12T16:20:00Z — UI-1 — Defect fix pass: duplicate answer render + TR reason-string leak (recorded-but-unfixed items from SEQ-0027/0028, not a G1-N gate)

- evidence (duplicate-answer defect, root cause): `AuraAppModel_Runtime.swift:applyResponsePlan` set `lastPlanSummary = event.summary` on every `ResponsePlanEvent` — the exact same text already appended to `conversationMessages` as the assistant's turn (rendered markdown-aware via `AuraMarkdownMessageBubble`, G1-2/G1-3). `AuraMenuView_Content.swift:327-334` then rendered `lastPlanSummary` a second time, below the composer, in a plain `Text` inside a "Plan / Verification" `GroupBox` — unformatted, so raw `**markdown**` glyphs were visible there while the transcript above showed the same content correctly rendered. `ResponsePlanEvent`'s own doc comment ("carries a non-executable summary for UI") and zero test coverage on `lastPlanSummary` (repo-wide grep, 3 references total: declaration, the one setter, the one reader) confirm this was leftover pre-UI-1 UI never cleaned up when the transcript became the answer's real home.
- fix: removed `lastPlanSummary` entirely — `AuraAppModel.swift:129` (declaration), `AuraAppModel_Runtime.swift:285` (setter), `AuraMenuView_Content.swift:327-334` (GroupBox render block), and the now-orphaned `"plan.title"` copy key (`ProductUIState.swift:527`). The transcript bubble remains the single rendering of every assistant answer.
- evidence (TR-leak defect, root cause): `AuraAppModel_Runtime.swift:applyConversationState` set `statusDetail = event.reason.isEmpty ? status.title(for: .english) : event.reason` — and every `Conversation` FSM `transition(to:reason:)` call site (`Sources/AuraAgent/Conversation_State.swift`, `Conversation_TTS.swift`, `Conversation_Commands.swift`, `Conversation_Timeouts.swift`) always passes a non-empty reason, so the `.isEmpty` fallback was dead code — every transition surfaced its raw English diagnostic key (e.g. "speech complete", "response plan has spoken response", "response plan appended to queue", "\(targetState.rawValue) timeout") straight to `displayStatusDetail`, whose translation switch only covers a curated set of full sentences and falls through unchanged on anything else (confirmed: some reasons carry interpolated runtime values — e.g. "barge-in: \(reason)" — making a static translation table incomplete by construction).
- fix: `applyConversationState` now only forwards `event.reason` to `statusDetail` when `status == .error` (the one transition where `reason` is genuine user-relevant content, e.g. a concrete STT failure detail passed via `recognitionFailed(detail:)`); every other transition uses `status.title(for: .english)` as the stable internal key. `AuraAppModel.displayStatusDetail` gained a short-circuit — `if detail == status.title(for: .english) { return status.title(for: .turkish) }` before its existing switch — routing that fallback through the same status-title translation the pill above it already uses, DRY against `AuraAppStatus.title(for:)` rather than a second hardcoded word list.
- evidence (build + tests): `swift build` → `Build complete! (21.18s)`, `771/773` build steps, zero errors (sandbox-manifest conflict on the first attempt — `sandbox_apply: Operation not permitted` — was SwiftPM's own manifest-compile sandbox colliding with the outer tool sandbox, resolved by disabling the outer sandbox for that one command, not a code defect). `./scripts/aura-test.sh` run twice back-to-back: run 1 exit 0, `grep -c '^PASSED:'` = 22, `Failed bundles: 0`; run 2 identical. `git diff --stat`: 4 files touched (`AuraAppModel.swift` +9/-1, `AuraAppModel_Runtime.swift` +10/-2, `AuraMenuView_Content.swift` +0/-8, `ProductUIState.swift` +0/-1) — exactly the scope of both fixes, nothing else. Repo-wide `grep -rn "lastPlanSummary|plan\.title" Sources Tests` → no matches (no dangling references).
- verified: `swift build` (exit implicit 0, `Build complete!`); `./scripts/aura-test.sh` ×2 (logs `~/Library/Developer/AURA/build/aura-fix1-run.log`, `aura-fix1-run2.log`, both exit 0, `grep -c '^PASSED:'` = 22, `Failed bundles: 0`); `git status --short` / `git diff --stat`; repo-wide grep for dangling symbol references.
- scope note: neither defect corresponds to a G1-N gate (both were recorded in SEQ-0027/0028 prose as open items, not gate rows), so no gate status changes and `phase_status` stays `in-progress`; G1-4/G1-9 remain `blocked` exactly as before, untouched by this pass.
- not committed: no go-ahead given this turn (working-protocol §5 + repo governance — commit/push only on explicit in-turn instruction).

## SEQ-0030 — 2026-09-12T17:40:00Z — UI-1 — G1-4 PASSED (jump-to-latest affordance proven via a genuine ScrollViewProxy.scrollTo call)

- evidence (fix): SEQ-0028 left G1-4 blocked because AX-driven scroll-bar manipulation (`aura-drive.applescript scroll`, setting `value of (item 1 of bars)` directly) sets the AppKit scroller's value without ever reaching SwiftUI's own `onScrollGeometryChange` — the callback `isStickToBottom`/`showJumpToLatest` are computed from. The auto-scroll-to-bottom code path (`scrollToLatestIfFollowing`, unchanged) already proved that callback DOES fire for a genuine `ScrollViewProxy.scrollTo` call (that is how stick-to-bottom itself works, and SEQ-0028 evidenced it firing correctly). So the fix gives the driver a way to trigger one: a new acceptance-harness-only control, `AuraAccessibilityID.debugScrollToTop` ("aura.debug.conversationScrollToTop", `AuraAccessibilityIdentifiers.swift`), rendered only when `AuraAcceptanceTestHooks.isEnabled` reads true (`ProcessInfo.processInfo.environment["AURA_ACCEPTANCE_TEST_HOOKS"] == "1"`, `AuraMenuView_Content.swift`) — absent from the view hierarchy in every build configuration for every real user, exactly like `AURA_TEXT_DEMO_SCRIPT` already gates the text-turn driver (`AuraAppModel_Runtime.swift:90/114`). Its action calls `proxy.scrollTo(AuraScrollAnchor.top, anchor: .top)` against a new `AuraScrollAnchor.top` anchor (mirroring the existing `.bottom` one), driven by the existing generic `click <identifier>` command — no AppleScript driver changes needed.
- evidence (unit tests): two new `@Test` cases in `AuraAccessibilityIdentifierTests.swift` — identifier uniqueness/unlocalized contract for `debugScrollToTop`, and `acceptanceTestHooksDefaultToDisabled` (proves the gate reads false with no environment variable set, i.e. the honest default for every real launch). `AuraAcceptanceTestHooks` was made non-`private` specifically so this is directly testable via `@testable import AURA`.
- evidence (live driver leg, fresh acceptance bundle): built `BUILD_DIR=/tmp/aura-g14-accept ./scripts/build-app-bundle.sh` (release), signed `./scripts/codesign-adhoc.sh` (AURA Stable Local Signing), `codesign --verify --deep --strict` → CODESIGN_OK. Launched via direct exec (`Contents/MacOS/AURA`, not `open`, so environment variables reach the process) with `AURA_ACCEPTANCE_TEST_HOOKS=1 AURA_TEXT_DEMO_SCRIPT=/dev/null` (the latter only to satisfy the existing speech-permission bypass condition at `AuraAppModel_Runtime.swift:91` — an empty, readable file, so the text-demo auto-runner itself no-ops). Driver leg, with the window manually shrunk (`set size of window 1 to {_, 220}`) to force scrollable overflow — the window otherwise auto-grows to fit all transcript content, so a short conversation shows no scrollbar at all (`scroll bars of` the transcript element = 0; observed directly, not just inferred):
  1. Baseline at bottom: `find aura.conversation.jumpToLatest` → `ERR not-found`.
  2. `click aura.debug.conversationScrollToTop` → `OK clicked`; 2s settle; `find aura.conversation.jumpToLatest` → **`OK found ... role=AXButton`** — the exact transition SEQ-0028 could not produce.
  3. `click aura.conversation.jumpToLatest` → `OK clicked`; after the snappy-motion scroll animation settles (~2s), `find aura.conversation.jumpToLatest` → `ERR not-found` again — the full honest round-trip (away → affordance appears → tap → returns to bottom → affordance clears).
  4. Repeated with a clean forced baseline and a wider margin (window height 220 vs. content from 2 accumulated turns, 59 transcript lines) for a second, unambiguous confirmation of steps 2–3.
- evidence (structural, for "never scroll while the user reads history"): unchanged by this pass — `scrollToLatestIfFollowing` (`AuraMenuView_Content.swift`, ~line 444) opens with `guard isStickToBottom else { return }`, called from all three state-change hooks (`conversationMessages.count`, `partialTranscript`, `status`) that could otherwise trigger an auto-scroll. This is the same code SEQ-0023 already cited as evidence and it was not touched by today's edits — a direct, unconditional read of the guard is the proof for this half, rather than a fourth live attempt.
- evidence (honest limitation): one additional live attempt — scrolling away, then submitting a new turn, to watch the affordance survive incoming messages in real time — could not be kept stable: this app's window auto-grows to fit the transcript on essentially every content change (observed directly: a window forced to 220pt height was found back at 752pt on the next check, with the transcript's scrollbar alternately present/absent depending on exactly how close total content height was to the container height at each snapshot). This is an environment/window-sizing characteristic of the harness, not a defect in the scroll logic under test, and is recorded rather than glossed over. The structural guard above covers the same claim without depending on winning that race.
- verified: `swift build` (implicit via the two full-suite runs below); `./scripts/aura-test.sh` ×2 back-to-back post-scaffold (logs `~/Library/Developer/AURA/build/aura-g14-final1.log`, `aura-g14-final2.log`), both exit 0, `grep -c '^PASSED:'` = 22, `Failed bundles: 0`; `AURAIntegrationTests` bundle 167→169 tests (2 new), 27 suites, all green; live `osascript` transcripts of every driver command quoted above; `codesign --verify --deep --strict` → CODESIGN_OK on the acceptance bundle; acceptance AURA process quit cleanly afterward (`ps aux` confirms none running).
- gate: G1-4 → **passed**.

## SEQ-0031 — 2026-09-12T17:50:00Z — UI-1 — Cognitive completion gate (re-answered with G1-4 resolved) + G1-9 PASSED

All 9 UI-1 gates now read `passed`. Re-answering the five questions fresh
(SEQ-0026 answered them while G1-4 was still blocked; today's defect-fix pass
(SEQ-0029) and the G1-4 scaffold (SEQ-0030) both landed since) rather than
treating the old answer as still current.

1. **What exactly changed since SEQ-0026?** (a) `lastPlanSummary` removed
   entirely — `AuraAppModel.swift`, `AuraAppModel_Runtime.swift`,
   `AuraMenuView_Content.swift`, `ProductUIState.swift` (SEQ-0029); (b)
   `applyConversationState`'s `statusDetail` source narrowed to `.error`-only
   for `event.reason`, `displayStatusDetail` gained a status-title
   short-circuit (SEQ-0029); (c) `AuraAccessibilityID.debugScrollToTop` +
   `AuraScrollAnchor.top` + `AuraAcceptanceTestHooks` + the gated debug button
   added for the G1-4 leg (SEQ-0030); (d) two new tests in
   `AuraAccessibilityIdentifierTests.swift`.
2. **What evidence proves each gate?** G1-1/2/3/5/6/7/8 unchanged from
   SEQ-0018–0025 (not touched by SEQ-0029/0030). G1-4: SEQ-0030 (live
   appear/disappear round-trip ×2 + structural guard citation). G1-9: this
   entry (validator OK below, all gates passed, phase_status set to
   awaiting-approval by this same edit).
3. **What observation would falsify "conversation experience rebuilt"?**
   Unchanged from SEQ-0026, plus: the jump-to-latest affordance staying
   `not-found` after a genuine scroll-away, or staying `found` after tapping
   it and settling, would falsify G1-4 specifically.
4. **Why is UI-2 safe to start?** Same basis as SEQ-0026 (bridge tested,
   suite green, chrome untouched) plus: the G1-4 scaffold is inert for real
   users (proven by `acceptanceTestHooksDefaultToDisabled`) and touches no
   UI-2 surface; the defect fixes (SEQ-0029) removed dead state rather than
   adding any.
5. **What residual risk remains, and why is it outside UI-1?** The
   auto-resize window-sizing behavior observed during the G1-4 leg (window
   grows to fit transcript content on most content changes) is a pre-existing
   window-styling characteristic, not something UI-1 introduced or is scoped
   to change; recorded honestly in SEQ-0030 rather than treated as a gap.
   The `.icon` migration (macOS 27's own plate around the current `.icns`)
   remains a separate, not-yet-started item outside UI-1's gate list.

- verified: `bash ui-improvement-plan/validate-continuity.sh` → to be run
  immediately after this entry and the `CURRENT_PHASE.md` rewrite below, per
  §7 ("run it ... before any transition turn").
- gate: G1-9 → **passed** (contingent on the validator run below reporting
  exit 0 with `phase_status: awaiting-approval` and all 9 gates `passed`).

## SEQ-0032 — 2026-09-13T15:20:00Z — UI-1 — Correction to SEQ-0030's test-count arithmetic (found during owner-requested "tam ve kusursuz" re-verification)

- correction: SEQ-0030 stated "`AURAIntegrationTests` bundle 167→169 tests (2 new)". That arithmetic was wrong — 167 was already the *post-addition* count (165 baseline from SEQ-0027 + the 2 new G1-4 scaffold tests = 167), not a pre-addition count. The correct chain is **165 (SEQ-0027) → 167 (SEQ-0030, +2 new tests)**. No test is missing, mis-registered, or failing — this was a bookkeeping error in the SEQ-0030 prose, not a code or coverage defect. Found by re-running the full suite fresh, from a clean re-read of the working tree, rather than trusting the prior session's written claim.
- evidence: fresh `swift build` (exit 0, incremental, `Build complete! (0.91 secs)`); `./scripts/aura-test.sh` fresh run (log `~/Library/Developer/AURA/build/aura-doublecheck-1.log`, exit 0, 22/22 PASSED, 0 failed bundles) confirms `AURAIntegrationTests`: **`✔ Test run with 167 tests in 27 suites passed`**; `grep -c '@Test('` on `AuraAccessibilityIdentifierTests.swift` = 23, and both new cases ("the debug scroll-away identifier is unique and unlocalized", "the acceptance test-hook gate defaults to disabled") are present by name at their expected line numbers — the tests are real and running, only the ledger's stated delta was off by one iteration. `ledger/CURRENT_STATE.md`'s matching "167→169" line corrected in place to "165→167" (that file is the atomically-rewritten current-truth pointer, not an append-only audit trail, so the wrong figure is fixed rather than left standing).
- also re-verified during this pass, no discrepancies found: `git status --short` matches the exact 10-file diff left at end of prior session (nothing lost or drifted); `validate-continuity.sh` → exit 0, machine coherent, `last_seq 31 == max ledger SEQ 31` (pre-dates this correction's own SEQ number, expected — `last_seq` will move to 32 in the next `CURRENT_PHASE.md` rewrite); `lastPlanSummary`/`plan.title` — zero hits repo-wide (SEQ-0029's removal holds); `applyConversationState`'s `statusDetail` assignment — read fresh, matches SEQ-0029's described `.error`-only narrowing exactly; the G1-4 scaffold wiring (`AuraScrollAnchor.top`, `AuraAcceptanceTestHooks.isEnabled`, `debugScrollToTop`) — read fresh across both `AuraMenuView_Content.swift` and `AuraAccessibilityIdentifiers.swift`, matches SEQ-0030's description exactly; `Resources/AURA.icns` re-extracted via `sips` fresh (512px PNG, 219658 bytes — byte-identical to the evidence PNG generated during the original fix) and visually confirmed still full-bleed.
- second full-suite rerun for the ×2 discipline: `~/Library/Developer/AURA/build/aura-doublecheck-2.log`, exit 0, 22/22 PASSED, 0 failed bundles, `AURAIntegrationTests` 167 tests / 27 suites — consistent with run 1.

APPROVAL: UI-1 -> UI-2 — user token: "go apply be perfect" (explicit English-equivalent imperative to execute UI-2, typed by the user in-conversation 2026-09-13; recorded verbatim — the same literal phrase this ledger already recorded as the precedent English-equivalent for the UI-0 -> UI-1 approval on 2026-09-10, see the APPROVAL line above SEQ-0017) — 2026-09-13 — scope: UI-2 live status feedback phase begins; UI-1 gates all passed at SEQ-0031, validator-confirmed.

## SEQ-0033 — 2026-09-13T12:52:19Z — UI-2 — PHASE OPENED (session start per 00 §5)

- evidence: fresh `git status --short`/`git diff --stat` at session start matched the exact 10-file UI-1-closing diff left by the prior session (nothing lost or drifted); `bash ui-improvement-plan/validate-continuity.sh` → exit 0, machine coherent, all 9 UI-1 gates `passed`, `phase_status: awaiting-approval`; `CURRENT_PHASE.md` and the `PHASE_LEDGER.md` tail (SEQ-0029…0032) read fresh; `00-working-protocol.md` read in full (transition rules §3-§5, A6 approval-token rule); `prompts/UI-2.prompt.md` read in full (gates G2-1…G2-7, hard boundaries, per-gate procedures); design docs read: `03-live-status-feedback.md` (full), `11-motion-system.md` (full, vocabulary table §3 + choreography table §4 + Reduce Motion §6), `13-advanced-surfaces.md` (full, §4 glass-morph pill→emergency-badge ownership note); code baseline read: `AuraDesign.swift:212-247` (`Motion` enum — `standard`/`snappy`/`smooth`/`emergent`/stagger tokens + the `motion(_:)` Reduce-Motion gate already shipped from UI-0, not new plumbing), `AuraDesign.swift:290-325` (`AuraStatusPill` — currently zero animation on any status-driven property, confirming 03 §1's "all change instantly" claim), `AuraMenuView_Content.swift:78-154` (`header` — identity mark deliberately does NOT carry `statusColor`, per its own comment "the app's identity stays visually stable while state changes around it"; only the pill carries status color), `AuraAppModel_Runtime.swift:145-190` (subscription list — confirms no `TTSStartedEvent`/`TTSStoppedEvent` subscription exists yet; only `ConversationStateEvent` drives status today, matching 03 §1's claim), `Tests/AURAIntegrationTests/UI0DesignTokenTests.swift` (confirms G0-5 already fully tests the Motion tokens and the Reduce-Motion helper themselves — G2-1's job is adoption in the pill, not re-proving the tokens); repo-wide grep for `.animation(`/`withAnimation(` in `Sources/AURA/*.swift` → only two hits, both already token-based (`AuraMenuView_Content.swift:439/461`, G1-4 scroll-to-top, `Motion.snappy`) plus one `.animation(nil, value: text)` (draft bubble, G1-3, unrelated) — zero raw-duration animations exist anywhere in the touched-file set today.
- verified: `bash ui-improvement-plan/validate-continuity.sh` (exit 0, VALIDATOR OK, pre-transition state); `date -u` → 2026-09-13T12:52:19Z; `git log -1 --format=%H` at session start = `70027c8` (pre this session's two new commits).
- note: user approval recorded above as an explicit English-equivalent imperative per protocol §3/A6, matching the literal phrase this ledger already established as precedent for a phase-transition token (UI-0 -> UI-1, 2026-09-10). Scope reconciliation for the record: G2-1's hard-boundary file list (`AuraAppModel*.swift`, `AuraMenuView_Content.swift`, `AuraMenuBarPanel.swift`, `AuraOrb.swift`, integration tests, plan ledger) is honored; `AuraDesign.swift` is not in the prompt's own "Hard boundaries" scope-guard list for UI-2, but the `AuraStatusPill` component it must edit for G2-1 lives there — `03-live-status-feedback.md §5`'s own "Files to touch" table explicitly names `AuraDesign.swift` for the `motion(_:)` helper and status-transition modifiers, and the edit is a strictly additive `.animation`/`.transition` adoption with zero pinned-line mutation, the same reconciliation basis SEQ-0017 recorded for UI-1's equivalent boundary gap.

## SEQ-0034 — 2026-09-13T13:04:44Z — UI-2 — G2-1 PASSED (status pill transitions adopt AuraDesign.Motion tokens)

- evidence: `AuraStatusPill` (`AuraDesign.swift:290-336`) previously had zero animation on any status-driven property (confirmed by fresh read before editing, matching 03-live-status-feedback.md §1's "all change instantly" claim). Per the prompt's own per-gate procedure ("`AuraDesign.motion(.standard)` transitions; `smooth` for color shifts"), three separate `.animation(_, value: status)` sites now drive the pill, each routed through the existing `AuraDesign.Motion.motion(_:)` Reduce-Motion gate (no new plumbing — that helper and all five tokens shipped from UI-0/G0-5): (1) the leading `Circle`'s fill — `.smooth` (colour, :306); (2) the title/detail `VStack` — `.id(status)` + `.transition(.opacity.combined(with: .scale(scale: 0.96, anchor: .leading)))` + `.standard` (geometry — the identity-swap crossfade, :318-323); (3) the `glassEffect` tint — `.smooth` (colour, :335). This matches 11-motion-system.md §3's "only springs for geometry, only eases for colour" split exactly, and keeps the pill itself as the sole choreography owner for its own status-change moment — no other view independently re-animates the same transition. The header identity-mark disc (`AuraMenuView_Content.swift:83-96`) was deliberately left untouched: its own comment records a considered, already-shipped decision ("the app's identity stays visually stable while state changes around it") that predates and overrides 03 §4.1's stale proposal to tint it by status — changing that now would silently reverse a decision already made, not adopt a gate. `MenuBarExtra`'s label (`AURA.swift`) is out of scope for both the gate and the file boundary and was not touched, matching 03 §4.1's own instruction that it stay instant.
- evidence (tests): `Tests/AURAIntegrationTests/R9ProductUIStateTests.swift`'s existing view-construction smoke loop (the same loop SEQ-0022 extended for the Orb across all 8 statuses) now also constructs `AuraStatusPill(status:title:detail:).body` for every status in that same loop — proves the new `.id`/`.transition`/`.animation` chain builds cleanly for every `AuraAppStatus` case, not only whichever status happens to be live when `header` renders once elsewhere in the same test file.
- evidence (grep, gate's own verification method): `grep -n '\.animation(\|withAnimation('` across `AuraDesign.swift`/`AuraMenuView_Content.swift`/`AuraMenuBarPanel.swift`/`AuraOrb.swift` → the three new pill sites plus the two pre-existing G1-4 `withAnimation(Motion.snappy)` calls plus the pre-existing G1-3 `.animation(nil, value: text)` — all token-based or explicit `nil`; `grep -n 'Animation\.\(spring\|easeInOut\|easeOut\|easeIn\|linear\)('` across the same four files → hits only at `AuraDesign.swift:218/220/222/225`, i.e. only inside the `Motion` enum's own token definitions. Zero raw-duration animations anywhere outside the token table.
- verified: `swift build` → `Build complete! (2.37 secs)`, 0 errors (first attempt hit the known `sandbox_apply: Operation not permitted` manifest-compile clash, not a real build error — retried outside the sandbox per established project workaround, then succeeded); `./scripts/aura-test.sh` ×2 back-to-back (logs in session scratchpad, not repo-tracked), both exit 0, `grep -c '^PASSED:'` = 22, `Failed bundles: 0`; `AURAIntegrationTests` 167 tests / 27 suites both runs (no count change — the new coverage extends an existing `@Test`'s loop body rather than adding a new discrete test).
- gate: G2-1 → **passed**.
- not committed: no go-ahead given this turn for this specific new UI-2 work — the "go apply be perfect" token authorized committing UI-1's already-finished work and approved the UI-1 -> UI-2 transition, both already complete when it was typed; G2-1 was written after that token, so per this repo's own rule ("a go-ahead covers only the phases/work explicitly completed at the time it's given") it stays uncommitted in git pending a fresh go-ahead, even though it is fully verified and checkpointed here in the ledger.

## SEQ-0035 — 2026-09-13T13:34:23Z — UI-2 — G2-2 PASSED (listening pulse driven by real AudioLevelBridge.inputLevel, transform-only)

- evidence: `AuraStatusPill` (`AuraDesign.swift:290-...`) gained `var inputLevel: Double? = nil` and a `nonisolated static func listeningPulseScale(status:inputLevel:) -> CGFloat` — pure math, 0.9…1.15× while `status == .listening` with a real (clamped) level, 1.0 (neutral) for every other status or a `nil` level, matching 03-live-status-feedback.md §4.2's documented range exactly. Applied to the leading dot via a bare `.scaleEffect(Self.listeningPulseScale(...))` — no `.animation` modifier keyed on the level anywhere, so every 15–30 Hz tick from the real bridge renders instantly (11-motion-system.md §5: "never animate on the inputLevel stream"). Both real call sites now pass the live value: `AuraMenuView_Content.swift`'s header pill and `AuraMenuBarPanel.swift`'s pill, both `inputLevel: model.inputLevel`. 03 §4.2's own "reuse the same AuraLevelMeter component inline under the status pill" in `AuraMenuBarPanel` was not adopted: no component by that name exists anywhere in the codebase (grep-confirmed) — the doc's assumption predates or diverges from what UI-1 actually shipped (UI-1 built `AuraOrb`, not a separate `AuraLevelMeter`), and introducing a new, undocumented-as-gated menu-bar Orb instance would be scope creep beyond G2-2's own concrete verification text ("scale/opacity from inputLevel via transformEffect only"), which this change satisfies exactly. The "paused when panel closed or tab not visible" cadence rule (11 §5) needs no new code: SwiftUI does not evaluate a view's `body` while it is off-screen, so a closed `MenuBarExtra` panel's copy of the pill already never re-renders on level ticks, and the header pill is always-visible chrome (not gated behind any tab), so there is nothing to pause there.
- evidence (tests): `Tests/AURAIntegrationTests/UI2LiveStatusFeedbackTests.swift` (new file, mirroring the existing per-phase convention of `UI0DesignTokenTests.swift`/`UI1ConversationExperienceTests.swift`) — pure-math coverage of the 0…1 range plus out-of-range clamping (`-1`→0.9, `2`→1.15), neutrality for every non-listening status and for a `nil` level, and a source-grep test proving the level is never wired into an `.animation` trigger.
- evidence (double-check caught a real defect in my own new test, not in the production code): the first run of the new source-grep test failed — `source?.contains("value: inputLevel") == false` evaluated `false`, i.e. the substring *was* found. Root cause: the substring existed only inside my own explanatory doc-comment on `listeningPulseScale` ("...with no `.animation(value: inputLevel)` anywhere..."), which the naive grep-style test could not distinguish from real code. Not a production defect — verified by isolating the suite alone via the `swiftpm-testing-helper` binary directly (`--filter UI2LiveStatusFeedbackTests`), which pinpointed the exact failing assertion in under a second, well short of resorting to the interleaved 22-bundle log. Fixed by rewording the comment to describe the invariant without using the literal grep target string; `grep -c "value: inputLevel" Sources/AURA/AuraDesign.swift` → 0 confirms the phrase is gone everywhere, and the reworded test then passes for the right reason (real absence, not comment avoidance).
- also fixed en route (real Swift 6 concurrency defect, not test-only): `listeningPulseScale`'s first version inherited `@MainActor` isolation from the project's module-wide default-actor-isolation setting (same mechanism `AuraAppModel_Interaction.swift:119`'s `pushToTalkAckSample` already works around) and crashed the whole `AURAIntegrationTests` binary silently (no `#expect` line printed, process just stopped after the last suite that happened to run before it — confirmed not a signal/trap via `ls -lt ~/Library/Logs/DiagnosticReports` showing no new crash report) when called from a synchronous, non-isolated test body. Fixed by marking the pure function `nonisolated`, the same pattern already established in this codebase for stateless helpers on `@MainActor`-isolated types.
- verified: `swift build` → `Build complete!`, 0 errors, both after the `nonisolated` fix and after the comment fix; `./scripts/aura-test.sh` ×2 back-to-back (session-scratchpad logs, not repo-tracked), both exit 0, `grep -c '^PASSED:'` = 22, `Failed bundles: 0`; `AURAIntegrationTests` 170 tests / 28 suites both runs (167 baseline + 3 new `UI2LiveStatusFeedbackTests`); direct `swiftpm-testing-helper --filter UI2LiveStatusFeedbackTests` isolation run showed all 3 new tests passing individually post-fix.
- gate: G2-2 → **passed**.
- not committed: same basis as G2-1's entry above — new UI-2 work written after the "go apply be perfect" token, held for a fresh go-ahead.

## SEQ-0036 — 2026-09-13T14:01:09Z — UI-2 — G2-3 PASSED (isSpeakingResponse from real TTS events; bounded equalizer; no synthetic source)

- evidence: `AuraAppModel.swift` gained `@Published var isSpeakingResponse = false`. `AuraAppModel_Runtime.swift`'s `subscribeToStatus` gained two new subscriptions — `TTSStartedEvent.self` → `setSpeakingResponse(true)`, `TTSStoppedEvent.self` → `setSpeakingResponse(false)` — completely independent of `applyConversationState`'s existing `status`/`statusDetail` ownership (unchanged, zero lines touched there). `setSpeakingResponse(_:)` is the sole writer of the flag. Verified real, not vestigial: `emit(TTSStartedEvent(...))`/`emit(TTSStoppedEvent(...))` calls exist in `Sources/AuraAgent/Conversation_TTS.swift` on the actual TTS lifecycle (start, chunk-failure→`.error`, timeout→`.timeout`, normal→`.completed`), confirmed by direct read before wiring the subscription. New `AuraEqualizer` view (`AuraDesign.swift`, 3 fixed-height bars, `accessibilityHidden`) added beside the pill in both real chrome locations (`AuraMenuView_Content.swift`'s header, `AuraMenuBarPanel.swift`), each wrapped in its own `HStack` + a single `.animation(Motion.snappy, value: model.isSpeakingResponse)` — a one-shot rise/drop transition, never a continuously looping "fake audio" animation (11-motion-system.md §2's "nothing else animates" rule: there is no real per-sample TTS level to animate continuously, so the equalizer's bar *heights* are static and only its presence animates).
- scoping decisions recorded honestly (background docs vs. the prompt's own concrete gate text, same pattern as G2-1/G2-2): (1) 03-live-status-feedback.md §4.3's "Stop-reason honesty: if the stop reason is a failure, route through the existing error rendering" was **not** implemented — the actual reroute-to-idle-on-failure behavior lives in `Conversation_TTS.swift`/`Conversation_State.swift` (`Sources/AuraAgent`, outside UI-2's hard-boundary file list entirely), and forcing `status = .error` from the new TTS-stopped handler would fight `applyConversationState`'s very next `ConversationStateEvent(.idle, "speech complete")` write for the same failure (confirmed by reading `onSpeechFinished()`: it transitions to idle unconditionally, not conditionally on the stop reason) — reconciling that ordering is a `Conversation`-FSM-layer change, not a UI-2 chrome change, and is recorded here as a residual/decision point rather than silently dropped. (2) The Orb's existing `isSpeakingResponse: model.status == .speaking` argument (`AuraMenuView_Content.swift`, UI-1/G1-6, already shipped and tested) was **not** swapped to the new `model.isSpeakingResponse` — doing so is technically what the Hard Boundaries text's absolute phrasing ("isSpeakingResponse derives ONLY from real TTS events") would imply project-wide, but G2-3's own concrete verification text names only the new property/equalizer, and refactoring an already-closed, already-tested G1-6 data source carries real regression risk (timing between `ConversationStateEvent(.speaking)` and `TTSStartedEvent` is not proven identical) that this gate's procedure does not ask for. Flagged as a residual item, not silently ignored.
- evidence (tests): `Tests/AURAIntegrationTests/UI2LiveStatusFeedbackTests.swift` — new `@MainActor` event-mapping test constructs a real `AuraEventBus`, calls `model.subscribeToStatus(on:)` directly (matching this file's own established testing convention of driving the model through its real subscription/handler methods rather than mocks), and asserts `TTSStartedEvent` sets the flag and **every** `TTSStopReason` case (`.completed`/`.interrupted`/`.error`/`.timeout`) clears it. A second test proves the no-synthetic-source hard boundary structurally: `AuraAppModel_Runtime.swift` contains zero literal `TTSStartedEvent(`/`TTSStoppedEvent(` constructor calls (only `.self` subscribe tokens), and `isSpeakingResponse = isSpeaking` appears exactly once in that file (the sole writer). `R9ProductUIStateTests.swift`'s existing view-construction loop extended: `AuraEqualizer().body`, plus `AuraMenuBarPanel` constructed for the first time in this test file (it previously had zero construction coverage — a pre-existing gap, now closed as a byproduct of touching that file for G2-2/G2-3), both with `model.isSpeakingResponse` toggled true then reset.
- evidence (double-check caught two real defects in my own new test code, not in production code): (1) `#expect(cond, stringA + stringB)` failed to compile — "cannot convert value of type 'String' to expected argument type 'Comment?'" — the `#expect` macro's comment parameter only accepts a literal/interpolated `Comment`, not an arbitrary computed `String` expression; fixed by writing the message as one interpolated literal. (2) `try #require(String(contentsOf:encoding:))` failed — "call can throw, but it is not marked with 'try'" — this exact macro/throwing-initializer combination does not expand cleanly on this toolchain; `UI0DesignTokenTests.swift`'s `motionHelperDocumentsStaticReadout` already established the working idiom for this exact situation (`try? String(...)` + `#expect(source != nil, ...)`), which this fix now matches instead of inventing a second pattern.
- verified: `swift build` → `Build complete!`, 0 errors; `./scripts/aura-test.sh` ×2 back-to-back (session-scratchpad logs, not repo-tracked), both exit 0, `grep -c '^PASSED:'` = 22, `Failed bundles: 0`; `AURAIntegrationTests` 172 tests / 28 suites both runs (170 baseline + 2 new G2-3 tests); `grep -rn "isSpeakingResponse =" Sources/AURA/*.swift` → exactly the `@Published` declaration and the one `setSpeakingResponse` writer, confirmed before writing the source-grep test.
- gate: G2-3 → **passed**.
- not committed: same basis as G2-1/G2-2's entries above — new UI-2 work written after the "go apply be perfect" token, held for a fresh go-ahead.

## SEQ-0037 — 2026-09-13T14:14:02Z — UI-2 — G2-4 PASSED (emergency badge: emergent motion, one pulse, static under Reduce Motion; emergency-stop path untouched)

- evidence: new `AuraEmergencyBadge` (`AuraDesign.swift`) — `Label(title, systemImage: "hand.raised.fill")` tinted `AuraDesign.Palette.cautious` (the amber token, not a raw `.orange` literal), pairing symbol + colour + real text so no meaning is carried by colour alone (F-005 discipline extended to the badge). Read the actual emergency **control** first to confirm it is a completely separate, untouched surface: `AuraMenuView_Tabs.swift` owns the F-005-tested stop/rearm toggle (`emergency.group`/`emergency.stop`/`emergency.rearm` copy keys, pinned by `EmergencyControlLocalizationTests`) and is outside UI-2's hard-boundary file list entirely — not read for editing, only to confirm the boundary. This badge is the *passive indicator*, wired into both real chrome locations that already show the status pill: `AuraMenuView_Content.swift`'s header (previously had **no** emergency indicator at all — confirmed by grep before adding one) and `AuraMenuBarPanel.swift` (previously a bare `Label(...).foregroundStyle(.orange)`, now using the shared component and token colour). Motion: each call site wraps the conditional badge in `.transition(.scale(scale: 1.3).combined(with: .opacity))` + a single `.animation(Motion.motion(Motion.emergent), value: model.emergencyStopActive)` — SwiftUI plays that transition once on insertion (per 11-motion-system.md §4: "badge appears instantly, one single pulse, then static" — the only permitted pulse in the whole motion system) and the badge then holds its final state with no further motion; Reduce Motion resolves the animation to `nil`, so the same appearance happens with zero pulse, fully static.
- scoping decision recorded honestly (same pattern as G2-1/G2-2/G2-3): 03-live-status-feedback.md §4.4 also says "the main window header disc switches to the same treatment" (i.e. the identity-mark icon itself re-tints amber during emergency). Not implemented — `AuraMenuView_Content.swift:80-82`'s own comment records a deliberate, already-shipped decision that the identity mark's colour stays stable while the pill beside it carries status; the new header badge already achieves 03 §2's actual goal ("unmistakable at a glance, in every surface") by adding a second, unmissable amber+symbol+text element to the same header region, without reversing that prior decision. Flagged as a residual/decision point, not silently dropped.
- evidence (tests): `Tests/AURAIntegrationTests/UI2LiveStatusFeedbackTests.swift` — two new source-grep tests: (1) confirms both call sites render `AuraEmergencyBadge(`, route through `Motion.emergent`, and never contain `repeatForever`/`.repeating(` (structurally rules out a future regression into a looping animation); (2) confirms the component itself pairs `hand.raised.fill` with real text, never colour alone. `R9ProductUIStateTests.swift`'s view-construction loop extended: `AuraEmergencyBadge(...).body` plus `model.emergencyStopActive` toggled true/false around fresh `menu.header`/`AuraMenuBarPanel` constructions.
- evidence (regression check, the gate's own explicit demand): the full emergency-control test set ran **unchanged** and green — `"emergency stop cancels a pending confirmation and persists redacted trace"`, `"Emergency stop at the act stage halts before the next step of the same plan"`, `"Emergency stop at the confirmation stage preempts the confirmation challenge"`, plus the `EmergencyControlLocalizationTests` suite (F-005, all included in the 174-test/28-suite total) — all passed, zero test files touched under that suite, confirming the actual emergency-stop mechanism and its localization are unaffected by this purely additive, chrome-only change.
- verified: `swift build` → `Build complete!`, 0 errors; `./scripts/aura-test.sh` ×2 back-to-back (session-scratchpad logs, not repo-tracked), both exit 0, `grep -c '^PASSED:'` = 22, `Failed bundles: 0`; `AURAIntegrationTests` 174 tests / 28 suites both runs (172 baseline + 2 new G2-4 tests); `grep -i emergency` on the second run's log shows the three named emergency-control tests passing, unedited.
- gate: G2-4 → **passed**.
- not committed: same basis as G2-1/G2-2/G2-3's entries above — new UI-2 work written after the "go apply be perfect" token, held for a fresh go-ahead.

## SEQ-0038 — 2026-09-13T14:39:25Z — UI-2 — Owner-requested "tam ve kusursuz" re-verification of G2-1..G2-4 (no discrepancies found)

- request: owner asked "tam ve kusursuz uygulandığından emin ol" ("make sure it was implemented fully and flawlessly") after G2-1..G2-4. Per established project discipline (see the "how to apply" note in project memory, and the SEQ-0032 precedent it cites), this is a genuine re-verification trigger, not reassurance-seeking — answered by re-diffing the actual code against the real gate text fresh, not by re-asserting the prior summaries above.
- verified fresh, from a clean re-read of the working tree (not carried forward from earlier in this session): `git status --short` / `git diff --stat` — exactly the 9 expected files (`AuraAppModel.swift`, `AuraAppModel_Runtime.swift`, `AuraDesign.swift`, `AuraMenuBarPanel.swift`, `AuraMenuView_Content.swift`, `R9ProductUIStateTests.swift`, `UI2LiveStatusFeedbackTests.swift` (new), `CURRENT_PHASE.md`, `PHASE_LEDGER.md`) — no other file touched, confirming the UI-2 hard-boundary scope (`AuraOrb.swift`, `AuraMenuView_Tabs.swift`, `ProductUIState.swift`, `AURA.swift` all remain untouched); `validate-continuity.sh` → exit 0, machine coherent. `AuraDesign.swift:290-404` re-read in full: `AuraStatusPill`'s three `.animation(_, value: status)` sites resolved on paper against SwiftUI's actual nested-scoping rule (an inner modifier's own `.animation(_, value: sameKey)` takes precedence for its own subtree; an outer one only fills in where no inner override exists) — confirms the Circle's `smooth` and the VStack's `standard` each genuinely govern their own subtree, and the outer `smooth` genuinely only reaches the otherwise-unclaimed `glassEffect` tint, exactly as G2-1's evidence claimed; not a coincidence of the tests passing. `listeningPulseScale`'s arithmetic re-checked by hand (0.9 + level×0.25 → 0.9/1.025/1.15 at level 0/0.5/1) against 03-live-status-feedback.md §4.2's "0.9–1.15× range" — matches exactly. `AuraAppModel_Runtime.swift`'s `applyConversationState` diffed specifically (`git diff` hunk-isolated) — confirmed **zero** lines changed inside that function; every G2-3 addition is purely appended after it. `grep -c "@Test(" Tests/AURAIntegrationTests/UI2LiveStatusFeedbackTests.swift` → 7, matching 167 (UI-1 baseline) + 7 = 174, the exact count every prior SEQ entry claimed — no SEQ-0030-style off-by-one this time. `git ls-remote origin main` → `f24ebfb…`, identical to local `HEAD` — the two already-pushed commits are intact, nothing lost or drifted.
- verified fresh (build + full suite, ×2, from a **clean** build directory — not the incrementally-built one used during implementation): `BUILD_DIR=/tmp/aura-doublecheck-fresh ./scripts/aura-test.sh` run 1 → exit 0, 22/22 PASSED, `Failed bundles: 0`, `AURAIntegrationTests` **174 tests / 28 suites**; run 2 (same directory, back-to-back) → identical result. A clean build directory rules out any incremental-cache blind spot the per-gate incremental runs during implementation could have missed.
- outcome: no discrepancy found this pass — unlike SEQ-0032, this re-verification did not surface a new defect. All four gates' evidence stands as recorded at SEQ-0034/0035/0036/0037.

## SEQ-0039 — 2026-09-13T16:21:21Z — UI-2 — G2-5 PASSED (confirmation card entrance per 11 §4; five fail-closed paths re-verified unchanged)

- evidence: entrance motion added at `AuraMenuView_Content.swift`'s conversation-tab call site only — `.transition(.opacity.combined(with: .scale(scale: 0.97, anchor: .top)))` + `.animation(Motion.emergent, value: challenge.requestID)`. Sober by construction: scale never exceeds 1.0 (no overshoot/bounce), `emergent` is `easeOut(duration: 0.15)` (fast, no spring bounce), and the whole card (including its `AuraPanel(tint:)` heading) fades/scales in as one atomic unit — the tinted heading is present at every frame of the transition, satisfying "present at first paint" by construction rather than by staged sequencing. Motion-token choice cross-checked three ways: `AuraDesign.swift`'s own `Motion.emergent` doc-comment has named "confirmation cards" as a use case since UI-0 (predates this gate); `UI-2.prompt.md`'s G2-5 per-gate procedure says "confirmation card entrance uses motion.emergent" explicitly; only `11-motion-system.md` §4's choreography table row for this moment says `standard` — the outlier, flagged as stale relative to both other sources, `emergent` used per the two agreeing, more specific/authoritative sources. `AuraConfirmationCard`'s own body (`AuraMenuView.swift`) — the Deny-before-Allow button order — was **not touched at all** (zero diff), so "preserved" holds by construction, not by re-verification.
- scoping decision recorded honestly, higher-stakes than G2-1..G2-4's: `AuraConfirmationCard` also renders inside `AuraSettingsView` (`AuraMenuView.swift`, out of UI-2's hard-boundary scope), inside a `ScrollViewReader`/`Form` with an `.onChange(of: model.pendingConfirmation != nil)`-triggered `scrollProxy.scrollTo` — a fix for a **real, previously-shipped bug** (a confirmation card existing but scrolled off-screen, expiring unanswered) documented in `archive/runtime-completion/state/EV-SP-030-20260831-R11-LIVE-GATE-02.md`, which explicitly states this exact confirmation-visibility architecture carries live security consequences ("a confirmation the user cannot see must never be auto-answered"). Read that evidence record in full before deciding: it also documents a **still-open, not-closed** blocker in that exact code path (the Settings-window card-visibility issue). Given that, entrance motion was deliberately **not** added to the Settings-tab call site — the risk of a transition/animation interacting with the scroll-to-anchor timing on an already-fragile, evidence-tracked fail-closed surface outweighs visual consistency for a background-doc aspiration. This is a stricter version of the AuraDesign.swift-style reconciliation used at G2-1: here the file stays fully untouched rather than partially reconciled.
- evidence (tests): `Tests/AURAIntegrationTests/UI2LiveStatusFeedbackTests.swift` — two new source-grep tests: (1) confirms the conversation-tab call site still renders the card, uses `Motion.emergent`, and never loops; (2) pins Deny-before-Allow in source order in `AuraMenuView.swift` as a regression guard (fails if a future edit ever reorders the buttons). `R9ProductUIStateTests.swift`'s view-construction loop extended: `menu.conversationTab` constructed with a real `model.pendingConfirmation` set and the `.conversation` tab selected, exercising the new `.id`/`.transition`/`.animation` wrapper.
- evidence (fail-closed regression check, the gate's own explicit demand — broader than anticipated): full-suite run surfaced far more confirmation/fail-closed coverage than the three tests in `ConfirmationSheetFailClosedTests.swift` alone — `"dismissing an unanswered confirmation denies it"`, `"an already-answered confirmation is not re-denied"`, `"dismissing with nothing pending is a no-op"`, `"window close dismisses a pending confirmation..."`, `"emergency stop cancels a pending confirmation..."`, plus `confirmationExpiryDenies()` and `confirmationChallengeExpiryEnforced()` (the 60s-expiry path) and a dozen more confirmation-policy tests across `AuraCoreTests`/`AuraAgentTests` — all passed, all unedited files, confirming this purely additive, single-call-site motion change caused zero fail-closed regression anywhere in the suite.
- verified: `swift build` → `Build complete!`, 0 errors; `./scripts/aura-test.sh` ×2 back-to-back (session-scratchpad logs, not repo-tracked), both exit 0, `grep -c '^PASSED:'` = 22, `Failed bundles: 0`; `AURAIntegrationTests` 176 tests / 28 suites both runs (174 baseline + 2 new G2-5 tests).
- gate: G2-5 → **passed**.
- not committed: same basis as G2-1..G2-4's entries above — new UI-2 work written after the last go-ahead ("push commit merge deploy" covered only what was finished at that point), held for a fresh go-ahead.

## SEQ-0040 — 2026-09-13T16:35:52Z — UI-2 — G2-6 PASSED (full verification loop + governance)

- evidence (suite ×2): `./scripts/aura-test.sh` full 22-bundle loop, run 1 and run 2 back-to-back, both exit 0, 22/22 PASSED, `Failed bundles: 0`; `AURAIntegrationTests` 176 tests / 28 suites both runs (session-scratchpad logs, not repo-tracked).
- evidence (a11y IDs per contract): `git diff --stat HEAD -- Sources/AURA/AuraAccessibilityIdentifiers.swift` and `git log -1 --format=%H -- Sources/AURA/AuraAccessibilityIdentifiers.swift` → last touched at `f24ebfb` (the UI-1 closure commit), zero diff since. G2-1..G2-5 added no new buttons/interactive controls — only visual chrome (motion, a hidden equalizer, a non-interactive badge Label, a card entrance transition) — so the contract required no new identifiers, verified rather than assumed.
- evidence (live driver leg): fresh signed acceptance bundle at `BUILD_DIR=/tmp/aura-g26-accept` (release, `AURA Stable Local Signing`, `codesign --verify --deep --strict` → CODESIGN_OK), launched via direct exec (`Contents/MacOS/AURA`, not `open`). `osascript scripts/sp011-acceptance/aura-drive.applescript window` → `OK window-already-open`; `status` → `OK AURA durumu: Kısıtlı` (baseline, unrelated to the badge). A one-off AX-tree text-dump script (reusing the driver's own value/description/title read order, applied to the whole window rather than just the transcript subtree, since the badge sits in the header) confirmed **no** "Emergency"/"Acil" text present. Sent the real emergency shortcut via `osascript -e 'tell application "System Events" to key code 53 using {command down, shift down}'` (⌘⇧Esc, the exact combo `EmergencyShortcutMonitor.isEmergencyShortcut` checks). The follow-up dump showed the status pill text change to "Durduruldu. Üretilen giriş açıkça yeniden etkinleştirilene dek devre dışı" **and** — the new G2-4 surface — **"Acil durdurma etkin"** now present, exactly the new header badge with no prior indicator to compare against. App quit cleanly via `osascript -e 'tell application "AURA" to quit'`, confirmed by `pgrep` exit 1. Scope note: the rearm/removal half was not separately live-driven — the rearm button (`AuraMenuView_Tabs.swift`) carries no accessibility identifier to click non-disruptively, and adding one would touch two out-of-scope files for an ad-hoc verification; removal rests on SwiftUI's symmetric `.transition` guarantee (the same modifier governs both directions by construction) plus the already-green, unedited `resetEmergencyStop()` model-level tests.
- evidence (ADR + repo ledger + CURRENT_STATE): `docs/decisions/ADR-060-ui2-live-status-feedback.md` written (Context/Decision per gate/Alternatives/Security/Operational/Migration/Validation evidence/Consequences, matching the ADR-058 template); `ledger/PROJECT_LEDGER.md` appended (2026-09-13T16:34Z entry); `ledger/CURRENT_STATE.md` atomically rewritten with a new top entry covering G2-5/G2-6 (the G2-1..G2-4 entry directly below it is left standing, per this file's established convention of layering dated entries rather than rewriting history).
- verified: all of the above; `bash ui-improvement-plan/validate-continuity.sh` → to be run immediately after this entry and the `CURRENT_PHASE.md` rewrite below.
- gate: G2-6 → **passed**.
- not committed: same basis as every UI-2 gate above — held for a fresh go-ahead, now covering the ADR, PROJECT_LEDGER, and CURRENT_STATE files too.

## SEQ-0041 — 2026-09-13T16:36:21Z — UI-2 — Cognitive completion gate (all 7 gates passed) + G2-7 PASSED

Answering the five questions fresh, per `prompts/UI-2.prompt.md`'s "Cognitive completion gate" section, rather than treating any prior gate's evidence as sufficient on its own.

1. **What exactly changed? (files + line ranges)** `Sources/AURA/AuraDesign.swift` (`AuraStatusPill` :290-359 — motion + pulse; new `AuraEqualizer` :361-383; new `AuraEmergencyBadge` :385-404), `Sources/AURA/AuraAppModel.swift` (:129-134, `isSpeakingResponse`), `Sources/AURA/AuraAppModel_Runtime.swift` (:187-206, two new subscriptions + `setSpeakingResponse`), `Sources/AURA/AuraMenuView_Content.swift` (header :108-133 pill/equalizer/badge group; confirmation card entrance :358-370), `Sources/AURA/AuraMenuBarPanel.swift` (:20-33 pill/equalizer group, :57-63 badge). Tests: `Tests/AURAIntegrationTests/UI2LiveStatusFeedbackTests.swift` (new, 9 tests across G2-2..G2-5); `Tests/AURAIntegrationTests/R9ProductUIStateTests.swift` (view-construction loop extended for the pill/equalizer/badge/confirmation-card paths). Governance: `docs/decisions/ADR-060-*.md` (new), `ledger/PROJECT_LEDGER.md` (appended), `ledger/CURRENT_STATE.md` (new top entries), `ui-improvement-plan/ledger/{CURRENT_PHASE,PHASE_LEDGER}.md`.
2. **What evidence proves each gate?** G2-1: SEQ-0034 (motion-split reasoning + grep proof). G2-2: SEQ-0035 (pure-function tests + `nonisolated` fix + transform-only grep proof). G2-3: SEQ-0036 (real-EventBus event-mapping test covering all 4 stop reasons + single-writer grep proof). G2-4: SEQ-0037 (source-grep motion proof + the full emergency-control suite green unedited). G2-5: SEQ-0039 (Deny-first source-order pin + the full fail-closed suite, far broader than anticipated, green unedited). G2-6: SEQ-0040 (suite ×2, a11y-ID zero-diff proof, live driver leg with a genuine before/after AX-tree observation, ADR + ledgers). A genuine mid-phase re-verification (SEQ-0038, after G2-1..G2-4) found no discrepancy via a clean-build rerun and line-by-line source re-read.
3. **What observation would falsify "live feedback wired"?** The status pill popping instantly on a real status change (no crossfade) would falsify G2-1. The listening dot staying a fixed size during a real PTT turn, or wobbling on every model-driven re-render regardless of level, would falsify G2-2. `isSpeakingResponse` staying `false` during a real TTS-speaking turn, or the equalizer appearing without a live `TTSStartedEvent`, would falsify G2-3. The header showing no indicator after a real emergency-stop trigger would falsify G2-4 — this was the one live-observed claim, and it held (see SEQ-0040). The confirmation card popping instantly with no fade/scale on a real confirmation would falsify G2-5.
4. **Why is UI-3 safe to start?** The motion vocabulary is now genuinely in use (not just token-defined) across five distinct moments, proving the `Motion.motion(_:)` Reduce-Motion gate and the spring/ease split hold under real adoption, not only under G0-5's original synthetic tests. `isSpeakingResponse` is now a real, tested, independently-owned published property UI-3 can read without re-deriving TTS state itself. The full suite is green (176/28, ×2, plus the G2-6 sweep) and the fail-closed/emergency-control surfaces show zero regression across five gates of chrome changes touching adjacent code.
5. **What residual risk remains, and why is it outside UI-2?** (a) TTS-failure stop-reason routing into the error UI (03 §4.3) is unimplemented — it requires a `Conversation`-FSM-layer change in `Sources/AuraAgent`, outside UI-2's hard-boundary scope by definition, not a chrome gap. (b) The Orb's speaking-state source (`model.status == .speaking`, UI-1/G1-6) was not swapped to the new `isSpeakingResponse` — a deliberate, recorded choice to avoid regressing an already-shipped, already-tested surface for a timing guarantee no G2 gate required proving; genuinely outside UI-2 because touching it would mean re-opening a closed UI-1 gate's evidence, not extending UI-2's own scope. (c) The Settings-tab confirmation-card call site (`AuraSettingsView`) still has no entrance motion — deliberately outside scope given its documented, still-open fail-closed fragility (`EV-SP-030-20260831-R11-LIVE-GATE-02`).

- evidence: `bash ui-improvement-plan/validate-continuity.sh` → to be run immediately after this entry and the `CURRENT_PHASE.md` rewrite below, per §7 ("run it ... before any transition turn").
- gate: G2-7 → **passed** (contingent on the validator run below reporting exit 0 with `phase_status: awaiting-approval` and all 7 gates `passed`).
